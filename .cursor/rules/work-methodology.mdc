---
description: 东雪莲 Bot 项目开发方法论 — 修 bug、加功能、拆模块时的硬规则
alwaysApply: true
---

# 工作方法论

## 改代码前

1. 先读仓库：rg 搜入口、调用点、测试覆盖、现有风格、package.json scripts、dirty worktree
2. 先确认事实，不靠猜；找不到证据就标注"待验证"
3. 先找最短可验证路径：能用一个最小输入复现的，不要上来改一大片

## 改代码时

1. 最小施工面：按依赖顺序改，不混无关重构，不重排大段逻辑
2. 最小 diff：不顺手格式化整个文件，不改编码，不碰无关文件
3. 先止血再缝合：先让链路恢复，再补测试，最后整理代码
4. 不越界：不做破坏性 git 操作，不加新框架/新依赖，不把小需求扩成大重构

## 改完之后

1. `node -c <改动文件>` 确认语法
2. `npm run check` 语法门卫
3. `npm run test:quick` 结构契约
4. `npm run test:scenario` 行为链路（涉及真实消息链路时）
5. `npm test` 提交前完整验证
6. `git diff --check` 检查空白字符

## 测试原则

- 行为变更优先加 scenario，不加源码字符串扫描
- 第一条测试先复现用户真实失败输入
- 不 mock 掉被测对象本身
- 断言要对准事故（不能只断言 length > 0）
- monkey patch 必须 try/finally 恢复

## 卡住时

- 一条路不通先问是不是路错了，不在同一假设上反复加码
- 先拆层看（输入/转换/状态/输出/外部依赖）
- 先换视角（报错/日志/测试/数据/配置/调用链交叉看）
- 先缩小范围：构造最小复现
