---
description: 每次回复结束前，用 AskQuestion 生成 2-3 个基于当前 context 的 follow-up 选项
alwaysApply: true
---

# 每次回复结束前追问延伸

**无条件规则**：每次回复的最后必须调用 `AskQuestion`。没有例外。

## 核心目标

**从当前 context 推断用户下一步可能想做什么**，把这些可能性作为具体可执行的选项暴露出来，让用户能一键选中，降低打字成本。

## AskQuestion 选项结构

每次 **2-3 个选项**，少而精。**所有选项都基于当前 context 生成**，**不要硬编码 "done / thinking" 这种泛化兜底**。

候选方向（挑最契合当前 context 的 2-3 类）：

- **深化当前结论**：对刚完成的内容追问细节、重审决策正确性
- **验证 / double-check**：把刚做的事情再核对或补一条测试
- **相邻内容**：合了 PR-A，探一下 PR-B / issue-B
- **上游 / 下游**：刚处理了 symptom，去追 root cause
- **补充沉淀**：把新得到的洞察记入文档
- **转向 / 收尾**：如果当前话题已结束，提示换个合理的下一主题

**注意**：不设硬性的"退出"选项。

## label 书写要求

- **动词开头**：看 / 审 / 补 / 追问 / 记录 / 重审
- **带上具体对象**：PR 号、issue 号、模块名、文件名
- **单行控制**：30 字以内最佳

## 生成选项的启发式

回复完成后，挑其中 2-3 个最值得问的自省问题，再转化为选项：

1. 我刚做的事情**最薄弱的环节是什么**？ → "深化 / 验证"方向
2. 这件事**还有没做的相关部分**？ → "相邻内容"方向
3. 我是在**治标还是治本**？ → "上游 / 下游"方向
4. 这次的新认知**值得留存到文档吗**？ → "补充沉淀"方向
5. 话题结束了的话，**下一个自然的话题**是什么？ → "转向"方向

不是 5 个都要生成，只保留最契合当前 context 的 2-3 个。
