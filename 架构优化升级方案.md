# 架构优化升级方案

## 目标

- 语音克隆从“上传按钮”升级为“可管理、可复用、可追踪”的资产系统。
- 保持现有 `__cloned__` 兼容，不破坏旧人格配置。
- 所有改动继续收口到稳定模块，不回流到 `index.js` / `handler.js`。

## 已完成

### 语音资产第一阶段

- 新增 `lib/voice-assets.js`，统一维护 `DATA_DIR/ai-voice-assets.json`。
- Dashboard 语音克隆支持显示名、备注、试听文本。
- 已克隆音色列表可试听、保存、删除、启用。
- 仍兼容旧的 `ai-voices/<personaName>.<ext>` 样本。

### 语音资产第二阶段

- 新克隆样本已改为独立 `voiceAssetId`。
- 样本文件按资产 ID 保存。
- `tts.resolvePersonaVoice()` 已优先读取 `voice_asset_id`。
- 同一人格可保留多个克隆版本。
- 同一克隆音色可复用给多个人格。
- 删除时返回引用者信息，避免误删。

## 当前完成状态

- 没有部署、重启或推送。
- 本地构建、语法检查、quick、scenario、插件测试和 Dashboard 浏览器点击 smoke 已通过。

## 已验证

- `npm run build --prefix packages/koishi-plugin-dashboard/frontend`
- `npm run check`
- `npm run test:quick`
- `npm run test:scenario`
- `npm run test:plugins`
- `node scripts/dashboard-click-smoke.js`

## 后续可选

- 如需生产确认，先按部署规则征得用户同意，再做服务器部署/重启/真实 Dashboard 点击验证。

## 风险提示

- 不要把语音资产逻辑再塞回 `handler.js`。
- 不要物理重命名克隆文件来做“改名”。
- 不要跳过管理员权限和 `DATA_DIR` 运行时隔离。
