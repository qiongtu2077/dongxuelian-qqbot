# 架构优化升级方案

## 目标

- 语音克隆从“上传按钮”升级为“可管理、可复用、可追踪”的资产系统。
- 保持现有 `__cloned__` 兼容，不破坏旧人格配置。
- 所有改动继续收口到稳定模块，不回流到 `index.js` / `handler.js`。

## 已完成

### 语音资产第一阶段

- 新增 `lib/voice-assets.js`，统一维护 `DATA_DIR/ai-voice-assets.json`。
- Dashboard 语音克隆支持显示名、备注、试听文本。
- 已克隆音色列表可试听、保存、删除、启用。
- 仍兼容旧的 `ai-voices/<personaName>.<ext>` 样本。

### 语音资产第二阶段（进行中）

- 新克隆样本已改为独立 `voiceAssetId`。
- 样本文件按资产 ID 保存。
- `tts.resolvePersonaVoice()` 已优先读取 `voice_asset_id`。
- 删除时开始返回引用者信息，避免误删。

## 当前暂停点

- 已停在“多资产复用 + 选择具体资产绑定”的收口阶段。
- 还没有做完整的浏览器点击回归和完整测试流水线收尾。
- 没有部署、重启或推送。

## 待完成

- 把前端的克隆音色选择器彻底跑通。
- 补全多个人格复用同一音色的验证场景。
- 跑完 `npm run check`、`npm run test:quick`、`npm run test:scenario`、浏览器模拟点击。
- 最后再决定是否需要服务器级模拟测试。

## 风险提示

- 不要把语音资产逻辑再塞回 `handler.js`。
- 不要物理重命名克隆文件来做“改名”。
- 不要跳过管理员权限和 `DATA_DIR` 运行时隔离。
