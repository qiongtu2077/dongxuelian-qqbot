# 架构优化升级方案

## 背景

当前后端大单体已经拆成多个职责模块，继续按行数拆分收益不高。本轮优化聚焦审计报告中的 Dashboard 架构建议：降低 Vue Options API `setup()` 手动 `return` 遗漏变量导致白屏的风险，并给 Dashboard 增加可重复执行的浏览器点击模拟验证。

## 本轮目标

1. 将高风险 Dashboard 组件迁移到 `<script setup>`：
   - `App.vue`：应用壳、主题、侧边栏、登录/解锁状态。
   - `PersonaPanel.vue`：人格、世界观、语音配置，曾出现过 `setup()` return 遗漏导致白屏。
   - `ConfigPanel.vue`：模型配置、自定义供应商、Fallback 链。
2. 保持用户可见功能和接口不变，不重排大段模板，不混入后端安全修复。
3. 新增浏览器级用户模拟点击脚本，覆盖登录后解锁、主题切换、侧边栏导航、配置表单、人格式编辑、世界观表单等关键操作。

## 本轮不做

1. 不迁移 `DeployPanel.vue`。它的主要问题是流程状态过大，单纯换 `<script setup>` 不能解决根因；应在下一阶段拆成本地部署流程、远程部署流程、卸载预览等 composable 或子组件。
2. 不改服务器部署脚本、不重启线上服务、不推送仓库。
3. 不改现有未提交的 Dashboard 后端安全修复文件，避免把审计 Phase 4 和本轮架构优化混在一起。

## 设计原则

1. 迁移只消除手动 `return` 暴露模板变量的风险，保留原有响应式状态、函数名、按钮文案和 API 调用。
2. 对模板变量依赖多的组件优先迁移；低风险小组件暂不动。
3. 点击模拟使用真实浏览器加载 Vite 页面，通过 mock Dashboard API 避免写生产数据。
4. 验证结果必须包含：
   - `npm run build --prefix packages/koishi-plugin-dashboard/frontend`
   - `npm run check`
   - Dashboard 点击模拟脚本完整通过
   - 如涉及后端行为，再追加 `npm run test:quick` / `npm run test:scenario`

## 验收清单

1. Dashboard 页面能在登录态直接打开。
2. 侧边栏解锁后可切换：功能地图、指令速查、模型配置、人格实验室、API Keys、黑白名单、安全设置、日志中心、系统状态、莲莲图集。
3. 主题选择弹窗可打开并切换主题。
4. ConfigPanel 可操作：
   - 切换供应商后模型下拉更新。
   - 添加/删除自定义供应商。
   - 添加/删除 Fallback 步骤。
   - 触发保存按钮时进入 loading 并收到 mock 成功响应。
5. PersonaPanel 可操作：
   - 编辑人格后表单填充并滚动到编辑区。
   - 创建/保存按钮能显示校验或成功结果。
   - 世界观编辑/取消、创建校验正常。
   - 语音配置选择人格后音色/风格同步。
6. 浏览器控制台无 Vue runtime error，页面无空白。

## 后续建议

1. 下一阶段优先处理 `DeployPanel.vue`：先提取纯流程状态和步骤计算，再考虑模板拆分。
2. 再下一阶段处理 `AgentPanel.vue` 和 `ControlPanel.vue`：将 API loading、管理员弹窗重试、保存反馈提成通用 composable。
3. 如果未来做线上验证，先确认服务器无未保存修改，再使用既有脚本部署/重启；不手写 SSH 重启命令。
