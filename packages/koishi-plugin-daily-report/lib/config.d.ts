/**
 * MODULE: 配置和常量定义。
 * 边界: 只定义静态配置，不含业务逻辑。
 */
declare const _default: {
    DATA_DIR: string;
    TIMEOUTS: {
        aiRequest: number;
        cooldown: number;
    };
    FORCE_TEMPLATE: string;
};
export = _default;
