# 《优化建议》持续扫描记录

扫描开始日期：2026-05-23

本文件承接上一份已归档的《优化建议》。只记录新一轮交替扫描中确认真实存在、仍需要处理的 bug 或风险；可疑点必须继续溯源，确认不了不写。

## 长任务续航提醒

每次上下文压缩或恢复后，先复述并继续执行当前任务：服务器一轮、本地一轮交替扫描；扫一轮就写一次本文件；只记录真实存在的 bug，可疑点必须继续溯源直到确认或排除。扫描范围不只代码安全和架构，还包括群聊接话、用户体验、上下文感知、图片/历史图片感知、文件读取、语音、搜索/fetch、人格文件、人格串台和 OOC 风险。不得凭记忆合并多轮，不得把未确认猜测写成 bug。

执行边界：
- 涉及 AI 调工具、搜索/fetch、读图、读文件、Agent 行动等能力，优先自由路由；硬编码只用于权限边界、安全兜底、异常降级和少数确定性入口。
- 文本文档临时改动不按脏工作区处理，但不得覆盖用户或其他任务留下的草稿。
- 每轮扫描要写明范围、证据、影响、建议修复和验证方式；如果本轮没有新真实 bug，也要记录“未发现新 bug”和已排除的疑点。
- 继续追踪早先的人格串台根因，但不能把兜底话术人格化冒充为根因已修复。

## 服务器第 1 轮

范围：部署后的生产日志尾部、回复分段日志、图片/文件历史样本、启动状态、服务器发布态。重点核对上一轮修复后是否仍有刷屏、内部草稿泄漏、固定兜底、图片历史空壳、文件历史异常、搜索/fetch 失败或崩溃。

结论：本轮未发现新的真实 bug。近期日志尾部没有新的 `TypeError`、`ReferenceError`、`SyntaxError`、`chat failed`、`agent auto-route failed`、旧固定兜底、`看不到图片`、`没有拿到可靠结果` 或内部工具计划泄漏；`[reply] sent` 近期最大为 `parts=2`，没有复现 6-17 段刷屏。启动日志显示 `adapter connect to server` 和 `dongxuelian-ai 1.1.6 loaded`。

已排除的疑点：
- 图片历史不是空壳：`data/image-history/942033342.json`、`620992167.json`、`917625559.json` 已出现部署后的 QQ 图片记录，包含 `url`、`file`、`conversationKey`、`userId`、`ts` 等字段。部分旧群文件仍是 13 字节空壳属于历史无图样本，不能当作新 bug。
- `thinking output in reply` 日志只看到部署前旧记录，部署后尾部未见新的 thinking 泄漏重试。
- `data/file-history` 暂无样本输出，但本轮日志尾部没有文件消息现场，缺少真实输入，不能写成文件链路 bug。

仍需继续观察：
- 生产目录 Git 发布态仍不干净且 `HEAD` 落后远端，这是上一轮已记录的运维风险；本轮不重复新增。后续若要彻底处理，需要单独备份和发布态整理计划，不能用破坏性命令盲清。
- 真实 QQ/NapCat 的搜索、fetch、读文件、语音和人格串台还需要下一轮本地/服务器交替扫描继续覆盖。

## 本地第 1 轮

范围：回复分段、Agent 工具上下文桥接、文件读取链路、文件下载安全、硬编码读文件兜底、Agent 结果转述输出守卫。重点复核上一轮修复后是否有新副作用。

结论：回复分段当前已按“自然段优先；无自然段时至少两句话一组；异常多段才合并”的方向工作，不再是逐句刷屏，也没有把已有自然换行糊成单段。Agent 工具上下文只在“刚刚搜到什么来源”等追问注入，普通 `hello` 不注入。文件下载会拒绝 `127.0.0.1`；硬编码“读文件”摘要不会泄漏 `[用户上传文件]` 包裹。新发现 1 个真实 bug。

### L1-1 Agent 文件材料转述坏路会裸发内部文件包裹

真实性：真实存在，可本地隔离复现。

证据：
- `file-analyzer.js` 会把解析后的文件正文包装为内部工具材料：`[用户上传文件: ...]\n---文件内容开始---\n...\n---文件内容结束---`。
- 硬编码“读文件”路径已经会用 `summarizeFileContentForChat()` 转成自然摘要，但 Agent 结果转述路径依赖 `chat(..., { isAgentResult: true })` 的输出守卫。
- 当前 `reply-guard.js` 的 `hasInternalContextLeak()` 只识别 `<user>`、`昵称：`、`发言：`、`[群聊刷到]` 等内部上下文标记，没有识别文件工具包裹。
- 隔离复现：给 `chat()` 的 Agent 转述模式传入文件工具材料，并 mock 转述模型首轮直接返回同一段 `[用户上传文件: plan.txt] ---文件内容开始--- line A line B ---文件内容结束---`；最终 `chat()` 返回该包裹文本，模型调用次数为 1，没有触发重试或人格兜底。

影响：
- 用户追问文件内容时，如果 Agent/模型没有自然总结而是照抄工具结果，QQ 里会出现内部工具包裹，观感像调试输出。
- 这绕过了上一轮“硬编码读文件不直发工具包裹”的修复，只发生在 Agent 结果转述坏路，因此普通 happy path 测试不一定能发现。
- 文件内容本身可能较长，裸包裹还可能和回复分段、上下文保存叠加，让后续聊天记录里混入内部工具格式。

建议修复：
- 把文件工具包裹视为内部上下文泄漏：`hasInternalContextLeak()` 应识别 `[用户上传文件:`、`---文件内容开始---`、`---文件内容结束---`。
- Agent 转述首轮出现该包裹时，按现有人格化纠偏链路重试；重试仍失败时走当前人格兜底，不直接发送包裹。
- 补 scenario：Agent 结果转述模式中，模型首轮照抄文件工具包裹，断言会二次调用模型，最终回复不包含 `[用户上传文件:` 或 `---文件内容开始---`，并能发送自然摘要。

验证方式：
- 修复前隔离复现返回文件包裹且 `calls=1`；修复后应触发重试，返回自然文本。
- 跑文件场景和 chat 场景，确认普通“读文件”兜底、文件追问 happy path 仍通过。

修复状态：已修复。

修复实现：
- `reply-guard.js` 的 `hasInternalContextLeak()` 已把 `[用户上传文件:`、`---文件内容开始---`、`---文件内容结束---` 纳入内部上下文泄漏判定。
- `chat.test.js` 新增 `Agent retell file wrapper leak retries` 场景，模拟 Agent 转述首轮照抄文件工具包裹，断言会重试并最终发送自然摘要，不泄漏包裹标记。

验证结果：
- 修复前隔离复现：`reply` 为文件包裹、`calls=1`、`leaks=true`。
- 修复后隔离复现：`reply="clean file summary"`、`calls=2`、`leaks=false`。
- `npm run check`：通过，语法检查 173 个生产 JS 文件。
- `npm run test:quick`：通过，`passed: 2082, failed: 0, skipped: 1`。
- `npm run test:scenario -- --grep "Agent retell file wrapper"`：实际运行完整 scenario，通过，`passed: 753, failed: 0, skipped: 2`。
