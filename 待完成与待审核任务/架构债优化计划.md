# index.js / chat.js 主干拆分 0 风险待审核计划

> 状态：待审核
> 目标：在不改变用户可见行为、不改变线上配置、不引入循环依赖的前提下，把 `index.js` 与 `chat.js` 的主干职责逐步拆薄。
> 范围：只讨论 `packages/koishi-plugin-dongxuelian-ai/lib/index.js` 与 `packages/koishi-plugin-dongxuelian-ai/lib/chat.js` 的结构拆分，不包含部署、重启、推送、线上改配置。

## 一、当前判断

当前不是“补丁太多拆不了了”，而是已经进入高耦合维护区：

- `index.js` 当前工作区约 86 KB / 1928 行，最大函数 `exports.apply()` 约 698 行。
- `chat.js` 当前工作区约 75 KB / 1479 行，最大函数 `chat()` 约 967 行。
- 两个文件都有架构红线注释，说明项目本来就希望“入口只编排、聊天核心保留所有权、新器官拆模块”。
- 目前已经有一部分拆分成果：`chat-prompt-builder.js`、`reply-guard.js`、`chat-memory.js`、`file-followup-guard.js`、`random-reply-mode.js`、`group-scene-index.js` 等，说明拆分路径是可行的。

本计划的核心原则：**先做函数溯源和行为锁定，再做纯搬运；每一步只移动一个清晰职责，不顺手改逻辑。**

## 二、0 风险定义

这里的“0 风险”不是承诺任何重构天然无风险，而是把每一步限制成可验证的 0 行为差异改动：

1. **纯搬运优先**：先复制函数和状态到新模块，接口保持原返回值、参数、异常行为不变。
2. **不改业务判断**：随机回复概率、Agent 路由、文件/图片/语音处理、敏感词处理、工具调用、发送策略都不在拆分提交里改规则。
3. **不新增运行时状态到主干**：新状态必须归属到对应模块，`index.js` / `chat.js` 不再新增 Map/Set/全局缓存。
4. **不反向 import `index.js`**：新模块只能依赖稳定模块或通过参数注入依赖，禁止 `require('./index')`。
5. **一刀一测**：每拆一个模块，至少跑 `node -c`、`npm run test:quick`；触碰主链路时补跑相关 scenario。
6. **可回滚**：每一步拆分后都能用一个小提交回滚，不和功能开发混在一起。
7. **脏工作区冻结**：真正开始拆分前必须确认当前未提交修改归属；如果分支 ahead/behind 或工作区异常，先停下汇报。

## 三、开工前必须完成的只读检查

每次拆分开始前，先执行并记录结果：

```bash
git rev-parse --show-toplevel
git branch --show-current
git status --short --branch
git rev-parse HEAD
git rev-parse origin/bot_ZZY
```

如果出现以下情况，暂停拆分：

- 不在 `E:\莲莲Bot`。
- 不在 `bot_ZZY`。
- 分支 ahead/behind 状态和预期不一致。
- 工作区有不属于本轮拆分的修改，且涉及同一文件。
- `AGENTS.md` 被 git 跟踪或不再被 `.gitignore` 忽略。

涉及任何 commit / push 前，还要按隐私规则做扫描；本计划不授权部署、重启或推送。

## 四、函数溯源总表：index.js

下面按当前顶层函数列出职责、依赖和建议归属。实际拆分前必须重新生成一次清单，避免行号因别的修改漂移。

| 函数/状态 | 当前职责 | 主要依赖 | 建议归属 | 拆分阶段 | 风险 |
|---|---|---|---|---|---|
| `patchElementText` | Satori element 转文本兼容 | satori element shape | `session-compat.js` | P1 | 低 |
| `patchElementsToText` | elements 数组转文本 | `patchElementText` | `session-compat.js` | P1 | 低 |
| `patchElementId` | 兼容 element id | satori element shape | `session-compat.js` | P1 | 低 |
| `patchStripNickname` | Session stripped 兼容 | session/content | `session-compat.js` | P1 | 中低 |
| `patchBuildStripped` | 构建 stripped 文本 | `patchStripNickname` | `session-compat.js` | P1 | 中低 |
| `patchInstallAccessors` | 安装 Session 兼容属性 | `KoishiSession.prototype` | `session-compat.js` | P1 | 中 |
| `patchEnsureSession` | 单个 session 补丁 | patch accessors | `session-compat.js` | P1 | 中 |
| `originalSessionFactory` | bot.session monkey patch 原始引用 | `KoishiBot.prototype.session` | `session-compat.js` | P1 | 中 |
| `resolveCurrentBot` | 当前 bot 解析 | ctx/session bot | `bot-resolver.js` 或 `session-compat.js` | P1 | 低 |
| `createBotResolver` | 延迟 bot resolver | `resolveCurrentBot` | `bot-resolver.js` | P1 | 低 |
| `withCurrentBot` | session 注入当前 bot | session mutation | `bot-resolver.js` | P1 | 中低 |
| `runtimeSettingsLoaded` 等 | 运行时设置缓存 | 文件指纹、随机配置 | `runtime-settings.js` | P3 | 中 |
| `channelQueues` / `channelQueueDepth` | 按频道串行队列 | promise queue | `channel-task-queue.js` | P2 | 中 |
| `channelMissCount` | 随机回复 miss 计数 | 随机回复链路 | `random-state.js` | P4 | 中 |
| `armedEventDumpCache` | 原始事件抓取开关 | event dump | `event-dump.js` | P2 | 低 |
| `channelMutedUntil` | 闭嘴静默随机回复 | random gate | `random-state.js` | P4 | 中 |
| `lastRandomReplyTs` | 随机回复冷却 | random gate | `random-state.js` | P4 | 中 |
| `channelPendingRandom` | 延迟随机回复 | timer、version | `random-state.js` / `random-scheduler.js` | P4 | 高 |
| `channelMessageVersions` | 频道消息版本 | 随机过期判断 | `random-state.js` | P4 | 中 |
| `channelExplicitVersions` | 显式交互版本 | 随机取消判断 | `random-state.js` | P4 | 中 |
| `sendFailState` | 发送失败/限流状态 | send guard | `safe-send.js` | P2 | 中 |
| `userBlacklistCache` | 用户黑名单缓存 | 文件指纹 | `user-blacklist.js` | P3 | 中低 |
| `lastEmotionCache` | 敏感/情绪相关缓存 | sensitive/affect | 暂留 `index.js` 或独立 `emotion-state.js` | P5 | 中 |
| `restoreTodayCacheEntry` | 恢复今日缓存 | `channelTodayCache` | `conversation-startup.js` | P3 | 中低 |
| `normalizeChatResultText` | chat result 文本归一 | chat result shape | `agent-chat-retell.js` | P2 | 低 |
| `retellToolBlockedReply` | 工具被阻止后人格化转述 | `chat`、guard | `agent-chat-retell.js` | P2 | 中 |
| `retellAgentResult` | Agent 结果转 chat 口吻 | agent bridge、chat | `agent-chat-retell.js` | P2 | 中 |
| `handleChatResult` | chat heavy tool 升级 Agent | agent router/queue/chat | `chat-result-flow.js` | P3 | 高 |
| `enqueueForChannel` | 频道串行执行 | `channelQueues` | `channel-task-queue.js` | P2 | 中 |
| `getRandomTriggerRate` | 随机概率读取 | runtime rate | `random-state.js` | P4 | 中低 |
| `getRandomTriggerBaseRate` | 基础概率读取 | `randomRateCache` | `runtime-settings.js` / `random-state.js` | P3 | 中低 |
| `getRandomWhitelistStatus` | 白名单判断 | `randomWhitelistCache` | `runtime-settings.js` | P3 | 中低 |
| `getChannelMessageVersion` | 消息版本读取 | version map | `random-state.js` | P4 | 中低 |
| `bumpChannelMessageVersion` | 消息版本递增 | version map | `random-state.js` | P4 | 中 |
| `getExplicitInteractionVersion` | 显式版本读取 | version map | `random-state.js` | P4 | 中低 |
| `bumpExplicitInteractionVersion` | 显式版本递增 | version map | `random-state.js` | P4 | 中 |
| `trimRandomChannelState` | 随机状态裁剪 | version/pending maps | `random-state.js` | P4 | 中 |
| `cancelPendingRandom` | 取消延迟随机回复 | pending timer | `random-state.js` | P4 | 高 |
| `getGroupPersonaName` | 群人格名查询 | `getGroupPersona` | `persona-runtime-adapter.js` 或暂留 | P5 | 低 |
| `isPersonaSwitchRisky` | 随机人格切换风险判断 | persona resolution | `random-persona-risk.js` | P5 | 中低 |
| `buildRandomSendOptions` | 随机回复发送选项 | version/freshness | `random-send-state.js` | P4 | 中 |
| `isRandomReplyFresh` | 随机回复是否过期 | version state | `random-send-state.js` | P4 | 中 |
| `logStaleRandomSkip` | 过期随机回复诊断 | logger | `random-send-state.js` | P4 | 低 |
| `safeSendRepeat` | 复读发送 | `sendReply` | `safe-send.js` 或 `repeat.js` | P2 | 中低 |
| `resolveSharedRecordText` | 群共享记录文本 | analyzed/plain | `message-recording.js` | P5 | 中低 |
| `logReplyTimingDiagnostic` | 旁路回复时机诊断 | reply timing | `diagnostics.js` | P2 | 低 |
| `logAffectRouterDiagnostic` | 旁路情绪诊断 | affect router | `diagnostics.js` | P2 | 低 |
| `getNextShanghaiMidnightDelayMs` | 每日清理延迟 | time | `scheduler.js` | P2 | 低 |
| `scheduleDailyStatsCleanup` | daily stats 定时清理 | conversation cleanup | `startup-schedulers.js` | P2 | 中低 |
| `getExpressionHarvestDelayMs` | 表达学习定时延迟 | time | `startup-schedulers.js` | P2 | 低 |
| `scheduleExpressionHarvest` | 表达学习定时任务 | expression abstractor | `startup-schedulers.js` | P2 | 中低 |
| `getArmedEventDump` | 查询事件抓取 | cache | `event-dump.js` | P2 | 低 |
| `armEventDump` | 开启事件抓取 | cache/session | `event-dump.js` | P2 | 低 |
| `clearArmedEventDump` | 清理事件抓取 | cache | `event-dump.js` | P2 | 低 |
| `cacheSmallFileBackground` | 小文件后台缓存 | file-store/file-safety | `incoming-file.js` | P2 | 中 |
| `decodeEntityAttribute` | HTML entity 解码 | pure helper | `message-segment.js` | P1 | 低 |
| `extractAttrValue` | 提取 XML 属性 | pure helper | `message-segment.js` | P1 | 低 |
| `extractCqAttrValue` | 提取 CQ 属性 | pure helper | `message-segment.js` | P1 | 低 |
| `extractImageRefFromContent` | 从 content 提图引用 | helper | `message-segment.js` | P1 | 中低 |
| `appendUniqueSegments` | segment 去重合并 | helper | `message-segment.js` | P1 | 低 |
| `getMessageSegments` | 从 session 取 segments | session shape | `message-segment.js` | P1 | 中低 |
| `normalizeSegmentData` | segment data 归一 | segment shape | `message-segment.js` | P1 | 低 |
| `extractFileRefFromContent` | 从 content 提文件引用 | helper | `message-segment.js` | P1 | 中低 |
| `getFileSegmentData` | 从 session 取文件 segment | `getMessageSegments` | `message-segment.js` | P1 | 中低 |
| `dumpSessionEvent` | 原始事件落盘 | event dump dir、安全 JSON | `event-dump.js` | P2 | 中 |
| `getFileFingerprint` | 文件指纹 | fs stat | `file-fingerprint.js` 或 `utils.js` | P3 | 低 |
| `getRuntimeSettingsFingerprint` | 运行配置指纹 | config files | `runtime-settings.js` | P3 | 中 |
| `loadRuntimeSettings` | 加载随机配置 | whitelist/rate cache | `runtime-settings.js` | P3 | 中 |
| `loadUserBlacklist` | 加载用户黑名单 | file/fingerprint | `user-blacklist.js` | P3 | 中低 |
| `notifyAdminsSendFailure` | 发送失败通知管理员 | bot/private send | `safe-send.js` | P2 | 中 |
| `resetSendFailState` | 重置发送失败状态 | `sendFailState` | `safe-send.js` | P2 | 低 |
| `logPlatformMute` | 禁言状态日志 | send guard | `safe-send.js` | P2 | 低 |
| `handleRateLimitedSendFailure` | 限流失败处理 | admin notify/mute | `safe-send.js` | P2 | 中 |
| `safeSendReply` | 统一安全发送 | send guard/reply | `safe-send.js` | P2 | 高 |
| `safeSendRareVoice` | 罕见语音发送 | rare voice/session send | `safe-send.js` 或 `rare-voice-send.js` | P2 | 中 |
| `exports.apply` | Koishi ready/dispose/middleware 主编排 | 几乎所有模块 | 最后拆为 `plugin-lifecycle.js` + `message-pipeline.js` | P6 | 高 |

## 五、函数溯源总表：chat.js

| 函数/状态 | 当前职责 | 主要依赖 | 建议归属 | 拆分阶段 | 风险 |
|---|---|---|---|---|---|
| `skillsCache` 等 | skill 内容缓存 | skill dirs/fs | `chat-skills.js` | P1 | 中低 |
| `hostileLevelCache` | 敌意等级短缓存 | retaliation | `chat-retaliation-state.js` 或暂留 | P5 | 中 |
| `lastCacheCleanupTs` | chat runtime 清理节流 | runtime cache | `chat-runtime-state.js` | P5 | 中低 |
| `parseChatPositiveInt` | chat 数值环境变量解析 | pure | `chat-skills.js` 或 `utils.js` | P1 | 低 |
| `readChatSkillTextIfSmall` | 小 skill 文件读取 | fs/path | `chat-skills.js` | P1 | 低 |
| `stripChatSkillFrontmatter` | skill frontmatter 剥离 | pure | `chat-skills.js` | P1 | 低 |
| `getChatSkillDirectoryFingerprint` | skill 目录指纹 | fs | `chat-skills.js` | P1 | 低 |
| `getChatSkillsContentFingerprint` | skill 总指纹 | skill dirs | `chat-skills.js` | P1 | 低 |
| `trimRuntimeCaches` | chat runtime cache 清理 | conversation/hostile cache | `chat-runtime-state.js` | P5 | 中 |
| `shouldInjectLore` | legacy lore 关键词命中 | constants | `chat-lore-legacy.js` 或保留给 builder | P3 | 低 |
| `shouldInjectTerraLore` | terra lore 关键词命中 | constants | `chat-lore-legacy.js` 或保留给 builder | P3 | 低 |
| `detectTopicSwitch` | 轻量模型话题切换判断 | api/runtime config | `topic-switch.js` | P3 | 中 |
| `isContextJailbroken` | 历史回复越狱检测 | conversation/reply guard | `chat-jailbreak-flow.js` | P3 | 中低 |
| `loadSkills` | 加载 core skill 到缓存 | fs/skill seeds | `chat-skills.js` | P1 | 中低 |
| `loadSkillsContentCache` | 加载 core/mode/lore 内容 | fs/persona schema | `chat-skills.js` | P1 | 中 |
| `refreshSkillsContentCacheIfChanged` | skill 热刷新 | fingerprint/cache | `chat-skills.js` | P1 | 中 |
| `buildTestSystemPrompt` | 测试人格 prompt | skill cache | `chat-system-prompt.js` | P2 | 低 |
| `buildFriendlySystemPrompt` | 友善 prompt | skill cache | `chat-system-prompt.js` | P2 | 低 |
| `buildFriendlySafetyFramework` | 安全框架 | skill cache | `chat-system-prompt.js` | P2 | 低 |
| `buildAbusiveSystemPrompt` | 嘴臭 prompt | skill cache | `chat-system-prompt.js` | P2 | 低 |
| `getOutputGuardReason` | 输出守卫原因聚合 | reply guard/utils | `chat-output-flow.js` | P3 | 低 |
| `callOpenAI` | LLM 调用统一入口 | runtime config/api/search | 暂留 `chat.js`，后续 `llm-call.js` | P5 | 中 |
| `chatJailbreak` | 越狱专用回复 | api/runtime/guard | `chat-jailbreak-flow.js` | P3 | 中 |
| `topicSwitchLocks` | 话题切换锁 | topic switch | `topic-switch.js` | P3 | 中 |
| `chat` | 聊天主循环 | 全链路 | 最后只保留编排所有权 | P6 | 高 |
| `getSkillsCount` | skill 数量查询 | skill cache | `chat-skills.js` re-export | P1 | 低 |

## 六、推荐拆分阶段

### P0：冻结与溯源，不改代码

目标：先把“当前行为是什么”固定下来，避免拆分时不知道哪里变了。

动作：

1. 记录 `index.js` / `chat.js` 顶层函数清单、文件大小、最大函数行数。
2. 用 `rg` 查所有外部依赖和测试引用：
   - `require('./index')` / `require("../lib/index")`
   - `buildRepeatCandidate`、`checkGroupRepeat`、`loadRuntimeSettings`、`callOpenAI`、`loadSkillsContentCache`
3. 对要拆的函数做“入参、返回值、副作用、异常、状态读写”五列表。
4. 标记共享状态：
   - `randomWhitelistCache` / `randomRateCache`
   - `channelPendingRandom` / version maps
   - `sendFailState`
   - `skillsContentCache`
   - `hostileLevelCache`
5. 给每个目标模块写最小契约注释，不写实现。

验收：

- 没有源码改动，只有计划/台账文档。
- 能清楚回答每个函数依赖哪些状态，哪些测试覆盖它。

### P1：纯 helper 和兼容补丁拆分

优先拆没有业务策略的函数。

候选模块：

- `session-compat.js`
- `bot-resolver.js`
- `message-segment.js`
- `chat-skills.js`

拆分方法：

1. 新建目标文件，完整复制函数和状态。
2. `node -c` 目标文件。
3. 源文件加 import，但暂时不删旧函数。
4. 用新 import 替换调用点。
5. `node -c index.js/chat.js`。
6. `npm run test:quick`。
7. 通过后删除旧函数。
8. 再跑 `node -c` + `npm run test:quick`。

重点风险：

- `session-compat.js` 有 monkey patch，必须保证插件加载时仍然立即安装补丁。
- `chat-skills.js` 的缓存必须仍是单例，不能每次 require 产生多套状态。
- `message-segment.js` 不能改 CQ/XML 属性解析行为。

建议新增/确认测试：

- cascade 检查新模块可 require。
- session stripped / parsed / send 兼容仍可用。
- 图片/文件 segment 从 session 和 content 两路都能解析。
- `AI重载` 仍会刷新 skill cache。

### P2：发送、诊断、定时器、事件 dump 拆分

候选模块：

- `safe-send.js`
- `event-dump.js`
- `startup-schedulers.js`
- `diagnostics.js`
- `channel-task-queue.js`
- `agent-chat-retell.js`

拆分方法：

1. 优先通过依赖注入传 `ctx`、`session`、`sendReply`、`chat`、`agentEngine`，避免新模块回头 import `index.js`。
2. `safe-send.js` 自己持有 `sendFailState`，导出：
   - `safeSendReply`
   - `safeSendRepeat`
   - `safeSendRareVoice`
   - `resetSendFailState`
3. `startup-schedulers.js` 导出 scheduler 创建/清理函数，`index.js` 只保存 timer dispose 句柄。
4. `agent-chat-retell.js` 不直接拥有 Agent engine，只接收运行函数或依赖对象。

验收：

- 限流、禁言、失败通知逻辑不变。
- dispose 时 timer 全部清理。
- Agent 结果仍经过 chat 人格化转述。
- 随机罕见语音 freshness 判断仍在发送前执行。

建议测试：

- `sendReply` 抛 rate limit 后走精简重试。
- 非 rate-limit 错误不会重复刷屏。
- Agent heavy tool 请求返回后仍由 chat 转述，不直出工具报告。
- dispose 后 pending timer 不再发消息。

### P3：运行时设置、黑名单、话题切换、chat 输出守卫

候选模块：

- `runtime-settings.js`
- `user-blacklist.js`
- `topic-switch.js`
- `chat-output-flow.js`
- `chat-jailbreak-flow.js`

拆分原则：

- `runtime-settings.js` 只管配置缓存，不接管随机回复决策。
- `user-blacklist.js` 只管读取和缓存，不决定“是否记录上下文”。
- `topic-switch.js` 可持有 `topicSwitchLocks`，但清历史和清 Agent context 仍由调用方显式传入回调。
- `chat-output-flow.js` 处理模型输出后的 retry/fallback，但不调用发送。

验收：

- 用户黑名单仍然“记录上下文，但不回复”。
- 话题切换检测失败时，历史仍降级为背景，不直接误删。
- jailbreak 输入和 jailbreak 输出仍走专用兜底。
- 输出中出现内部工具计划、`<user>` 包装、banned output 仍会重试或 fallback。

建议测试：

- 短追问没有误触发旧搜索上下文。
- prompt injection 输入不会污染正常 chat 历史。
- 模型返回 JSON/random mode 时不会原文发群。
- 文件工具拒绝后 fallback 能调用对应工具。

### P4：随机回复状态与延迟随机调度

这是 `index.js` 中风险最高的拆分之一，必须晚一点做。

候选模块：

- `random-state.js`
- `random-send-state.js`
- `random-scheduler.js`
- `random-gate.js`

拆分边界：

- `random-state.js`：只管 state map、版本、pending、mute、cooldown。
- `random-gate.js`：只算候选、概率、miss count，不发送。
- `random-scheduler.js`：只负责 15 秒延迟随机的 timer 和 freshness 检查。
- `random-send-state.js`：只构造 send options 和 stale 日志。

高风险点：

- 新显式交互必须取消旧随机回复。
- 延迟随机等待期间，如果频道出现新消息，旧随机不能补发。
- `ambient_water` 不能引用、不能 replyTo、不能 @。
- rare voice 与普通文本的 freshness 判断必须一致。

验收场景：

1. 普通随机命中后立即回复。
2. 连续发言触发延迟随机。
3. 延迟期间出现点名/私聊/命令，旧随机取消。
4. 延迟期间出现新群消息，旧随机过期。
5. `ambient_water` 发送时不 quote、不 @、不 replyTo。
6. 高风险人格切换时随机回复不能错绑旧人格。

### P5：persona、affect、emotion、hostile 等旁路状态整理

这一阶段不是为了“变小”而拆，而是为了让人格/情绪/随机回复边界更清楚。

候选：

- `chat-retaliation-state.js`
- `random-persona-risk.js`
- `message-recording.js`
- `emotion-state.js`
- `persona-runtime-adapter.js`

原则：

- 不在这一阶段改变人格解析结果。
- 不引入新的 `PersonaRuntimePlan` 行为接管，除非另有审核计划。
- affect-router / expression shadow 继续保持旁路诊断，不改变 prompt 和回复。

验收：

- 自定义人格仍绕过 hostile 模式。
- `will` 因子仍按旧逻辑影响随机概率。
- 旁路日志开启/关闭不影响 chat 输出。

### P6：最后整理 `apply()` 与 `chat()`

前面阶段完成后，才可以拆最大函数。

目标形态：

- `index.js`
  - 只保留 `exports.name`
  - 安装 session compat
  - `exports.apply = createPluginApply(deps)` 或直接注册 lifecycle/middleware
  - 不再持有大量全局 Map/Set

- `chat.js`
  - 保留 `chat()` 主流程所有权
  - 保留 `callOpenAI()` 或明确迁入 `llm-call.js`
  - prompt 构建、工具流、输出守卫、topic switch 等都用独立模块
  - `chat()` 负责“按顺序编排”，不再内联所有细节

必须禁止：

- 一次性把 `apply()` 拆成多个互相调用但状态不清的闭包。
- 把 `chat()` 拆成另一个同样 800 行的 `chat-flow.js`。
- 为了拆分顺手重写随机回复、搜索路由、人格逻辑。

## 七、每次拆分的标准流程

每个模块拆分都按下面流程走：

1. **溯源**
   - 列出函数名、行号、调用点。
   - 列出读取的外部状态。
   - 列出写入的外部状态。
   - 列出可能抛出的异常。
   - 列出测试覆盖点。

2. **复制**
   - 新建目标模块。
   - 原样复制函数，先不删旧函数。
   - 必要状态也搬到目标模块，但不改默认值。

3. **编译**
   - `node -c 新模块`
   - `node -c index.js/chat.js`

4. **接线**
   - 源文件 require 新模块。
   - 调用点切到新模块。
   - 如果目标模块需要旧模块函数，通过参数注入，不反向 import。

5. **验证**
   - `npm run test:quick`
   - 触碰 chat 主链路：`npm run test:scenario`
   - 触碰图片/文件/语音/随机/Agent 时跑对应 scenario。

6. **删除旧定义**
   - 测试通过后删除源文件旧函数。
   - 再跑一次 `node -c` + `npm run test:quick`。

7. **记录**
   - 在提交信息或待审核记录写明“移动了什么、为什么、验证了什么、未覆盖什么”。

## 八、场景验证矩阵

拆分不是只看语法，必须覆盖真实链路。

| 链路 | 必测输入/行为 | 断言 |
|---|---|---|
| 普通点名聊天 | 群里 @bot 问普通问题 | 回复仍走 chat，不触发随机链路 |
| 私聊 | 私聊普通问题 | 不受群随机 gate 影响 |
| 随机主动回复 | 白名单群普通消息命中概率 | 能回复，miss count/cooldown 正常 |
| 延迟随机 | 同一用户连续发言 | 15 秒后仍需 freshness 检查 |
| 显式打断 | 延迟随机期间 @bot | 旧随机取消 |
| ambient water | 随机 mode 返回 `ambient_water` | 不引用、不 replyTo、不 @ |
| 图片当前消息 | 发图并点名 | `storeImageUrl` + `enqueueAnalysis` + vision request 正常 |
| 引用图片 | 回复旧图片问“这是什么” | 能走引用图片上下文，不误看别图 |
| 文件上传 | 上传小文件后问读文件 | 文件缓存/分析/总结链路正常 |
| 文件追问 | “改名发我/生成副本” | `create_uploaded_file_variant` 工具链路正常 |
| 语音 | 点名或私聊语音 | ASR 超时有降级 |
| Agent 搜索 | 明确搜索请求 | Agent run 后由 chat 转述 |
| 搜索误触发防线 | 短追问“哪个/然后呢” | 不凭旧冷上下文乱搜 |
| 敏感话题 | 开启检测群触发敏感词 | 固定拒答/通知处理者逻辑不变 |
| 发送限流 | mock send 抛 rate limit | 精简重试、禁言标记、管理员通知不变 |
| reload | `AI重载` | config/skills/runtime cache 都刷新 |
| dispose | 插件 dispose | timer、pending random、cron scheduler 清理 |

## 九、测试报告必须回答四问

任何拆分完成后，报告必须回答：

1. 复现了用户哪条真实失败输入或真实风险链路？
2. 断言了哪个失败现象不会出现？
3. 哪些依赖被 mock 了？
4. 因为 mock，哪些真实链路仍未覆盖？

如果只是纯搬运，没有真实失败输入，也要写：

- 本次是结构等价拆分，没有修复用户可见 bug。
- 断言的是“拆分前后导出、调用、状态副作用一致”。
- mock 了哪些平台发送/API/文件系统。
- 尚未覆盖的线上真实链路是什么。

## 十、提交拆分时的 commit message 模板

拆分提交不能只有标题，建议格式：

```text
refactor(dongxuelian-ai): split <module> from index/chat

- packages/koishi-plugin-dongxuelian-ai/lib/<source>.js:
  - 移出 <函数列表>
  - 原因：<入口/聊天主干减重，职责边界>
  - 行为：纯搬运，不改变 <具体链路>

- packages/koishi-plugin-dongxuelian-ai/lib/<target>.js:
  - 新增 <模块职责>
  - 依赖边界：只依赖 <稳定模块/注入参数>

验证：
- node -c <target>
- node -c <source>
- npm run test:quick
- npm run test:scenario / 对应 scenario

未覆盖：
- <真实平台发送/真实 ASR/真实 Agent 网络等>
```

禁止在 commit message 写真实服务器 IP、SSH 信息、部署路径、token、cookie、生产日志敏感 URL。

## 十一、优先级建议

推荐顺序：

1. `message-segment.js`：纯 helper，收益小但风险低，适合练手。
2. `chat-skills.js`：能明显减轻 `chat.js` 顶部缓存和加载逻辑。
3. `safe-send.js`：收益高，但要认真测限流和禁言。
4. `event-dump.js` + `startup-schedulers.js`：入口文件减负明显。
5. `topic-switch.js` + `chat-jailbreak-flow.js`：开始整理 chat 主流程。
6. `chat-output-flow.js`：把输出守卫从 `chat()` 拆出。
7. `random-state.js` / `random-scheduler.js`：最后做，必须配完整随机场景测试。
8. `apply()` / `chat()` 编排瘦身：等前面小模块都稳定后再做。

不建议第一刀：

- 直接拆 `exports.apply()`。
- 直接拆 `chat()` 主循环。
- 直接重写随机回复状态机。
- 同时拆 Agent、搜索、随机、发送多个链路。

## 十二、审核通过标准

这份计划通过审核后，真正实施前还需要明确：

- 当前工作区哪些修改属于谁，是否可以基于当前状态拆。
- 第一刀选哪个模块。
- 本轮是否允许只做结构拆分，不夹带功能修复。
- 每一步拆完是否需要单独提交。
- 是否需要先补对应 scenario 测试，再拆模块。

建议第一刀选择 `message-segment.js` 或 `chat-skills.js`。这两块对用户可见行为影响最小，能验证拆分流程是否稳。

## 十三、2026-05-25 现状复核修订版

> 这一节是当前工作的修订版，以这一节为准。前文保留的是旧台账，里面有一些“拟拆模块”已经在仓库里落地了，不能再按旧清单推进。

### 1. 先纠偏：当前仓库已经拆出来的东西

`packages/koishi-plugin-dongxuelian-ai/lib/` 里已经有一批稳定子模块，不应该再当成 `index.js` / `chat.js` 的待拆主债：

- 对话与记忆：`conversation.js`、`chat-memory.js`、`group-scene-index.js`
- 回复与守卫：`reply.js`、`reply-guard.js`、`send-guard.js`、`random-reply-mode.js`、`rare-voice.js`
- 多模态与文件：`vision.js`、`image-store.js`、`image-analyzer.js`、`file-store.js`、`file-safety.js`、`file-followup-guard.js`、`uploaded-file-action-route.js`
- Agent 与复述：`agent/` 下大部分工具链、`agent-chat-bridge.js`、`agent-retell-guard.js`
- prompt 与旁路诊断：`chat-prompt-builder.js`、`sticker-shadow.js`、`expression-abstractor.js`、`affect-router.js`
- 技能与人格：`skills-loader.js`、`skill-seeds.js`、`persona*`

结论很简单：**这次不是“从零拆入口”，而是“把剩余主干再削薄一层”。**

### 2. 当前真正还卡住的主干

#### `index.js`

当前 `index.js` 仍然同时承担这些职责：

1. Koishi 兼容补丁：`Session` / `Bot.session` / stripped 兼容。
2. 插件生命周期总装配：`exports.apply()`。
3. 群聊消息主入口编排：命令、图片、语音、文件、转发、敏感词、复读、随机回复、Agent 路由。
4. 运行时状态：随机概率、队列深度、随机版本、静默状态、事件 dump、发送失败状态、黑名单缓存、情绪缓存。
5. 定时任务：日统计清理、表达学习 harvest。
6. 杂项 helper：消息段解析、随机状态裁剪、随机 freshness、诊断日志、今日缓存回填。

这说明 `index.js` 的问题不只是“长”，而是**混了入口、状态、调度、解析、发送、诊断五类职责**。

#### `chat.js`

当前 `chat.js` 仍然承担这些职责：

1. 技能文件加载和缓存刷新。
2. system prompt 组装和人格/安全框架拼接。
3. `callOpenAI()` 路由和搜索能力分发。
4. jailbreak / context jailbreak 专用链路。
5. Agent 结果复述和工具结果脱敏。
6. 聊天主循环 `chat()` 的所有前置判定、输出守卫、重试、fallback。

这说明 `chat.js` 的问题不是“模型调用很多”，而是**prompt、状态、输出守卫、Agent 复述、主循环都缠在一起**。

### 3. 修订后的拆分原则

1. 不再优先考虑“把 `apply()` 整体拆成一个更大的中间层”。
2. 不再把已经存在的独立模块重新写回计划主线。
3. 新模块只接收明确依赖，不反向 import `index.js`。
4. `chat()` 继续保留编排所有权，但不再自己生成所有 prompt 片段和输出后处理细节。
5. 任何新拆分都要能用 `node -c`、`npm run test:quick`、必要时 `npm run test:scenario` 说明没有把行为打歪。
6. 只拆“真正还缠在主干里”的职责，不顺手做功能重写。

### 4. 现阶段推荐的实际优先级

#### 第一组：先让 `chat.js` 再瘦一圈

优先级从高到低：

1. `chat-skills.js`
   - 目标：`skillsCache`、`skillsContentCache`、fingerprint、load/refresh 逻辑独立出去。
   - 原因：这块是纯加载与缓存，和 chat 主循环强耦合但业务风险最低。
2. `chat-system-prompt.js`
   - 目标：`buildTestSystemPrompt`、`buildFriendlySystemPrompt`、`buildFriendlySafetyFramework`、`buildAbusiveSystemPrompt`、`getOutputGuardReason`。
   - 原因：prompt 组装是纯文本拼接，最适合先搬。
3. `chat-jailbreak-flow.js`
   - 目标：`chatJailbreak()`、`isContextJailbroken()`、输入/历史越狱分支。
   - 原因：这条链很独立，能明显缩短主循环。
4. `llm-call.js`
   - 目标：`callOpenAI()`。
   - 原因：这是清晰的 IO 边界，适合和 prompt 分离。
5. `chat-output-flow.js`
   - 目标：输出守卫、retry、fallback、banned output、重复回复控制。
   - 原因：chat 的后半段最容易继续膨胀。
6. `chat-agent-retell.js`
   - 目标：Agent 结果转述、工具摘要注入、脱敏与兜底。
   - 原因：Agent 已经是独立大系统，chat 只该拿结果，不该管太多细节。

#### 第二组：再让 `index.js` 退回真正的入口

优先级从高到低：

1. `session-compat.js`
   - 目标：`patchElementText`、`patchStripNickname`、`patchBuildStripped`、`patchInstallAccessors`、`patchEnsureSession`、`originalSessionFactory`。
   - 原因：这是独立兼容层，最适合先从入口剥离。
2. `bot-resolver.js`
   - 目标：`resolveCurrentBot`、`createBotResolver`、`withCurrentBot`。
   - 原因：依赖很窄，便于测试。
3. `random-state.js`
   - 目标：`channelMissCount`、`channelMutedUntil`、`lastRandomReplyTs`、`channelPendingRandom`、`channelMessageVersions`、`channelExplicitVersions`、`randomWhitelistCache`、`randomRateCache`。
   - 原因：这组状态已经明显超过入口该持有的边界。
4. `random-scheduler.js`
   - 目标：`buildRandomSendOptions`、`isRandomReplyFresh`、`cancelPendingRandom`、`trimRandomChannelState`。
   - 原因：随机回复 freshness 和定时取消是完整子系统。
5. `channel-task-queue.js`
   - 目标：`enqueueForChannel()` 和队列深度状态。
   - 原因：队列独立，且会继续被多个链路复用。
6. `event-dump.js`
   - 目标：`armedEventDumpCache`、`getArmedEventDump()`、`armEventDump()`、`clearArmedEventDump()`。
   - 原因：这是一次性抓取状态机，不该留在入口。
7. `message-segment.js`
   - 目标：`decodeEntityAttribute`、`extractAttrValue`、`extractCqAttrValue`、`extractImageRefFromContent`、`appendUniqueSegments`、`getMessageSegments`、`normalizeSegmentData`、`extractFileRefFromContent`、`getFileSegmentData`。
   - 原因：纯解析 helper，适合一次性搬走。
8. `startup-schedulers.js`
   - 目标：`scheduleDailyStatsCleanup()`、`scheduleExpressionHarvest()`、相关 timer 句柄。
   - 原因：定时任务要和消息入口脱钩。

### 5. 修订后的阶段划分

#### P0 只读复核

- 重新生成 `index.js` / `chat.js` 顶层函数清单。
- 重新确认当前已拆模块的真实边界。
- 对照测试目录，标出哪些 scenario 已经覆盖了当前主干。
- 明确本轮只做结构拆分，不夹带功能修复。
- 可以先审核一下当前架构，确认各个细节都已经落实到位；如果细节不对，再按现状重写一版。

#### P1 先拆 `chat.js` 的纯加载和纯文本

- `chat-skills.js`
- `chat-system-prompt.js`
- 必要时把 `buildTestSystemPrompt` 一类纯函数先搬过去

验收：

- `chat()` 还能正常组装 prompt。
- `skills` 热刷新逻辑没丢。
- `npm run test:quick` 通过。

#### P2 再拆 `chat.js` 的控制流边界

- `chat-jailbreak-flow.js`
- `llm-call.js`
- `chat-output-flow.js`
- `chat-agent-retell.js`

验收：

- 越狱、输出守卫、Agent 复述都保持原行为。
- `chat()` 只剩“按顺序编排”。

#### P3 再拆 `index.js` 的入口状态

- `session-compat.js`
- `bot-resolver.js`
- `random-state.js`
- `random-scheduler.js`
- `channel-task-queue.js`
- `event-dump.js`
- `message-segment.js`
- `startup-schedulers.js`

验收：

- `exports.apply()` 不再持有大块 Map/Set 和零散 helper。
- 随机回复、事件抓取、定时器和兼容补丁都能独立测试。

#### P4 最后收口

- `index.js` 只剩生命周期装配和消息管线编排。
- `chat.js` 只剩聊天主循环的顺序编排。
- 旧台账里那些已经拆掉的模块，从“待拆”改成“已拆/已验收”。

### 6. 这轮真正应该盯住的验收问题

1. `chat.js` 拆出去后，技能刷新和 prompt 拼接是否仍然等价。
2. `chat.js` 的 jailbreak / 输出守卫是否还覆盖原失败输入。
3. `index.js` 拆出去后，随机回复 freshness、队列深度、事件 dump 是否仍然可控。
4. `exports.apply()` 是否真的退回到了“入口编排”。
5. `chat()` 是否真的退回到了“模型调用主循环”，而不是又长出一个新的半成品主干。

### 7. 本轮不要再碰的东西

- 不要再把已拆出的 `conversation.js`、`reply.js`、`file-followup-guard.js`、`uploaded-file-action-route.js`、`group-scene-index.js` 重新纳入主拆分目标。
- 不要把 `index.js` 直接重写成另一个超大的聚合层。
- 不要把 `chat()` 再拆成一个同样 800 行的 `chat-flow.js`。
- 不要为了“先拆出来”顺手改随机回复策略、Agent 路由策略、人格切换策略。

### 8. 备注

- 仓库里没有找到单独的 `agent.md`，当前以 `AGENTS.md` 作为本地协作约束来源。
- 这个修订版是给后续真正动刀时用的，不是为了把计划写得更大，而是为了让接下来每一步都和当前代码对得上。

## 十四、2026-05-25 本轮只读探索归档

> 本节记录本轮实际探索结果。用户要求先探索、写文档，稍后再提交；因此本轮不做源码拆分，不部署、不重启、不推送。

### 1. 本轮已执行的前置检查

- 已重新读取 `AGENTS.md` 和本文件，确认继续任务前必须先读约束与计划。
- 已确认工作目录为 `E:\莲莲Bot`，当前分支为 `bot_ZZY`。
- 已确认 `HEAD` 与 `origin/bot_ZZY` 同步，没有 ahead/behind。
- 已确认工作区存在大量未提交修改，其中包括 `packages/koishi-plugin-dongxuelian-ai/lib/index.js` 和 `packages/koishi-plugin-dongxuelian-ai/lib/chat.js`。
- 结论：当前适合做只读审计和计划归档；真正拆 `index.js` / `chat.js` 前，必须先确认这些现存修改归属，避免覆盖其他人的工作。

### 2. 后台 agent 审计结论摘要

本轮使用两个后台探索 agent 分别只读审计 `index.js` 和 `chat.js`，没有让它们改文件。

#### `index.js`

当前工作区版本约 1928 行 / 86 KB，`exports.apply()` 约 698 行。

主要职责簇：

- 导入与依赖汇聚：大量模块在入口集中引入。
- Koishi / Satori session 兼容补丁：包含 stripped / parsed / resolve / send / Bot.session monkey patch。
- Bot resolver：`resolveCurrentBot()`、`createBotResolver()`、`withCurrentBot()`。
- 顶层运行时状态：随机概率、频道队列、随机版本、pending random、event dump、发送失败、黑名单、情绪缓存。
- Agent heavy tool 升级与复述桥：`normalizeChatResultText()`、`retellToolBlockedReply()`、`retellAgentResult()`、`handleChatResult()`。
- 频道串行队列：`enqueueForChannel()` 与队列深度状态。
- 随机回复 gate / version / freshness / persona 风险判断。
- 诊断与旁路 shadow 日志。
- 启动定时器：日统计清理、表达学习 harvest。
- 原始事件抓取：armed state + `dumpSessionEvent()` 落盘。
- 小文件后台缓存。
- 消息段 / CQ / XML / 图片 / 文件解析 helper。
- runtime settings 与 user blacklist 文件缓存。
- safe send / rate-limit / 管理员失败通知 / rare voice 发送。
- `exports.apply()` 生命周期与 middleware 主流程。

需要修正计划的点：

- §13 原先低估了 `index.js` 剩余债，必须补回 `safe-send.js`、Agent 复述 / heavy-tool bridge、diagnostics / shadow logging、runtime-settings、完整 `event-dump.js`。
- `session-compat.js` 目标不能只写 stripped 兼容，还要包含 `Session.prototype.resolve`、`Session.prototype.send` 和 `Bot.session` patch。
- `random-state.js` 与 `random-scheduler.js` 边界要重写：`trimRandomChannelState()`、`cancelPendingRandom()` 当前更像状态模块职责；延迟随机 timer body 才更接近 scheduler。
- `randomWhitelistCache` / `randomRateCache` 不应直接塞进 `random-state.js`，当前读取与指纹刷新属于 `runtime-settings.js`。
- `event-dump.js` 不能只管 arm / clear，还必须包括 `dumpSessionEvent()` 的安全落盘。
- `startup-schedulers.js` 还要写清 dispose 清理 timer 的接线。
- 旧优先级里“第一刀 message-segment.js”和 §13 “第一刀 session-compat.js”冲突；后续以本节下面的更新优先级为准。

#### `chat.js`

当前工作区版本约 1479 行 / 75 KB，`chat()` 约 967 行。文件内的 size gate 注释仍写“约 350 行，上限 400 行”，已经过时，后续源码修改时要同步修正。

主要职责簇：

- 导入与依赖汇聚：prompt builder、memory、vision、file follow-up、Agent bridge、chat tools、persona profile、expression shadow、runtime config 等。
- 顶层状态：`skillsCache`、`skillsContentCache`、fingerprint / refresh promise、`hostileLevelCache`、`lastCacheCleanupTs`。
- skill 加载、缓存、fingerprint、热刷新。
- runtime cache 与 hostile cache 清理。
- lore trigger helper。
- topic switch 检测与锁。
- context jailbreak 检测与专用回复。
- system prompt helper。
- output guard reason helper。
- chat tool usage state helper。
- `callOpenAI()`：搜索、thinking、tool-call 路由。
- `chat()` 主流程：前置判定、人格、prompt、scene、memory、vision、tools、file evidence、reminder fallback、uploaded-file variant fallback、输出守卫、随机 mode、最终保存。

需要修正计划的点：

- `skills-loader.js` 虽然存在，但 `chat.js` 当前没有使用它；`chat-skills.js` 不能写成“新建一套 loader”，应写成“与现有 `skills-loader.js` 去重 / 合并”，并保留 `chat.js` 当前的 fingerprint refresh 与 `loreMeta:*` 行为。
- `chat-system-prompt.js` 只适合搬纯 prompt helper；`getOutputGuardReason()` 应归入输出守卫流，不应归入 system prompt。
- `chat-jailbreak-flow.js` 不是完全独立，它会触碰 system prompt、conversation save、历史清理、Agent retell 相关路径，拆分时必须依赖注入。
- `llm-call.js` 拆出时要保持 `callOpenAI` 的公开导出形状，因为 `index.js`、`handler.js`、命令模块和测试都依赖它。
- `chat-output-flow.js` 原计划过窄；后半段还包含 tool-call loop、heavy tools、reminder fallback、uploaded-file variant fallback、file evidence、vision blindness retry。建议拆成 `chat-tool-flow.js`、`chat-file-followup-flow.js`、`chat-final-output-flow.js` 等更小边界。
- `chat-agent-retell.js` 还要覆盖政治敏感固定拒答、Agent 复述守卫和 fallback，不只是简单转述。
- `chat()` 最终目标不应写成“只剩模型调用主循环”，更准确是“对话响应编排器”：保留 scene、memory、tools、media、file evidence、最终持久化的顺序所有权。

### 3. 已确认的测试覆盖入口

当前 scenario 目录已有这些可作为拆分回归的入口：

- `chat.test.js`：普通聊天、Agent auto route、工具调用、短追问、输出守卫、skill cache 等。
- `random.test.js`：随机回复、延迟随机、ambient water、freshness 相关链路。
- `file.test.js`：文件上传、文件追问、上传文件变体。
- `vision.test.js`：图片、视觉会话、vision request。
- `voice-tts.test.js`：语音、TTS、罕见语音相关链路。
- `command.test.js`：`AI重载`、工具开关、概率配置、权限门。
- `send-guard.test.js`：发送守卫相关链路。
- `repeat.test.js`：复读候选与复读触发。
- `concurrency.test.js`：队列、并发、隔离性。
- `sensitive.test.js`：敏感词与固定拒答链路。
- `agent-phase3.test.js`：Agent 工具链相关行为。

后续拆分每一步都应该优先从这些 scenario 里选最贴近的回归入口，而不是只依赖 cascade 字符串检查。

### 4. 修订后的可执行优先级

#### A. 当前可做：继续只读归档

适合在用户提交当前工作区前继续做：

1. 继续补全 `index.js` / `chat.js` 的函数-状态-测试覆盖台账。
2. 对照 cascade-test，列出新增模块时必须补的 `modPaths` / `syntaxFiles` / `expectedExports`。
3. 给每个候选模块写“输入、输出、副作用、状态所有权、测试入口”表。

不适合做：

- 不直接改 `index.js`。
- 不直接改 `chat.js`。
- 不改 cascade-test。
- 不新增生产模块。

原因：这些文件当前已有现存未提交修改，直接动刀容易混入其他人的改动。

#### B. 用户提交或明确授权后，第一刀建议

如果用户先提交当前工作区，建议第一刀从 `session-compat.js` 开始：

- 原因：职责独立，主要是 Koishi / Satori 兼容层，能从入口剥离一整段 monkey patch。
- 范围：`patchElementText()`、`patchElementsToText()`、`patchElementId()`、`patchStripNickname()`、`patchBuildStripped()`、`patchInstallAccessors()`、`patchEnsureSession()`、`Bot.session` patch、`Session.prototype.resolve`、`Session.prototype.send`。
- 验证：`node -c session-compat.js`、`node -c index.js`、`npm run test:quick`，再跑涉及 middleware 的 `npm run test:scenario` 或至少 `chat.test.js` / `command.test.js` 相关入口。

如果用户希望先碰 `chat.js`，建议第一刀不是新建全新 `chat-skills.js`，而是“整理 chat skill loader 与现有 `skills-loader.js` 的差异台账”，确认 `loreMeta:*` 与 fingerprint 热刷新能无损迁移后再搬。

### 5. 更新后的阶段规划

#### P0：只读台账完成

- 记录当前行数、主函数行数、工作区脏状态。
- 记录后台 agent 审计结论。
- 记录哪些旧计划条目过时。
- 记录可复用测试入口。
- 不改源码。

#### P1：入口兼容层拆分

- `session-compat.js`
- `bot-resolver.js`

这一步只拆 Koishi/Satori 兼容与 bot resolver，不碰随机、发送、Agent、chat 主流程。

#### P2：入口低风险 helper 与状态拆分

- `message-segment.js`
- `event-dump.js`，必须包含 `dumpSessionEvent()`。
- `startup-schedulers.js`，必须包含 dispose timer 清理接线。
- `channel-task-queue.js`。

#### P3：入口发送与 Agent bridge 拆分

- `safe-send.js` 或扩展现有 `send-guard.js`，需要先判断现有模块边界。
- `agent-chat-retell.js` / `chat-result-flow.js`，统一命名后再拆。
- `diagnostics.js` / `shadow-diagnostics.js`。

#### P4：chat skill / prompt / llm 边界拆分

- 先合并或替换现有 `skills-loader.js` 与 `chat.js` 内部 skill loader 的重复职责。
- 再拆纯 prompt helper。
- 再评估 `llm-call.js`，保持 `callOpenAI` public export 不变。

#### P5：chat 后半段控制流拆分

- `chat-tool-flow.js`
- `chat-file-followup-flow.js`
- `chat-final-output-flow.js`
- `chat-jailbreak-flow.js`
- `topic-switch.js`

这一步必须补足真实场景测试，不能只靠静态检查。

#### P6：随机回复状态机拆分

- `random-state.js`
- `random-gate.js`
- `random-scheduler.js`
- `random-send-state.js`
- `runtime-settings.js`

这一步最后做，因为它关系到延迟随机、显式打断、ambient water、rare voice freshness 和群聊上下文窗口。

### 6. 本轮探索结论

当前可以确认：计划方向正确，但 §13 仍低估了当前主干。真正执行拆分前，必须先把“当前脏工作区归属”处理清楚；否则拆出来的 commit 会混入已有功能改动，无法证明是结构等价拆分。

因此本轮归档状态是：

- 已完成：P0 只读审计、后台 agent 辅助审计、计划修订。
- 未执行：源码拆分、测试运行、部署、推送。
- 下一步：等待用户提交或确认当前工作区归属后，再按 `session-compat.js` 第一刀推进。

## 十五、2026-05-25 提交后复扫归档

> 用户说明“刚刚做了一些更改”，本节记录基于最新 `HEAD` 的复扫结果。当前只更新计划判断，不改源码、不部署、不重启、不推送。

### 1. 当前仓库状态变化

- 当前 `HEAD` / `origin/bot_ZZY` 已同步到 `18661af96ee7063c118f19cce24e322618aa20f4`。
- 当前工作区干净，`git status --short --branch` 只显示分支状态，没有未提交文件。
- 这意味着上一节里的“等待用户提交或确认当前工作区归属”已经解除；后续可以进入源码拆分前复核阶段。
- 仍然必须遵守每轮开工先读 `AGENTS.md` 和本计划的要求；正式拆分前还要重新确认工作区是否仍干净。

### 2. 新提交对计划的影响

这次提交不是单纯文档变更，也包含 `index.js` / `chat.js` / `group-scene-index.js` / `reply-guard.js` 等主链路相关改动。对拆分计划的影响如下：

1. `index.js` 仍然约 1928 行，`exports.apply()` 仍从入口位置开始，入口复杂度没有降低。
2. `chat.js` 增至约 1507 行，`chat()` 约从 497 行到 1490 行，聊天主循环更长；文档里的“约 1479 行 / 967 行”需要在下一轮详细台账里更新。
3. `chat.js` 新增了 `classifySceneItemsForActive()` 的调用，把 active scene 分层结果用于 prompt 与旧背景媒体粘连检测。
4. `reply-guard.js` 新增了 `detectOldMediaTopicSticking()` / `buildOldMediaStickingRetryPrompt()`，属于输出守卫后半段的一部分。
5. `index.js` 新增了 `isSafeSendReplyFresh()`，并把随机 freshness 检查收口到 `safeSendReply()` 前置判断；这强化了后续 `safe-send.js` / `random-send-state.js` 的耦合边界。
6. `index.js` 给 `chat()` 传入了 `directAt` / `nameMentioned`，并在 shared context note 中传递显式交互信息；这说明 active scene 已经从随机链路扩展到全局 chat 编排，不应被拆进随机专用模块。

### 3. 需要更新的拆分判断

- `session-compat.js` 第一刀建议仍然成立。它与这次 active scene / old media / freshness 改动交集最小，仍是最适合练手的低风险入口拆分。
- `message-segment.js` 不建议作为当前第一刀。虽然它是纯解析 helper，但当前刚落地群聊现场分层与媒体锚点相关逻辑，先拆消息段解析容易和媒体上下文链路交叉，适合稍后做。
- `safe-send.js` 的拆分优先级需要提高，但不能先做。因为 `safeSendReply()` 现在同时承担发送限流、平台禁言、随机 freshness、rare voice 前置判断等职责，拆之前必须先画清 `sendOptions` / `randomFreshness` / `personaName` 的契约。
- `chat-output-flow.js` 的拆分边界必须扩展：现在不仅包括 banned / thinking / internal leak / repetition retry，还包括旧背景媒体粘连重试。后续应改名或拆分为 `chat-final-output-flow.js`，避免把工具流、文件流、视觉流全塞进去。
- `group-scene-index.js` 已经承担 active scene 分层能力，后续不要再把它当成“已拆完不用管”的纯背景模块；`chat.js` 与 `index.js` 的接线仍是架构债的一部分。
- `random-state.js` / `random-scheduler.js` 仍然最后做。原因是这次提交强化了随机 freshness 与发送层的连接，贸然先拆随机状态会更容易打歪显式打断和延迟随机。

### 4. 更新后的下一步建议

如果要继续执行拆分，建议顺序调整为：

1. 重新生成最新 `index.js` / `chat.js` 行号台账，覆盖本节新增的 active scene、旧媒体粘连、freshness 收口点。
2. 第一刀拆 `session-compat.js`，只搬兼容补丁和 monkey patch，不碰 active scene / random / safe send。
3. 第二刀拆 `bot-resolver.js`。
4. 第三刀再评估 `message-segment.js` 或 `event-dump.js`。
5. `safe-send.js`、`chat-final-output-flow.js`、`random-state.js` 放到后续阶段，等契约表补全再动。

### 5. 本节结论

这次更改不推翻计划方向，但改变了计划状态：

- 之前的“等待用户提交”阻塞已经解除。
- `chat.js` 主循环更长，且新增了 active scene 分层和旧背景媒体粘连重试，后续不能再只按旧的 output guard 方案拆。
- 第一刀仍建议 `session-compat.js`，因为它避开了刚改动的上下文窗口、随机 freshness 和输出守卫区域。

## 十六、2026-05-25 `session-compat.js` 第一刀执行归档

> 用户要求开始拆分计划；本节记录本轮实际执行步骤。每轮继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划。

### 1. 开工状态

- 已确认工作目录为 `E:\莲莲Bot`，当前分支为 `bot_ZZY`。
- 已确认 `HEAD` 与 `origin/bot_ZZY` 同步到 `18661af96ee7063c118f19cce24e322618aa20f4`。
- 已确认 `AGENTS.md` 未被 git 跟踪，且仍由 `.gitignore` 忽略。
- 当前只有本计划文档存在未提交修改；源码拆分从干净源码基线开始。

### 2. 本轮拆分范围

- 第一刀只拆 `packages/koishi-plugin-dongxuelian-ai/lib/index.js` 里的 Koishi / Satori session 兼容补丁。
- 目标新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/session-compat.js`。
- 范围包括 `patchElementText()`、`patchElementsToText()`、`patchElementId()`、`patchStripNickname()`、`patchBuildStripped()`、`patchInstallAccessors()`、`patchEnsureSession()`、`Bot.session` patch、`Session.prototype.resolve`、`Session.prototype.send`。
- 本轮不碰 active scene、随机回复、safe send、Agent 路由、chat 输出守卫。

### 3. 实际执行步骤

1. 新建 `packages/koishi-plugin-dongxuelian-ai/lib/session-compat.js`，完整搬入 session 兼容函数，并新增 `installSessionCompatibility()` 作为显式安装入口。
2. 文件头已按协作规则补 `MODULE` 注释，写明职责、边界和状态说明。
3. `index.js` 改为从 `./session-compat` 导入 `patchEnsureSession` 与 `installSessionCompatibility`。
4. `index.js` 顶层调用 `installSessionCompatibility({ KoishiSession, KoishiBot })`，保持原先随插件加载安装补丁的时机。
5. `withCurrentBot()` 仍调用 `patchEnsureSession(runtimeSession)`，但函数所有权已转移到新模块。
6. 删除 `index.js` 内原兼容补丁定义；入口减少约 120 行，当前 `index.js` 约 1681 行，`session-compat.js` 约 128 行。

### 4. cascade / check 同步

- `packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js` 已新增 `sessionCompat` 模块加载检查。
- `expectedExports.sessionCompat` 已锁定 8 个导出：`patchElementText`、`patchElementsToText`、`patchElementId`、`patchStripNickname`、`patchBuildStripped`、`patchInstallAccessors`、`patchEnsureSession`、`installSessionCompatibility`。
- `syntaxFiles` 已加入 `session-compat.js`。
- `duplicateScanFiles` 已加入 `session-compat.js`。
- `package.json` 不需要改；`npm run check` 已递归覆盖 `packages/koishi-plugin-dongxuelian-ai/lib`。

### 5. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/session-compat.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run check`：通过，检查 187 个文件。
- `npm run test:quick`：通过，`passed: 2316`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 938`、`failed: 0`、`skipped: 2`。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；通过模拟中间件场景覆盖普通聊天、命令、文件、随机、语音等已有真实行为入口。
2. 断言的失败现象：兼容补丁外移后，插件入口仍可加载，middleware 场景仍可发送回复，session stripped / parsed / send 兼容没有导致场景回归。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发；本轮也未部署、未重启、未推送。

### 6. 本节结论

- `session-compat.js` 第一刀已完成，属于纯结构搬运，未改用户可见策略。
- `index.js` 已把 Koishi / Satori 兼容层剥离成显式模块，入口复杂度下降。
- 下一刀仍建议 `bot-resolver.js`，目标为 `resolveCurrentBot()`、`createBotResolver()`、`withCurrentBot()`；开始前仍必须重新读取 `AGENTS.md` 和本计划。

## 十七、2026-05-25 `bot-resolver.js` 第二刀执行归档

> 本节记录第二刀实际执行结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划。

### 1. 拆分范围

- 从 `packages/koishi-plugin-dongxuelian-ai/lib/index.js` 拆出当前 bot 解析与异步 session 注入逻辑。
- 新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/bot-resolver.js`。
- 范围包括 `resolveCurrentBot()`、`createBotResolver()`、`withCurrentBot()`。
- 新模块只依赖 `./session-compat` 的 `patchEnsureSession()`，不反向 import `index.js`，不注册 middleware，不发送消息。

### 2. 实际执行步骤

1. 新建 `bot-resolver.js`，添加 `MODULE` 注释，写明职责、边界和状态说明。
2. 将 `resolveCurrentBot()`、`createBotResolver()`、`withCurrentBot()` 从 `index.js` 迁移到新模块。
3. `index.js` 改为从 `./bot-resolver` 导入 `createBotResolver` 与 `withCurrentBot`。
4. 保持 `createBotResolver(ctx, session)` 在 middleware / helper 中的调用形状不变。
5. 保持队列、文件处理和 Agent 路径中的 `withCurrentBot(session, resolveBot())` 行为不变。

### 3. cascade / check 同步

- `cascade-test.js` 已新增 `botResolver` 模块加载检查。
- `expectedExports.botResolver` 已锁定 `resolveCurrentBot`、`createBotResolver`、`withCurrentBot`。
- `syntaxFiles` 已加入 `bot-resolver.js`。
- `duplicateScanFiles` 已加入 `bot-resolver.js`。
- cross-file regression guard 已从检查 `index.js` 内部函数，改为检查 `index.js` 引入 `./bot-resolver`，并检查新模块仍定义 `createBotResolver()` / `withCurrentBot()`。

### 4. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/bot-resolver.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2322`、`failed: 0`、`skipped: 1`。

### 5. 本节结论

- `bot-resolver.js` 第二刀已完成，属于纯结构搬运。
- 当前 bot 解析逻辑从入口剥离，异步队列中 session 注入最新 bot 的行为保持原样。
- 下一步继续拆入口低风险状态模块，优先选择同频道任务队列或 event dump。

## 十八、2026-05-25 `channel-task-queue.js` 第三刀执行归档

> 本节记录第三刀实际执行结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划。

### 1. 拆分范围

- 从 `index.js` 拆出同频道串行任务队列。
- 新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/channel-task-queue.js`。
- 范围包括 `channelQueues`、`channelQueueDepth`、`channelQueuedDepth` 三个 Map，以及 `enqueueForChannel()`。
- 新增 `clearChannelQueues()`，供 `ctx.on('dispose')` 清理队列状态。
- 本轮不改变队列深度、超时、过期丢弃、错误吞掉策略。

### 2. 实际执行步骤

1. 新建 `channel-task-queue.js`，添加 `MODULE` 注释，写明职责、边界和状态所有权。
2. 将 `channelQueues`、`channelQueueDepth`、`channelQueuedDepth` 与 `enqueueForChannel()` 从 `index.js` 搬到新模块。
3. 新模块从 `./logging-config` 引入 `logDebug()`，保持原来的 reject / stale queued task 调试日志。
4. `index.js` 改为导入 `enqueueForChannel` 与 `clearChannelQueues`。
5. `ctx.on('dispose')` 中原本三行 `Map.clear()` 改为调用 `clearChannelQueues()`。
6. `index.js` 中两个 `enqueueForChannel(channelKey, async () => ...)` 调用点保持参数与行为不变。

### 3. cascade / check 同步

- `cascade-test.js` 已新增 `channelTaskQueue` 模块加载检查。
- `expectedExports.channelTaskQueue` 已锁定 `enqueueForChannel` 与 `clearChannelQueues`。
- `syntaxFiles` 已加入 `channel-task-queue.js`。
- `duplicateScanFiles` 已加入 `channel-task-queue.js`。
- `package.json` 不需要改；`npm run check` 递归覆盖 `lib` 下新增文件。

### 4. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/channel-task-queue.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2327`、`failed: 0`、`skipped: 1`。
- `npm run check`：通过，检查 189 个文件。
- `npm run test:scenario`：通过，`passed: 938`、`failed: 0`、`skipped: 2`。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；通过完整模拟场景覆盖聊天、Agent auto route、文件、随机、并发队列、命令等入口。
2. 断言的失败现象：队列状态外移后，同频道排队任务仍能执行，Agent / chat 中间件路径没有回归，dispose 清理仍存在。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发；本轮未部署、未重启、未推送。

### 5. 本节结论

- `channel-task-queue.js` 第三刀已完成，入口中的同频道队列状态已归属到独立模块。
- 这一刀没有改变聊天、随机回复、Agent 路由、发送策略，只是纯搬运队列状态与函数。
- 后续可继续拆 `event-dump.js`，它的状态与落盘逻辑边界清晰，但需要保留 admin command 的依赖注入形态。

## 十九、2026-05-25 `event-dump.js` 第四刀执行归档

> 本节记录第四刀实际执行结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划。

### 1. 拆分范围

- 从 `index.js` 拆出一次性原始事件抓取状态与安全落盘逻辑。
- 新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/event-dump.js`。
- 范围包括 `armedEventDumpCache`、`getArmedEventDump()`、`armEventDump()`、`clearArmedEventDump()`、`dumpSessionEvent()`。
- 新模块从 `constants.js` 读取 `EVENT_DUMP_DIR` 与 `EVENT_DUMP_ARM_EXPIRE_MS`，从 `conversation.js` 读取 `getChannelKey()`，从 `utils.js` 读取 `getSenderUserId()`、`sanitizeFileToken()`、`safeJsonStringify()`。
- 本轮保留 `admin-commands.js` 的依赖注入形态，不让命令模块反向 import `index.js`。

### 2. 实际执行步骤

1. 新建 `event-dump.js`，添加 `MODULE` 注释，写明职责、边界和状态说明。
2. 将事件抓取缓存、开启、查询、清理与落盘函数从 `index.js` 搬到新模块。
3. `index.js` 改为从 `./event-dump` 导入 `getArmedEventDump`、`armEventDump`、`clearArmedEventDump`、`dumpSessionEvent`。
4. `index.js` 删除对 `EVENT_DUMP_DIR`、`EVENT_DUMP_ARM_EXPIRE_MS`、`getSenderUserId`、`sanitizeFileToken`、`safeJsonStringify` 的无关直接引用。
5. 保持中间件在分析消息后检查 armed state、抓到下一条事件后落盘并清理状态的顺序不变。
6. 保持 `AI抓事件`、`AI抓事件查看`、`AI抓事件取消` 通过 `handleCommand()` 依赖注入访问事件抓取函数。

### 3. cascade / check 同步

- `cascade-test.js` 已新增 `eventDump` 模块加载检查。
- `expectedExports.eventDump` 已锁定 `getArmedEventDump`、`armEventDump`、`clearArmedEventDump`、`dumpSessionEvent`。
- `syntaxFiles` 已加入 `event-dump.js`。
- `duplicateScanFiles` 已加入 `event-dump.js`。
- `package.json` 不需要改；`npm run check` 递归覆盖 `lib` 下新增文件。

### 4. 场景测试补充

- `packages/koishi-plugin-dongxuelian-ai/test/scenarios/command.test.js` 已新增 `AI抓事件` 回归。
- 覆盖内容：
  - `AI抓事件` 会回复已开始抓取。
  - 下一条普通消息会在临时 `DATA_DIR/ai-event-dumps` 写出一个 JSON。
  - JSON 中保存目标消息的 `messageId`、`content` 与 `plain`。
  - 抓取完成后 `AI抓事件查看` 返回当前没有待抓取。
  - `AI抓事件取消` 会回复已取消，并阻止取消后的普通消息继续写入。
- 执行测试时发现一个既有行为：如果频道已 armed，`AI抓事件取消` 这条命令本身会先被抓取，再进入取消逻辑。本轮为结构等价拆分，没有顺手改变这个行为；如需修正，应另开行为修复并补专门 scenario。

### 5. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/event-dump.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2334`、`failed: 0`、`skipped: 1`。
- `npm run check`：通过，检查 190 个文件。
- `npm run test:scenario`：通过，`passed: 946`、`failed: 0`、`skipped: 2`。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；新增场景使用真实命令输入 `AI抓事件`、`AI抓事件查看`、`AI抓事件取消` 与下一条普通消息作为回归入口。
2. 断言的失败现象：事件抓取状态外移后，命令仍能 arm / view / cancel，下一条目标消息仍会安全落盘，抓取后状态会清理，取消后不会继续抓后续普通消息。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并使用临时 `DATA_DIR`；外部 HTTP、模型、TTS、文件下载等仍由测试框架 mock 或隔离。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发；本轮未部署、未重启、未推送。

### 6. 本节结论

- `event-dump.js` 第四刀已完成，入口中的一次性事件抓取状态与落盘逻辑已归属到独立模块。
- 这一刀没有改变聊天、随机回复、Agent 路由、发送策略，也没有改变 admin command 的注入边界。
- 后续可继续拆 `startup-schedulers.js`，它的职责边界是启动期定时器与 dispose 清理；拆分时必须保留每日统计清理和表达学习 harvest 的 timer 生命周期。

## 二十、2026-05-25 `startup-schedulers.js` 第五刀执行归档

> 本节记录第五刀实际执行结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划；本轮同时使用后台只读 agent 审计了 timer 生命周期风险。

### 1. 拆分范围

- 从 `index.js` 拆出启动期周期维护任务的 timer 状态和调度函数。
- 新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/startup-schedulers.js`。
- 范围包括 `dailyCleanupTimer`、`expressionHarvestTimer`、`getNextShanghaiMidnightDelayMs()`、`scheduleDailyStatsCleanup()`、`getExpressionHarvestDelayMs()`、`scheduleExpressionHarvest()`。
- 新增 `clearStartupSchedulers()`，供 `ctx.on('dispose')` 统一清理两个 timer。
- 本轮不改变 ready 阶段“立即执行一次 `cleanupDailyStatsFiles().catch(...)`”的行为；该立即清理仍保留在 `index.js` 中，定时器模块只负责后续周期调度。

### 2. 实际执行步骤

1. 新建 `startup-schedulers.js`，添加 `MODULE` 注释，写明职责、边界和状态说明。
2. 将上海午夜延迟计算、表达学习提前 5 分钟计算、每日统计清理定时器、表达学习 harvest 定时器从 `index.js` 搬到新模块。
3. 新模块从 `conversation.js` 引入 `cleanupDailyStatsFiles()` 与 `trimChannelRuntimeCaches()`，从 `utils.js` 引入 `todayCst()`，从 `logging-config.js` 引入 `logDebug()`，从 `expression-abstractor.js` 引入 harvest 执行与诊断格式化。
4. `index.js` 改为从 `./startup-schedulers` 导入 `scheduleDailyStatsCleanup`、`scheduleExpressionHarvest`、`clearStartupSchedulers`。
5. `ctx.on('ready')` 中保留原调用顺序：先执行一次即时 daily cleanup，再启动两个周期定时器。
6. `ctx.on('dispose')` 中原来两个 `clearTimeout` 改为调用 `clearStartupSchedulers()`，并保留随机 pending timer、channel queue 等其他清理顺序。

### 3. cascade / check 同步

- `cascade-test.js` 已新增 `startupSchedulers` 模块加载检查。
- `expectedExports.startupSchedulers` 已锁定 `getNextShanghaiMidnightDelayMs`、`scheduleDailyStatsCleanup`、`getExpressionHarvestDelayMs`、`scheduleExpressionHarvest`、`clearStartupSchedulers`。
- `syntaxFiles` 已加入 `startup-schedulers.js`，并新增 syntax runner 覆盖断言。
- `duplicateScanFiles` 已加入 `startup-schedulers.js`。
- cross-file regression guard 已更新：不再用已搬走的 `getNextShanghaiMidnightDelayMs` 作为 sticker-shadow 检查截断边界，改为 `cacheSmallFileBackground`；同时新增“`index.js` 只 import / ready 调用 / dispose 清理 scheduler，不再内联 timer 状态”的守卫。
- cascade 已新增时间算法断言，覆盖上海日界线、1 秒下限和表达学习 harvest 提前 5 分钟。

### 4. 后台 agent 审计结论采纳

- 审计确认这刀最大风险是 timer 所有权，不是时间计算本身。
- 已保留 `try/finally` 中重新 `setTimeout` 的形态，没有改成 `setInterval`，避免长任务重叠。
- 已保留每次挂 timer 后的 `unref()`。
- 已保留 dispose 时 `clearTimeout` 后置空，只是收口到 `clearStartupSchedulers()`。
- 已保留 ready 阶段立即 daily cleanup，不吞进 scheduler。

### 5. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/startup-schedulers.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2347`、`failed: 0`、`skipped: 1`。
- `npm run check`：通过，检查 191 个文件。
- `npm run test:scenario`：通过，`passed: 946`、`failed: 0`、`skipped: 2`。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；通过模拟中间件全场景覆盖插件 ready / dispose 相关业务链路，并用 cascade 锁定 scheduler 时间算法。
2. 断言的失败现象：timer 状态外移后，入口仍启动 daily cleanup 与 expression harvest scheduler，dispose 仍清理 timer，上海午夜和提前 5 分钟算法未漂移。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 的时间算法断言为纯函数断言。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未等待真实午夜 timer 触发；本轮未部署、未重启、未推送。

### 6. 本节结论

- `startup-schedulers.js` 第五刀已完成，入口中的启动期 timer 生命周期已归属到独立模块。
- 这一刀没有改变聊天、随机回复、Agent 路由、发送策略，也没有改变表达学习 harvest 的执行时机。
- 后续可继续拆 `diagnostics.js` / `shadow-diagnostics.js`，或在补完契约后拆 `message-segment.js`；`safe-send.js` 与随机状态机仍建议后置。

## 二十一、2026-05-25 `diagnostics.js` 第六刀执行归档

> 本节记录第六刀实际执行结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划。

### 1. 拆分范围

- 从 `index.js` 拆出入口旁路诊断 helper。
- 新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/diagnostics.js`。
- 范围包括 `logReplyTimingDiagnostic()`、`logAffectRouterDiagnostic()`、`buildAffectRouterDiagnosticForShadow()`、`logAffectRouterDiagnosticForOutputShadow()`、`logStickerShadowPlan()`、`logStickerShadowIngestDiagnostic()`、`logStickerShadowSendDiagnostic()`。
- 新模块依赖 `reply-timing.js`、`affect-router.js`、`sticker-shadow.js`、`random-voice-rate.js` 与 `logging-config.js`。
- 本轮不改变诊断开关、日志 channel、JSONL 写入内容、图片入库顺序、发送前 shadow 观察点，也不让诊断模块发送消息或修改回复。

### 2. 实际执行步骤

1. 新建 `diagnostics.js`，添加 `MODULE` 注释，写明职责、边界和状态说明。
2. 将回复时机诊断、情绪路由诊断、表情 shadow ingest / send 诊断 helper 从 `index.js` 搬到新模块。
3. `index.js` 改为从 `./diagnostics` 导入 `logReplyTimingDiagnostic`、`logAffectRouterDiagnosticForOutputShadow`、`logStickerShadowIngestDiagnostic`、`logStickerShadowSendDiagnostic`。
4. `index.js` 删除对 `reply-timing.js`、`affect-router.js`、`sticker-shadow.js` 的直接 helper 依赖。
5. `index.js` 删除已无使用的 `isDebugLogEnabled` 与 `getRandomVoiceRate` 直接 import；随机语音概率读取归入 `diagnostics.js` 内部。
6. 保持调用点不变：
   - 图片已写入 image-store 后、分析队列前调用 `logStickerShadowIngestDiagnostic()`。
   - 随机回复发送前调用 `logAffectRouterDiagnosticForOutputShadow()` 与 `logStickerShadowSendDiagnostic()`。
   - 普通回复发送前调用同一组输出 shadow 诊断。
   - legacy random 判定后继续调用 `logReplyTimingDiagnostic()`。

### 3. cascade / check 同步

- `cascade-test.js` 已新增 `diagnostics` 模块加载检查。
- `expectedExports.diagnostics` 已锁定 7 个导出。
- `syntaxFiles` 已加入 `diagnostics.js`。
- `duplicateScanFiles` 已加入 `diagnostics.js`。
- cross-file regression guard 已更新：原先检查 `index.js` 内 helper 的断言改为检查 `diagnostics.js` 内 helper debug-gated、不发送、不改消息；同时新增“`index.js` 只 import 诊断 helper，不再内联定义”的守卫。
- 既有“图片 shadow ingest 在 image-store 后、分析队列前”和“发送 shadow 只观察发送前 reply”的顺序守卫保留。

### 4. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/diagnostics.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2358`、`failed: 0`、`skipped: 1`。
- `npm run check`：通过，检查 192 个文件。
- `npm run test:scenario`：通过，`passed: 946`、`failed: 0`、`skipped: 2`。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；通过 sticker、random、chat、image/file 等模拟场景覆盖原诊断调用点所在链路。
2. 断言的失败现象：旁路诊断 helper 外移后，入口仍在原顺序调用 shadow ingest / send / reply timing 诊断，诊断模块仍不发送消息、不修改 reply、不接管概率。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；sticker shadow JSONL 在测试临时 `DATA_DIR` 内验证。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发；本轮未部署、未重启、未推送。

### 5. 本节结论

- `diagnostics.js` 第六刀已完成，入口中的旁路诊断日志 helper 已归属到独立模块。
- 这一刀没有改变聊天、随机回复、Agent 路由、发送策略，也没有改变 shadow 诊断的触发位置。
- 后续建议先拆 `message-segment.js` 前补契约；它会触碰图片/文件/媒体锚点解析，需要更仔细地对照 active scene 与文件追问 scenario。

## 二十二、2026-05-25 `message-segment.js` 第七刀执行归档

> 本节记录第七刀实际执行结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划；本轮同时使用后台只读 agent 审计了媒体/文件 segment 解析风险。

### 1. 拆分范围

- 从 `index.js` 拆出入站图片与文件消息片段解析 helper。
- 新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/message-segment.js`。
- 范围包括 `decodeEntityAttribute()`、`extractAttrValue()`、`extractCqAttrValue()`、`extractImageRefFromContent()`、`appendUniqueSegments()`、`getMessageSegments()`、`normalizeSegmentData()`、`extractFileRefFromContent()`、`getFileSegmentData()`。
- 新模块只依赖 `utils.js` 的 `normalizeUrl()` 与 `extractImageUrls()`，不持有状态，不读写文件，不写 conversation。
- 本轮保留 `index.js` 对 `session`、`content`、`plain`、`analyzed`、`channelKey`、`messageId` 的所有权；新模块只返回解析结果。

### 2. 实际执行步骤

1. 新建 `message-segment.js`，添加 `MODULE` 注释，写明职责、边界和状态说明。
2. 搬运前重新核对当前 `index.js` 最新实现，发现它已比旧计划更新：
   - 图片支持 `CQ:image` / `CQ:img`、`<img>` / `<image>`、以及 `extractImageUrls()` 支持的图片引用。
   - `normalizeSegmentData()` 当前合并顺序为 `attributes` < `attrs` < `data`。
   - `appendUniqueSegments()` 按对象引用去重。
3. 按最新实现校准新模块后，`index.js` 改为从 `./message-segment` 导入 `extractImageRefFromContent`、`getMessageSegments`、`normalizeSegmentData`、`getFileSegmentData`。
4. 删除 `index.js` 内原解析 helper 定义。
5. 删除 `index.js` 中已无使用的 `normalizeUrl` / `extractImageUrls` 直接 import；这两个工具只在 `message-segment.js` 内使用。
6. 保持调用点不变：
   - 图片入库前仍从 session segments 与 content 双路提取图片引用。
   - 文件入库前仍优先取 `type === 'file'` segment，再回退解析 `session.content`。

### 3. cascade / check 同步

- `cascade-test.js` 已新增 `messageSegment` 模块加载检查。
- `expectedExports.messageSegment` 已锁定 9 个导出。
- `syntaxFiles` 已加入 `message-segment.js`。
- `duplicateScanFiles` 已加入 `message-segment.js`。
- cross-file regression guard 已新增“`index.js` 只 import message-segment helper，不再内联定义”的守卫。
- cascade 已新增 helper 行为断言，覆盖：
  - CQ image / img 的 `url` 与 `file` 提取。
  - HTML image `src` 提取。
  - `attributes` / `attrs` / `data` 合并优先级。
  - file segment 优先于 content fallback。
  - CQ file 的 `name`、`file`、`url`、`size`、`mime` 解析。

### 4. 后台 agent 审计结论采纳

- 审计确认该模块风险低到中低，关键风险在解析优先级、合并顺序和返回形状。
- 已保留图片解析优先级：CQ image/img -> HTML img/image -> `extractImageUrls()` 支持的图片引用。
- 已保留文件解析优先级：CQ file -> HTML `<file>` -> `null`。
- 已保留 `normalizeSegmentData()` 合并顺序：`attributes` < `attrs` < `data`。
- 已保留 `appendUniqueSegments()` 按对象引用去重，没有改成内容/id 去重。
- 已保留 `getFileSegmentData()` 只有没有 `type === 'file'` segment 时才回退到 `session.content`。

### 5. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/message-segment.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2376`、`failed: 0`、`skipped: 1`。
- `npm run check`：通过，检查 193 个文件。
- `npm run test:scenario`：通过，`passed: 946`、`failed: 0`、`skipped: 2`。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过文件/图片相关 scenario 覆盖图片追问、CQ content-only file、Koishi elements file、mixed segment sources、file anchor、active group scene 等入口。
2. 断言的失败现象：解析 helper 外移后，图片/文件 segment 仍按原优先级解析，文件元数据仍能入库，文件锚点仍进入 active scene，图片追问仍能读取近期媒体锚点。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 对 helper 解析是纯函数断言。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖所有 adapter 可能产生的未知 segment shape；本轮未部署、未重启、未推送。

### 6. 本节结论

- `message-segment.js` 第七刀已完成，入口中的入站媒体/文件 segment 解析已归属到独立模块。
- 这一刀没有改变 active scene、文件追问、图片追问、随机回复、Agent 路由、发送策略。
- 后续低风险入口 helper 已基本完成；继续推进前建议重新评估 `index.js` 剩余函数，下一阶段可能进入 `runtime-settings.js` / `user-blacklist.js` 或 `safe-send.js` 契约拆分，风险会明显高于前七刀。

## 二十四、2026-05-25 `safe-send.js` 第九刀执行归档

> 本节记录第九刀实际执行结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `index.js`、`safe-send.js`、`send-guard.js`、`reply.js`、`rare-voice.js`、`cascade-test.js` 的当前状态。

### 1. 拆分范围

- 从 `index.js` 拆出发送安全层。
- 新模块为 `packages/koishi-plugin-dongxuelian-ai/lib/safe-send.js`。
- 范围包括 `sendFailState`、`logStaleRandomSkip()`、`notifyAdminsSendFailure()`、`resetSendFailState()`、`logPlatformMute()`、`handleRateLimitedSendFailure()`、`safeSendRepeat()`、`safeSendReply()`、`safeSendRareVoice()`。
- `index.js` 仅保留随机 freshness 判定包装 `safeSendReplyWithFreshness()`，实际发送、限流、禁言、复读、语音发送全部由 `safe-send.js` 承担。
- 本轮不改发送策略，不改随机触发概率，不改平台禁言规则，只做职责外移和接线校准。

### 2. 实际执行步骤

1. 新建 `safe-send.js`，写入 MODULE 注释，明确职责、边界和 `sendFailState` 状态说明。
2. 将发送失败通知、限流状态、禁言日志、限流重试、复读发送、罕见语音发送从 `index.js` 搬到新模块。
3. `safe-send.js` 通过依赖注入使用 `reply.js`、`send-guard.js`、`runtime-config.js`、`bot-resolver.js`、`utils.js`、`rare-voice.js`，不反向 import `index.js`。
4. `index.js` 改为引入 `safe-send.js`，并保留 `isSafeSendReplyFresh()` 作为随机 freshness 的入口包装。
5. 入口中所有 `safeSendReply()` 调用改为 `safeSendReplyWithFreshness()`，确保调用链还能复用当前频道消息版本和显式交互版本判断。
6. 补回 `RANDOM_TRIGGER_WARMUP` 与 `RANDOM_TRIGGER_RAMP` 的导入，修正迁移后随机概率计算的接线缺口。
7. `cascade-test.js` 已同步新增 `safe-send.js` 的 `modPaths`、`expectedExports`、`syntaxFiles`、`duplicateScanFiles`，并新增入口不再内联 safe-send helper 的守卫。
8. `send-guard.test.js` 补了直接模块导出检查与 freshness 跳过回归，并继续保留平台禁言、限流和管理员通知的 scenario。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/safe-send.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/scenarios/send-guard.test.js`：通过。
- `npm run test:quick`：通过，`passed: 2399`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。

### 4. 本节结论

- `safe-send.js` 第九刀已完成，入口中的发送安全职责已外移。
- 这一刀没有改变用户可见发送语义，只是把限流/禁言/复读/罕见语音的所有权收口到新模块。
- 下一步建议继续补 `chat.js` 的余债，或把 `random-state.js` 的状态机收束成独立模块。

## 二十三、2026-05-25 `runtime-settings.js` / `user-blacklist.js` 第八刀执行归档

> 本节记录运行时随机配置缓存与用户黑名单缓存的归档补漏。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并重新核对 `index.js`、`runtime-settings.js`、`user-blacklist.js`、`cascade-test.js` 的当前状态。

### 1. 拆分范围

- `runtime-settings.js` 负责群聊主动回复白名单与基础概率缓存。
- `user-blacklist.js` 负责用户黑名单缓存，并复用 `runtime-settings.js` 的文件指纹工具。
- `index.js` 只保留接线和调用，不再内联实现这两类缓存。
- 本轮不改随机概率、黑名单判定或管理员权限逻辑，只修拆分后的导入接线和归档说明。

### 2. 实际执行步骤

1. 先重新核对 `index.js` 的导入区、`runtime-settings.js`、`user-blacklist.js` 和 `cascade-test.js`。
2. 发现 `runtime-settings.js` 已完成结构等价拆分，导出了 `randomWhitelistCache`、`randomRateCache`、`loadRuntimeSettings()`、`getRandomTriggerBaseRate()`、`getRandomWhitelistStatus()`、`getFileFingerprint()`。
3. 发现 `user-blacklist.js` 已完成结构等价拆分，导出了 `loadUserBlacklist()`、`setBlacklistFingerprint()`，但 `index.js` 的导入一度遗漏了 `loadUserBlacklist`。
4. 以最小补丁把 `loadUserBlacklist` 接回 `index.js`，没有改缓存语义、命中规则或中间件顺序。
5. 重新做 `node -c` 校验，确认 `index.js`、`runtime-settings.js`、`user-blacklist.js` 语法都正常。
6. 复核 `cascade-test.js` 已覆盖：
   - `runtime-settings` / `user-blacklist` 模块加载
   - `expectedExports.runtimeSettings`
   - `expectedExports.userBlacklist`
   - `syntaxFiles` / `duplicateScanFiles`
   - `index.js` 只 import、不内联定义 helper 的守卫

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/runtime-settings.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/user-blacklist.js`：通过。
- `rg` 复核：`index.js` 已正确引用 `loadRuntimeSettings`、`getRandomTriggerBaseRate`、`getRandomWhitelistStatus`、`loadUserBlacklist`。
- `cascade-test.js` 已有对应模块加载与导出守卫，没有新增架构盲区。

### 4. 本节结论

- `runtime-settings.js` 与 `user-blacklist.js` 的结构等价拆分已完成，入口导入也已对齐。
- 这一刀没有改变随机回复概率、黑名单命中、管理员命令或消息中间件行为。
- 下一步更适合进入 `safe-send.js` 的契约拆分，但它的行为面更大，需要先把限流、禁言、随机 freshness、罕见语音的边界画清楚再动刀。

## 二十五、2026-05-25 本轮复扫与下一刀判断

> 本节记录用户继续任务后的复扫结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并重新核对当前工作区与 `index.js` / `chat.js` / `skills-loader.js` 的最新状态。

### 1. 当前工作区与体量

- 当前分支仍为 `bot_ZZY`，`HEAD` 与 `origin/bot_ZZY` 同步，工作区存在既有未提交修改。
- 当前实测尺寸：`packages/koishi-plugin-dongxuelian-ai/lib/index.js` 约 55,934 bytes / 1,093 行；`packages/koishi-plugin-dongxuelian-ai/lib/chat.js` 约 76,392 bytes / 1,386 行。
- 这意味着前面已完成的入口拆分已经把 `index.js` 压下来了，但 `chat.js` 仍是当前最大的单文件债之一。

### 2. 本轮复扫结论

- `index.js` 目前已把 session 兼容、bot resolver、channel task queue、event dump、startup schedulers、diagnostics、message segment、runtime settings、user blacklist、safe send 等职责外移，剩余更偏入口编排、Agent 复述桥、随机调度、文件/语音/命令路由和 chat 接线。
- `chat.js` 仍保留技能加载、skill 内容缓存、fingerprint refresh、`loreMeta` 兼容、system prompt 片段构建、output guard、topic switch、jailbreak、`callOpenAI` 和主聊天编排。
- `skills-loader.js` 已是现成公共 loader，但它与 `chat.js` 的能力并不完全重合：`chat.js` 仍有 `loreMeta:*`、`skillsContentCacheFingerprint` 和 `refreshSkillsContentCacheIfChanged()` 的兼容行为，不能简单替换成另一套平行实现。
- 因此，下一刀不建议直接再造一个全量的 `chat-skills.js`，更适合做“整合/适配层”：要么增强 `skills-loader.js` 以兼容 `chat.js` 现有行为，要么做一个很薄的 `chat-skills.js` 只负责桥接现有 loader 与 `chat.js` 的 fingerprint / `loreMeta` 兼容。

### 3. 当前优先级判断

- `index.js` 继续往下拆的优先级已经低于 `chat.js` 的技能/提示词/守卫区，因为入口已经从 1,900+ 行压到 1,000 行级别。
- `chat.js` 的最大剩余风险点不是 `callOpenAI` 本身，而是后半段工具循环、文件后续、vision retry、reminder fallback、uploaded-file fallback 和最终输出守卫还都挤在同一个函数里。
- `skills-loader.js` 的现状说明：后续拆分要避免“双 loader”并存，优先复用现成模块，再把 `chat.js` 里的兼容逻辑收口到更薄的适配层。

### 4. 对后续拆分的保守预估

- 如果只把 `chat.js` 中可纯搬运的技能加载/缓存/提示词片段继续下沉，并保留当前兼容行为，`chat.js` 预计还能再减少约 120~220 行，约 6~12 KB。
- 如果后续连 tool/file/final-output 三段也按计划切开，`chat.js` 的长期目标大致可以压到 700~900 行、45~55 KB 左右，但这需要分几刀完成，不能当成单次目标。
- `index.js` 若继续拆掉 Agent 复述桥、random-state、剩余路由和部分 middleware helper，长期还可以再压一截，但当前更适合先把 `chat.js` 的余债拆清楚。

### 5. 下一步建议

1. 先补一节 `chat-skills` 的拆分判断，明确是增强 `skills-loader.js` 还是新增薄适配层。
2. 然后从 `chat.js` 的后半段工具循环 / 文件后续 / vision retry / 输出守卫里选一块边界清晰的职责继续拆。
3. 每次继续任务前仍要先读 `AGENTS.md` 和本计划，避免上下文压缩后忘记当前边界。

## 二十六、2026-05-25 `skills-loader.js` / `chat.js` 技能缓存收口执行归档

> 本节记录本轮 `chat.js` 技能加载与缓存职责收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核 `chat.js`、`skills-loader.js`、`persona-lore-router.js`、`chat-prompt-builder.js`、`cascade-test.js` 的当前状态。

### 1. 拆分范围

- 将 `chat.js` 中技能文件读取、frontmatter 剥离、目录 fingerprint、skill 内容缓存、刷新 promise、`loreMeta` 兼容和基础 system prompt helper 收口到既有 `skills-loader.js`。
- `chat.js` 保留聊天主流程所有权，只通过 `skills-loader.js` 取得 `loadSkills()`、`loadSkillsContentCache()`、`refreshSkillsContentCacheIfChanged()`、`getSkillsContentCache()` 和基础 prompt helper。
- 本轮不改模型调用、不改人格选择、不改 lore 路由、不改工具调用，也不改输出守卫语义。

### 2. 实际执行步骤

1. 增强 `skills-loader.js`，保留原公共 loader 定位，同时补齐 `chat.js` 原有行为：
   - 支持 `DONGXUELIAN_CHAT_SKILL_FILE_MAX_BYTES` 优先的文件大小上限。
   - 使用 CRLF 兼容的 `stripSkillFrontmatter()`。
   - 新增 `getSkillDirectoryFingerprint()` / `getSkillsContentFingerprint()`。
   - 新增 `refreshSkillsContentCacheIfChanged()`，保留单飞 promise，避免并发重复刷新。
   - lore 文件继续通过 `parsePersonaSchemaFrontmatter()` 生成 `lore:` 与 `loreMeta:` 两类缓存键，并保留 frontmatter `name` 与文件名双 key 兼容。
2. `chat.js` 删除本地 skill cache / fingerprint / loader 函数，改从 `./skills-loader` 导入对应接口。
3. `chat()` 开头在刷新后调用 `getSkillsContentCache()`，继续把当前 cache 传给 `routePersonaLore()` 和 `createChatPromptLoreMessage()`。
4. 删除 `chat.js` 底部本地 `getSkillsCount()` wrapper，直接复用 `skills-loader.js` 的导出。
5. `cascade-test.js` 新增 `skillsLoader` 模块加载与导出守卫，加入 `syntaxFiles` / `duplicateScanFiles`。
6. `cascade-test.js` 新增 cross-file guard，要求 `chat.js` 只 import `skills-loader`，不再内联 `skillsCache`、`readChatSkillTextIfSmall()`、`getChatSkillsContentFingerprint()` 等技能缓存实现。
7. 修正 isolated skill cache 测试的 require cache 清理列表，加入 `skills-loader`，保证 `DONGXUELIAN_AI_DATA_DIR` 临时目录隔离在模块迁移后仍生效。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/skills-loader.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2418`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 196 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 persona prompt、Terra lore、chat middleware、文件/图片追问、Agent retell、随机回复等 scenario 覆盖原 skill cache 所在主链路。
2. 断言的失败现象：技能缓存迁出 `chat.js` 后，CRLF frontmatter、dashboard 编辑 lore 后刷新、`loreMeta` 缓存、基础 prompt helper、chat 对外导出形状仍保持可用。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 对 loader 导出、缓存刷新和源码边界做静态与临时目录断言。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实 dashboard 热编辑后长时间运行进程里的人工操作链路；本轮未部署、未重启、未推送。

### 4. 体量变化

- `chat.js` 从本轮开始前约 76,392 bytes / 1,386 行，降到约 70,665 bytes / 1,238 行。
- `skills-loader.js` 增至约 6,888 bytes / 191 行，承接技能文件加载、缓存刷新和基础 prompt helper 所有权。

### 5. 本节结论

- `chat.js` 的技能加载/缓存职责已完成收口，没有再持有 `skillsCache`、`skillsContentCacheFingerprint` 或本地 skill 文件读取 helper。
- 这一步没有改变用户可见回复、人格/lore 注入策略或模型请求策略。
- 下一步更适合继续拆 `chat.js` 后半段：优先候选是 tool-call loop、file follow-up/fallback、vision blindness retry 或 final output guard。

## 二十七、2026-05-25 `chat-tool-flow.js` 工具调用流执行归档

> 本节记录 `chat.js` 后半段工具调用与文件补偿逻辑收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `chat.js` 后半段、`chat-tools.js`、`file-followup-guard.js`、`reminder-route.js`、`uploaded-file-action-route.js` 与相关 scenario。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/chat-tool-flow.js`。
- 从 `chat.js` 拆出：
  - tool_calls 首轮执行与轻量工具多轮循环。
  - heavy tool follow-up 文案与 `heavyToolsRequested` 返回。
  - 外部工具拒绝策略提示。
  - analyze_file 文件证据直出与未守卫文件追问补偿。
  - reminder / scheduled task 明确请求 fallback。
  - uploaded file variant 拒绝重试与直接 fallback。
  - tool 使用状态标记 `usedAnalyzeFileTool`、`usedReminderActionTool`、`usedUploadedFileVariantTool`、`hasFileToolEvidence`。
- `chat.js` 保留模型调用所有权，通过 `callModel: callOpenAI` 注入，不让新模块反向 import `chat.js`。
- 本轮不改变工具暴露规则、不改变 tool result 格式、不改变文件证据文案、不改变 reminder / uploaded-file fallback 文案。

### 2. 实际执行步骤

1. 新建 `chat-tool-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将 `updateChatToolUsageState()`、QQ tool context 构造、tool_calls 处理循环、heavy tool 返回、file evidence 补偿、reminder fallback、uploaded file fallback 迁入新模块。
3. `chat.js` 删除对 `handleChatToolCalls()`、`executeChatTool()`、file-followup helper、reminder-route helper、uploaded-file fallback helper、external-tool-policy helper 的直接依赖。
4. `chat.js` 仍负责构造 `chatTools`、`fileFollowupState`、`activeFileContext`，然后调用 `handleChatToolFlow()`。
5. `chat.js` 根据 `handleChatToolFlow()` 返回结果继续执行 memory prompt、vision blindness retry、输出守卫和最终持久化。
6. `cascade-test.js` 新增 `chatToolFlow` 模块加载、导出、语法检查、重复函数扫描。
7. `cascade-test.js` 新增 cross-file guard，确保 `chat.js` 只 import `chat-tool-flow`，不再内联 `handleChatToolCalls()` / `executeChatTool()` / `updateChatToolUsageState()` 工具执行循环。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat-tool-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
- `npm run test:quick`：通过，`passed: 2425`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 197 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 heavy web_search / web_fetch、disabled web_search、lightweight tool loop、reminder fallback、file follow-up、uploaded file variant 等 scenario 覆盖原工具调用流。
2. 断言的失败现象：工具调用流迁出后，heavy tool 仍返回内部请求交给入口 Agent 处理，禁用工具仍拒绝执行，文件证据仍能直接回答，reminder / uploaded-file fallback 仍可创建并返回自然回复。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出与边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部搜索、真实文件下载和真实上传文件到群的生产链路；本轮未部署、未重启、未推送。

### 4. 体量变化

- `chat.js` 从上一刀后约 70,665 bytes / 1,238 行，降到约 60,585 bytes / 1,045 行。
- 新增 `chat-tool-flow.js` 约 12,041 bytes / 272 行，承接工具执行流和文件/提醒/上传文件 fallback 所有权。

### 5. 本节结论

- `chat.js` 已不再内联工具执行循环，主流程更接近“准备上下文 -> 调模型 -> 调工具流 -> 输出守卫 -> 保存”的编排形态。
- 这一刀没有改变用户可见工具行为，也没有改变 chat 模型调用所有权。
- 下一步更适合继续拆 final output guard / retry flow，或者单独拆 vision blindness retry；这两块仍在 `chat.js` 后半段占用较多行数。

## 二十八、2026-05-25 `chat-final-output-flow.js` 最终输出守卫执行归档

> 本节记录 `chat.js` 最终输出清洗、重试与兜底守卫的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `chat.js`、`chat-final-output-flow.js`、`reply-guard.js`、`random-reply-mode.js`、`persona-fallback.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/chat-final-output-flow.js`。
- 从 `chat.js` 拆出：
  - 最终回复最大长度选择 `getReplyMaxChars()`。
  - 禁用表达、thinking 泄漏、内部上下文泄漏、旧媒体背景粘连、重复回复的重试循环。
  - 随机回复 mode JSON 解析与 `no_send` / `ambient_water` 发送决策。
  - persistent thinking / internal context leak / banned output / repeated output 的 persona fallback。
  - 罕见触发前缀、嘴臭去图标记、语义画像拦截等最终输出后处理。
- `chat.js` 保留模型调用与 conversation 保存所有权，通过 `callModel: callOpenAI` 注入模型调用，并在 `finalizeChatReply()` 返回后自行决定是否 `saveConversationTurn()`。
- 新模块不发送消息、不保存 conversation、不反向 import `chat.js`。

### 2. 实际执行步骤

1. 新建 `chat-final-output-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将 `MAX_REPLY_RETRIES` 循环及其依赖的 retry prompt、old media sticking、repeat guard、random mode、persona fallback 逻辑迁入新模块。
3. `chat.js` 删除对 `MAX_REPLY_RETRIES`、`BANNED_ACTION_OUTPUT_RE`、`THINKING_OUTPUT_RE`、`parseRandomReplyDecision()`、`buildRepeatRetryPrompt()`、`buildOldMediaStickingRetryPrompt()`、`shouldRetryRepeatedReply()` 等最终输出守卫实现细节的直接依赖。
4. `chat.js` 在 vision blindness retry 与 jailbreak output 检查之后调用 `finalizeChatReply()`，再根据 `shouldSend` 决定随机回复是否静默，最后仍由 `chat.js` 保存最终对话。
5. 修复迁移中误清理的 import：`chat.js` 的连续复读早退仍需要 `pickRepeatedFallbackReply()`，因此保留该导入；同时清理已迁出的 `estimateTokens`、`classifySceneItemsForActive`、`ABUSIVE_INPUT_RE` 等未使用导入。
6. `cascade-test.js` 新增 `chatFinalOutputFlow` 模块加载、导出、语法检查、重复函数扫描。
7. `cascade-test.js` 新增 cross-file guard，确保 `chat.js` 只 import `chat-final-output-flow`，不再内联最终输出 retry guard，并确保新模块不 import `chat.js`、不 `session.send`、不 `saveConversationTurn()`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat-final-output-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2432`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 198 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 reasoning-only、thinking leak、tool plan leak、internal prompt leak、forward wrapper leak、persistent thinking leak、banned/repeated fallback、random reply、文件/图片追问、reminder fallback、uploaded-file fallback 等 scenario 覆盖原最终输出守卫所在主链路。
2. 断言的失败现象：最终输出守卫迁出后，thinking/tool plan/internal wrapper 不会原样发出，随机 mode JSON 不会泄漏到群里，重复/违禁输出仍会重试或 fallback，`no_send` 随机回复仍会静默，conversation 仍只保存最终可见回复。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部模型服务在生产账号下的长时间输出分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `chat.js` 从上一刀后约 60,585 bytes / 1,045 行，降到约 51,004 bytes / 898 行。
- 新增 `chat-final-output-flow.js` 约 11,376 bytes / 252 行，承接最终输出重试、随机 mode 解析与兜底回复所有权。
- `index.js` 当前约 55,934 bytes / 1,093 行。

### 5. 本节结论

- `chat.js` 已不再内联最终输出重试循环，主流程后半段进一步收敛为“工具流 -> memory prompt -> tool_call 文本剥离 -> vision blindness retry -> jailbreak output -> final output flow -> 保存”。
- 这一刀没有改变用户可见回复策略，也没有改变模型调用所有权或 conversation 保存所有权。
- 下一步更适合继续拆 `vision blindness retry` 或 `Agent retell / jailbreak` 这类仍在 `chat.js` 内部但边界清晰的剩余职责。

## 二十九、2026-05-25 `chat-jailbreak-flow.js` 越狱拦截流执行归档

> 本节记录 `chat.js` 输入越狱回复与上下文越狱检测职责的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `chat.js`、`chat-jailbreak-flow.js`、`utils.js`、`conversation.js`、`runtime-config.js`、`api.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/chat-jailbreak-flow.js`。
- 从 `chat.js` 拆出：
  - `isContextJailbroken()`：基于最近 assistant 回复的强/弱特征检测上下文软越狱。
  - `chatJailbreak()`：对输入越狱或 Agent 材料越狱进行轻量模型回绝，失败时使用既有 fallback。
- `chat.js` 保留触发时机、conversation 清理、Agent context 清理、保存回复的所有权。
- 新模块不发送消息、不保存 conversation、不反向 import `chat.js`。

### 2. 实际执行步骤

1. 新建 `chat-jailbreak-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将上下文越狱检测依赖的 `CONTEXT_JAILBREAK_STRONG_RE` / `CONTEXT_JAILBREAK_WEAK_RE` 和 `getRecentAssistantReplies()` 迁入新模块。
3. 将越狱回绝模型调用迁入新模块，仍复用 `loadConfig()` 与 `requestChatCompletions()`，保持 `_fallbackSet: 'lightweight'` 与 `max_tokens: 60` 不变。
4. `chat.js` 改为从 `chat-jailbreak-flow.js` 导入 `isContextJailbroken()` 与 `chatJailbreak()`。
5. 清理 `chat.js` 中迁移后不再需要的 `CONTEXT_JAILBREAK_*` 与 `pickJailbreakFallbackReply` 导入；保留 `JAILBREAK_OUTPUT_RE`，因为 Agent retell 和最终输出前仍需要检测。
6. `cascade-test.js` 新增 `chatJailbreakFlow` 模块加载、导出、语法检查、重复函数扫描。
7. `cascade-test.js` 新增 cross-file guard，确保 `chat.js` 不再内联 `isContextJailbroken()` / `chatJailbreak()`，并确保新模块不 import `chat.js`、不 `session.send`、不 `saveConversationTurn()`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat-jailbreak-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2438`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 199 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 persona jailbreak、Agent retell leak fallback、internal prompt leak、persistent thinking fallback、normal chat 等 scenario 覆盖原越狱拦截所在主链路。
2. 断言的失败现象：越狱拦截迁出后，输入 prompt injection 仍走专用回绝，历史上下文软越狱仍会触发清历史，Agent 材料中的越狱内容仍不会原样污染正常 chat。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部模型服务对越狱回绝的生产输出分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `chat.js` 从上一刀后约 51,004 bytes / 898 行，降到约 48,837 bytes / 862 行。
- 新增 `chat-jailbreak-flow.js` 约 2,862 bytes / 63 行，承接输入越狱回复与上下文越狱检测所有权。

### 5. 本节结论

- `chat.js` 已不再内联越狱回绝模型调用和上下文越狱检测，主流程仍保留“何时拦截、何时清历史、何时保存”的编排所有权。
- 这一刀没有改变用户可见越狱回绝策略，也没有改变模型调用参数、fallback 文案或 conversation 清理顺序。
- 下一步如果继续拆 `chat.js`，优先候选是 `Agent retell` 或 `topic-switch`；但两者都比本节更贴近主编排，需要继续按纯搬运和小验证推进。

## 三十、2026-05-25 `chat-topic-switch.js` 话题切换检测执行归档

> 本节记录 `chat.js` 话题切换轻量模型判断与 per-key 串行锁的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `chat.js`、`chat-topic-switch.js`、`conversation.js`、`runtime-config.js`、`api.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/chat-topic-switch.js`。
- 从 `chat.js` 拆出：
  - `topicSwitchLocks` per conversation key 串行锁。
  - `detectTopicSwitch()` 轻量模型 YES/NO 判断。
  - `resolveTopicSwitch()`：读取最近一条用户消息并在锁内返回 `true / false / null`。
  - `clearTopicSwitchLocks()`：测试/未来生命周期清理入口。
- `chat.js` 保留话题切换后的副作用所有权：`clearUserConversationHistory()` 与 `clearAgentContextForUser()` 仍在主流程执行。
- 新模块不清理 conversation、不清理 Agent context、不发送消息、不保存 conversation、不反向 import `chat.js`。

### 2. 实际执行步骤

1. 新建 `chat-topic-switch.js`，添加 MODULE 注释，明确职责、边界和 `topicSwitchLocks` 状态说明。
2. 将 `detectTopicSwitch()` 从 `chat.js` 迁入新模块，保持 prompt、`max_tokens: 5`、`_fallbackSet: 'lightweight'`、`_timeoutMs: 8000` 和 `YES/NO/null` 语义不变。
3. 将 `topicSwitchLocks` 迁入新模块，并用 `resolveTopicSwitch()` 封装“读取最近用户消息 -> 串行判断 -> 自动释放锁”的流程。
4. `chat.js` 改为调用 `resolveTopicSwitch({ topicKey, session, currentText: cleanInput })`，并只在返回 `true` 时执行清历史和清 Agent context。
5. 清理 `chat.js` 中迁移后不再需要的 `getRecentUserMessages` 导入、本地 `detectTopicSwitch()` 和 `topicSwitchLocks`。
6. `cascade-test.js` 新增 `chatTopicSwitch` 模块加载、导出、语法检查、重复函数扫描。
7. `cascade-test.js` 新增 cross-file guard，确保 `chat.js` 不再持有 topic lock 或直接读取最近用户消息，并确保新模块不 import `chat.js`、不清理 conversation/Agent context、不发送、不保存。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat-topic-switch.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2445`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 200 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 explicit at 当前话题、explicit at correction、private refinement context、normal chat、legacy history、Agent bridge 等 scenario 覆盖话题切换和历史降级相关主链路。
2. 断言的失败现象：话题切换检测迁出后，检测失败仍会把旧历史降级为背景，检测为切换时仍由 `chat.js` 清用户历史与 Agent context，当前显式交互仍不会被旧话题抢走。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部模型服务对话题切换 YES/NO 的生产输出分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `chat.js` 从上一刀后约 48,837 bytes / 862 行，降到约 47,503 bytes / 836 行。
- 新增 `chat-topic-switch.js` 约 2,131 bytes / 48 行，承接话题切换判断和 per-key 串行锁所有权。
- `index.js` 当前约 55,934 bytes / 1,093 行。

### 5. 本节结论

- `chat.js` 已不再持有话题切换锁和轻量模型检测实现，主流程仍保留清理 conversation 与 Agent context 的显式副作用。
- 这一刀没有改变话题切换判断 prompt、模型参数、降级规则或历史清理顺序。
- `chat.js` 已降到 50 KB 以下，后续更适合先复扫剩余大块职责；若继续拆，优先候选是 `Agent retell` 或 `vision blindness retry`，但必须继续保持模型调用所有权和保存所有权清晰。

## 三十一、2026-05-25 `chat-agent-retell-flow.js` Agent 转述流执行归档

> 本节记录 `chat.js` 中 Agent 内部材料转述职责的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `chat.js`、`chat-agent-retell-flow.js`、`agent-retell-guard.js`、`persona-fallback.js`、`chat-jailbreak-flow.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/chat-agent-retell-flow.js`。
- 从 `chat.js` 拆出：
  - Agent 材料脱敏与 2000 字上限裁剪。
  - Agent 材料越狱检测与 `chatJailbreak()` 回绝。
  - Agent 内部材料转述 prompt、当前时间注入与敏感频道固定回复规则。
  - Agent 转述输出中的 banned output、thinking/tool plan、内部上下文泄漏二次重试。
  - Agent 转述最终 persona fallback 与语义画像拦截。
  - `getAgentReplyMaxChars()` 最大回复长度选择。
- `chat.js` 保留触发时机、模型调用所有权和 `saveConversationTurn()` 所有权，通过 `callModel: callOpenAI` 注入模型调用。
- 新模块不发送消息、不保存 conversation、不反向 import `chat.js`。

### 2. 实际执行步骤

1. 新建 `chat-agent-retell-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将原 `options.isAgentResult && options.agentResultText` 分支中的 Agent 材料转述逻辑迁入新模块，保持内部材料提示词、敏感政治固定回复、越狱输出检查、二次重试和 fallback 语义不变。
3. `chat.js` 改为调用 `retellAgentResultForChat({ ... callModel: callOpenAI })`，然后仍由 `chat.js` 保存当前对话并返回。
4. 清理 `chat.js` 中迁移后不再需要的 `MAX_OUTPUT_CHARS_YINYANG` 导入；保留连续复读早退仍需要的 `MAX_OUTPUT_CHARS_FRIENDLY`、`MAX_OUTPUT_CHARS_ABUSIVE` 和 `trimReply()`。
5. `cascade-test.js` 新增 `chatAgentRetellFlow` 模块加载、导出、语法检查、重复函数扫描。
6. `cascade-test.js` 新增 cross-file guard，确保 `chat.js` 不再内联 Agent 材料脱敏/转述 prompt，也确保新模块不 import `chat.js`、不 `session.send`、不 `saveConversationTurn()`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat-agent-retell-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2451`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 201 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 heavy web_search / web_fetch retell、time-sensitive search retell、explicit URL fetch、Agent retell leak fallback、Agent file wrapper retry、simulated e2e mocked private/group search 等 scenario 覆盖原 Agent 转述主链路。
2. 断言的失败现象：Agent 转述迁出后，内部工具材料仍不会原样发群，工具名/markdown/内部 prompt/密钥样材料仍会被清洗或 fallback，Agent 材料里的越狱指令仍不会污染 chat 人格。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部搜索、真实网页抓取和真实模型服务在生产账号下的转述输出分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `chat.js` 从上一刀后约 47,503 bytes / 836 行，降到约 43,189 bytes / 795 行。
- 新增 `chat-agent-retell-flow.js` 约 5,491 bytes / 112 行，承接 Agent 材料转述、输出守卫和 fallback 所有权。
- `index.js` 当前约 55,934 bytes / 1,093 行。

### 5. 本节结论

- `chat.js` 已不再内联 Agent 内部材料转述 prompt 和守卫细节，主流程保留“何时转述、用哪个模型调用、何时保存”的编排职责。
- 这一刀没有改变 Agent 搜索/抓取结果的用户可见转述策略，也没有改变模型调用入口、敏感话题固定回复规则或 conversation 保存顺序。
- `chat.js` 当前已低于 45 KB；后续应优先复扫 `index.js` 的随机状态/发送/入口编排剩余债务，`chat.js` 只在确认有清晰独立职责时继续拆。

## 三十二、2026-05-25 `chat-result-flow.js` chat heavy tool / Agent 转述桥接执行归档

> 本节记录 `index.js` 中 chat 结果后处理、heavy tool 升级 Agent、Agent 结果转述桥接职责的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `index.js`、`chat-result-flow.js`、`agent/router.js`、`agent/config.js`、`agent-chat-bridge.js`、`agent-retell-guard.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/chat-result-flow.js`。
- 从 `index.js` 拆出：
  - `normalizeChatResultText()`。
  - `retellToolBlockedReply()`。
  - `retellAgentResult()`。
  - `handleChatResult()` 中 heavy `web_search` / `web_fetch` 请求提取、预执行 Agent tool、Agent 结果转述和失败兜底。
- `index.js` 保留中间件编排、发送所有权、队列入口所有权；通过参数注入 `chat`、`agentEngine`、`enqueueAgentTask`、`configureAgentQueue`。
- 新模块不发送消息、不注册 middleware、不拥有 apply 生命周期、不反向 import `index.js`。

### 2. 实际执行步骤

1. 新建 `chat-result-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将 `AGENT_RETELL_FALLBACK`、chat result 文本归一、工具阻止后人格化转述、Agent 结果脱敏/转述/记录、heavy tool 升级 Agent 逻辑迁入新模块。
3. `index.js` 改为从 `chat-result-flow.js` 导入 `handleChatResult()` 与 `retellAgentResult()`。
4. `index.js` 的延迟随机 heavy tool 路径、正常 chat heavy tool 路径、自动 Agent 路径分别通过参数注入所需依赖，避免新模块 import 入口。
5. 清理 `index.js` 中迁移后不再需要的 `buildExplicitUrlFetchRunOptions`、`isExecutableSearchQuery`、`externalToolsDenied`、`isAgentToolEnabled`、`recordAgentChatResult`、`summarizeAgentToolResults`、`guardAgentRetellReply`、`redactAgentMaterial` 等导入。
6. `cascade-test.js` 新增 `chatResultFlow` 模块加载、导出、语法检查、重复函数扫描。
7. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再内联 chat result bridge，且新模块不 import `index.js`、不发送消息、不注册 middleware、不拥有 `exports.apply`。
8. 修正已有“queued agent paths resolve current bot”守卫：入口直达 Agent 路径继续检查 `bot: resolveBot()`，heavy tool 路径改为检查 `chat-result-flow.js` 内部通过 `createBotResolver(ctx, session)` 和 `bot: getBot()` 解析当前 bot。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat-result-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2459`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 202 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 explicit web_search、heavy web_search / web_fetch、blocked heavy web_search、time-sensitive free search、explicit URL fetch、QQ Agent direct、simulated e2e mocked private/group search 等 scenario 覆盖原 chat result bridge 主链路。
2. 断言的失败现象：heavy tool 升级迁出后，搜索/抓取请求仍会预执行 Agent 工具，工具过程仍不会发进度消息，Agent 报告仍不会原样发群，失败和队列满仍返回原有安全文案。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部搜索、真实网页抓取、真实 Agent 长时间队列压力和生产模型输出分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 55,934 bytes / 1,093 行，降到约 48,613 bytes / 998 行。
- 新增 `chat-result-flow.js` 约 8,521 bytes / 176 行，承接 chat heavy tool / Agent 转述桥接所有权。
- `chat.js` 当前约 43,189 bytes / 795 行。

### 5. 本节结论

- `index.js` 已不再内联 chat result bridge 和 heavy tool Agent 升级细节，入口只负责触发位置、依赖注入和最终发送。
- 这一刀没有改变 Agent 路由策略、工具启用判断、预执行工具数量、Agent queue 参数、失败兜底文案或最终发送策略。
- 两个主文件当前都已低于 50 KB；剩余最明显的入口债务是随机回复状态与 freshness/pending timer 状态仍在 `index.js`，需要继续按 P4 的状态模块边界谨慎拆分。

## 三十三、2026-05-25 `random-state.js` 随机回复状态执行归档

> 本节记录 `index.js` 中随机回复状态 Map、pending timer 管理与 freshness 判断的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `index.js`、`random-state.js`、`safe-send.js`、`handler.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/random-state.js`。
- 从 `index.js` 拆出：
  - `channelMissCount` 随机 miss 计数状态及读写 helper。
  - 随机回复冷却、闭嘴静默、pending timer、频道消息版本、显式交互版本 Map。
  - `buildRandomSendOptions()`、`isRandomReplyFresh()`、`isSafeSendReplyFresh()` freshness 相关判断。
  - pending 随机回复的 set/take/cancel/clear 生命周期 helper。
- `index.js` 保留随机回复触发条件、延迟随机执行体、chat/Agent 调用、最终发送与日志所有权。
- 新模块不调用 chat/Agent、不发送消息、不读取运行时配置、不反向 import `index.js`；基础概率由 `index.js` 通过 `resolveRandomTriggerRate()` 注入。

### 2. 实际执行步骤

1. 新建 `random-state.js`，添加 MODULE 注释，明确职责、边界和 Map 状态说明。
2. 将 `channelMissCount`、`channelMutedUntil`、`lastRandomReplyTs`、`channelPendingRandom`、`channelMessageVersions`、`channelExplicitVersions` 与 `MAX_RANDOM_CHANNEL_STATE_ENTRIES` 迁入新模块。
3. 在 `random-state.js` 中保留原随机 miss、冷却、闭嘴、版本递增、pending 取消、freshness 检查语义。
4. `index.js` 改为从 `random-state.js` 导入随机状态 helper，并把原 `getRandomTriggerRate()` 包装改名为 `resolveRandomTriggerRate()`，避免与新模块导出重名。
5. 保留 `channelMissCount` 的 Map 导出，继续传给 `handleCommand()`，兼容当前命令处理链路。
6. `safeSendReply()` 继续通过注入的 `isSafeSendReplyFresh()` 做随机 freshness 二次确认，发送实现不反向依赖入口。
7. `cascade-test.js` 新增 `randomState` 模块加载、函数导出、`channelMissCount` Map 类型断言、语法检查、重复函数扫描。
8. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再内联随机状态 Map / `buildRandomSendOptions()`，并确保 `random-state.js` 不 import `index.js`、不发送、不注册 middleware、不拥有 `exports.apply`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/random-state.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2487`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 203 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过随机回复、显式 @、图片/文件追问、Agent/chat 主链路、safe send 与命令重载等 scenario 覆盖原随机状态所在链路。
2. 断言的失败现象：随机状态迁出后，显式交互仍会取消旧 pending 随机，频道新消息仍会让旧随机过期，`ambient_water` / 随机回复仍受 freshness 保护，AI 重载仍能清理 miss count。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数、Map 类型和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖生产长时间运行中的随机 pending timer 分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 48,613 bytes / 998 行，降到约 45,243 bytes / 931 行。
- 新增 `random-state.js` 约 6,963 bytes / 189 行，承接随机状态、pending timer 与 freshness 判断所有权。
- `chat.js` 当前约 43,189 bytes / 795 行。

### 5. 本节结论

- `index.js` 已不再持有随机回复 Map 状态和 freshness 判断实现，入口只保留随机触发、延迟执行体、依赖注入和最终发送编排。
- 这一刀没有改变随机概率、warmup/ramp、闭嘴时长、冷却时长、pending 取消、过期判断或发送前 freshness 二次校验策略。
- 两个主文件当前都已低于 50 KB；后续需要重新按当前代码复扫剩余债务，优先判断是否继续拆 `index.js` 中仍有独立所有权的调度/文件读取/诊断小块，还是将本轮架构债优化收束为“主干降到 50 KB 以下且边界守卫已落地”。

## 三十四、2026-05-25 `incoming-file.js` 入站小文件缓存执行归档

> 本节记录 `index.js` 中入站小文件后台缓存 helper 的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `index.js`、`incoming-file.js`、`file-store.js`、`file-analyzer.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/incoming-file.js`。
- 从 `index.js` 拆出 `cacheSmallFileBackground()`：
  - 下载小文件 URL 到 `FILE_CACHE_DIR`。
  - 调用 `setLocalPath()` 回写文件本地路径。
  - 裁剪每频道本地缓存文件数量到 10 个。
  - 保留后台 promise 链 `.catch(() => {})`，不影响主消息处理。
- `index.js` 保留文件 segment 识别、文件安全判断、文件元数据入库、`plain` 注入与是否触发后台缓存的所有权。
- 新模块不解析消息、不发送消息、不注册 middleware、不调用 chat/Agent、不反向 import `index.js`。

### 2. 实际执行步骤

1. 新建 `incoming-file.js`，添加 MODULE 注释，明确职责、边界和无模块级状态说明。
2. 将 `cacheSmallFileBackground()` 从 `index.js` 迁入新模块，保留原安全 channel / messageId 文件名清洗、下载、`setLocalPath()`、缓存裁剪和吞掉后台错误的行为。
3. `index.js` 改为从 `./incoming-file` 导入 `cacheSmallFileBackground()`。
4. `index.js` 对 `file-store` 的直接导入收窄为 `storeFile`，不再直接导入 `setLocalPath` / `cacheFileLocally`。
5. `cascade-test.js` 新增 `incomingFile` 模块加载、导出、语法检查和重复函数扫描。
6. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再内联 `cacheSmallFileBackground()`，并确保 `incoming-file.js` 不 import `index.js`、不发送、不注册 middleware、不调用 chat。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/incoming-file.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2492`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 204 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过私聊纯文件、群聊空文件、CQ content-only 文件、Koishi elements 文件、mixed segment 文件、文件追问和上传文件变体等 scenario 覆盖原入站文件主链路。
2. 断言的失败现象：小文件缓存 helper 迁出后，文件元数据仍能入库，纯文件消息仍保持静默，文件锚点仍进入 active scene，文件追问仍能读取最近文件，后台缓存失败仍不会打断主流程。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session、临时 `DATA_DIR`，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实文件 URL 在生产网络下的后台下载成功率；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 45,243 bytes / 931 行，降到约 44,152 bytes / 909 行。
- 新增 `incoming-file.js` 约 1,578 bytes / 38 行，承接入站小文件后台缓存 helper。
- `chat.js` 当前约 43,189 bytes / 795 行。

### 5. 本节结论

- `index.js` 已不再持有小文件后台缓存下载与本地路径回写 helper，入口只保留文件入站编排。
- 这一刀没有改变文件安全策略、文件元数据形状、文件追问策略、后台缓存上限或错误吞掉策略。
- 当前两个主文件均稳定低于 50 KB；继续拆分的剩余候选主要是 `index.js` 的延迟随机执行体、Agent 自动路由桥接、今日缓存恢复与共享记录文本等更贴近编排的区域，需要先复扫风险再决定是否继续。

## 三十五、2026-05-25 `shared-record-text.js` / `random-persona-risk.js` 纯 helper 执行归档

> 本节记录 `index.js` 中群共享记录文本归一和随机人格切换风险判断两个纯 helper 的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `index.js`、`shared-record-text.js`、`random-persona-risk.js`、`persona.js`、`message-reader.js`、`utils.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/shared-record-text.js`。
- 从 `index.js` 拆出 `resolveSharedRecordText()`：
  - 优先从入站 `plain` / `analyzed.memory` / `analyzed.plain` 中归一保存文本。
  - 保留语音、文件、消息记录 cue 的原有占位文本策略。
  - 只做文本归一，不写 conversation、不发送消息、不读写文件。
- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/random-persona-risk.js`。
- 从 `index.js` 拆出 `getGroupPersonaName()` 与 `isPersonaSwitchRisky()`：
  - 查询群级人格名。
  - 判断随机回复是否处于“用户人格覆盖群人格”的高风险状态。
  - 只做同步判断，不决定是否触发随机、不决定是否发送、不修改人格配置。

### 2. 实际执行步骤

1. 新建 `shared-record-text.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将 `resolveSharedRecordText()` 迁入新模块，保留 `normalizeText(stripMentions(...))` 与 `[语音]` / `[文件]` 占位策略。
3. 新建 `random-persona-risk.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
4. 将 `getGroupPersonaName()`、`isPersonaSwitchRisky()` 迁入新模块，保留对 `getGroupPersona(channelKey)` 和 `personaResolution.source === 'user'` 的原判断。
5. `index.js` 改为导入两个新模块，并移除不再需要的 `getGroupPersona` 直接导入。
6. `cascade-test.js` 新增两个模块的 `modPaths`、`expectedExports`、`syntaxFiles`、`duplicateScanFiles`。
7. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再内联三个 helper，并确保两个新模块不 import `index.js`、不发送、不注册 middleware、不拥有 `exports.apply`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/shared-record-text.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/random-persona-risk.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2503`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 206 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过群共享上下文、图片/文件/语音锚点、随机回复、人格绑定与主聊天场景测试覆盖原 helper 所在链路。
2. 断言的失败现象：helper 迁出后，群共享记录仍不会丢失语音/文件占位，随机回复在用户人格覆盖群人格时仍保持原高风险引用策略。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖生产长时间群聊中人格切换与随机回复的真实分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 44,152 bytes，降到约 43,582 bytes。
- `chat.js` 保持约 43,189 bytes。
- 新增 `shared-record-text.js` 约 748 bytes，承接群共享记录文本归一 helper。
- 新增 `random-persona-risk.js` 约 796 bytes，承接随机回复人格切换风险 helper。

### 5. 本节结论

- `index.js` 已不再持有群共享记录文本归一和随机人格切换风险判断细节，入口只保留调用位置和结果使用。
- 这一刀没有改变共享记录保存文本、语音/文件占位、人格解析来源判断或随机回复发送策略。
- 当前两个主文件均稳定低于 50 KB，剩余入口内的 `lastEmotionCache`、`restoreTodayCacheEntry()`、`safeSendReplyWithFreshness()` 与 `exports.apply()` 更像入口编排/缓存桥接；继续物理拆分需要另行评估收益和风险。

## 三十六、2026-05-25 主干收束完成态复核归档

> 本节记录本轮架构债优化的完成态复核。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并按工作目录规则复核了 `git rev-parse --show-toplevel`、当前分支、`git status --short --branch`、`HEAD` 与 `origin/bot_ZZY`。

### 1. 完成态结论

- `index.js` 当前约 43,582 bytes，已低于 50 KB 目标线。
- `chat.js` 当前约 43,189 bytes，已低于 50 KB 目标线。
- 当前未发现生产模块反向 import `index.js`；相关边界由 `cascade-test.js` 的 cross-file guard 持续约束。
- 本轮没有部署、没有重启、没有推送。

### 2. 旧计划名与实际落地模块对齐

- `session-compat.js`：承接 Session / Satori 兼容补丁。
- `bot-resolver.js`：承接当前 bot 解析与异步 session 注入。
- `channel-task-queue.js`：承接频道串行队列状态。
- `event-dump.js`：承接一次性原始事件抓取与安全落盘。
- `startup-schedulers.js`：承接启动期 daily cleanup / expression harvest 定时器。
- `diagnostics.js`：承接入口旁路诊断日志。
- `message-segment.js`：承接图片/文件 segment 解析。
- `runtime-settings.js`：承接随机白名单、概率与运行时设置缓存。
- `user-blacklist.js`：承接用户黑名单缓存读取。
- `safe-send.js`：承接发送失败、限流、禁言、复读和罕见语音安全发送。
- 计划中的 `chat-skills.js` 实际落地为 `skills-loader.js`：承接 skill 文件加载、缓存刷新和基础 system prompt。
- 计划中的 `chat-output-flow.js` 实际落地为 `chat-final-output-flow.js`：承接最终输出守卫、fallback 和重试。
- `chat-tool-flow.js`：承接 chat 内置工具调用循环。
- `chat-jailbreak-flow.js`：承接上下文越狱检测与 jailbreak 专用回复。
- `chat-topic-switch.js`：承接话题切换检测和 per-key 锁。
- 计划中的 `chat-agent-retell.js` 实际落地为 `chat-agent-retell-flow.js`：承接 Agent 材料转述和泄漏防线。
- `chat-result-flow.js`：承接 chat heavy tool 升级 Agent 与 Agent 转述桥接。
- `random-state.js`：承接随机回复 Map 状态、pending timer 和 freshness 判断。
- `incoming-file.js`：承接入站小文件后台缓存。
- `shared-record-text.js`：承接群共享记录文本归一。
- `random-persona-risk.js`：承接随机人格切换风险判断。

### 3. 保留在主干内的职责说明

- `index.js` 保留 `exports.apply()`、中间件编排、显式交互版本触发点、随机回复执行体、文件/图片/语音入站编排、Agent/chat 调用位置和最终发送调用，这是入口模块的责任边界。
- `lastEmotionCache` 仍留在 `index.js`，因为它紧贴敏感/情绪处理路径，暂未形成足够独立的状态模块收益。
- `restoreTodayCacheEntry()` 仍留在 `index.js`，因为它只服务启动期 today cache 恢复，当前体量小、迁出收益低。
- `safeSendReplyWithFreshness()` 仍留在 `index.js`，因为它是入口对 `safe-send.js` 与 `random-state.js` 的注入式桥接，继续迁出容易让发送层反向感知入口。
- `chat.js` 保留 `chat()` 主循环和 `callOpenAI()` 模型调用所有权，符合“聊天核心链路保留模型调用所有权”的协作规则。
- `chat.js` 中 `hostileLevelCache` / `trimRuntimeCaches()` 暂留，属于 chat 内部短缓存；若后续继续拆，应独立评估为 `chat-runtime-state.js`，不作为本轮主干收束的阻塞项。

### 4. 后续不默认继续拆的候选

- 延迟随机执行体：仍靠入口拿到 live session、freshness、chat/Agent、发送策略；继续拆会触碰时序和取消语义，收益低于风险。
- Agent 自动路由桥接：当前依赖入口上下文、队列和最终发送，已通过 `chat-result-flow.js` 收掉 heavy tool 细节；剩余部分更像入口编排。
- 今日缓存恢复和情绪缓存：体量小且贴近启动/敏感链路，不建议为追求行数继续拆。
- `chat()` 内部剩余大块：已低于目标线，模型调用和 conversation 保存所有权清楚；除非出现新行为需求或明确独立状态，否则不继续物理拆分。

### 5. 最终验收口径

- 架构目标：`index.js` / `chat.js` 主干均降至 50 KB 以下，已完成。
- 边界目标：新增模块均有 MODULE 注释，新增生产模块已进入 cascade 的导出、语法、重复函数和边界守卫，已完成。
- 行为目标：本轮拆分为结构等价迁移，不改变随机回复、Agent 路由、文件/图片/语音处理、敏感词、工具调用和发送策略。
- 验证目标：文档追加后已完成最终收尾验证：
  - `node -c packages/koishi-plugin-dongxuelian-ai/lib/shared-record-text.js`：通过。
  - `node -c packages/koishi-plugin-dongxuelian-ai/lib/random-persona-risk.js`：通过。
  - `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
  - `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
  - `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
  - `npm run test:quick`：通过，`passed: 2503`、`failed: 0`、`skipped: 1`。
  - `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
  - `npm run check`：通过，检查 206 个文件。
  - `git diff --check`：通过。

## 三十七、2026-05-25 `plugin-lifecycle.js` 生命周期执行归档

> 本节记录 `index.js` 中 ready / dispose 生命周期、启动期缓存恢复与敏感扫描 timer 的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并按工作目录规则复核了 `git rev-parse --show-toplevel`、当前分支、`git status --short --branch`、`HEAD` 与 `origin/bot_ZZY`。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/plugin-lifecycle.js`。
- 从 `index.js` 拆出：
  - `ctx.on('ready')` 启动初始化。
  - thinking / runtime settings / skill / persona / repeat / random voice 缓存加载。
  - 今日情绪磁盘缓存恢复。
  - daily stats cleanup、expression harvest 调度入口。
  - Agent cron scheduler 恢复。
  - 每 30 分钟敏感话题扫描 timer。
  - `ctx.on('dispose')` 中 sensitive timer、Agent cron、频道队列、随机 pending、startup scheduler 清理。
- `index.js` 保留 `exports.apply()`、消息 middleware 注册和聊天/随机/Agent 主流程，只用一行 `registerPluginLifecycle(ctx, { agentEngine, configureAgentQueue })` 接线。
- 新模块不注册消息 middleware、不发送消息、不处理聊天/随机/Agent 路由决策、不反向 import `index.js`。

### 2. 实际执行步骤

1. 新建 `plugin-lifecycle.js`，添加 MODULE 注释，明确职责、边界和 `sensitiveTimer` 状态说明。
2. 将 `restoreTodayCacheEntry()`、今日缓存扫描恢复、ready 初始化、敏感扫描 timer、dispose 清理迁入新模块。
3. `index.js` 改为导入 `registerPluginLifecycle()`，并删除 lifecycle 迁出后不再需要的 `path`、`loadStickerCache`、`loadRepeatConfig`、若干旧 constants 直接导入。
4. 首轮 `npm run test:scenario` 暴露 `channelTodayCache is not defined`：原因是 `handleCommand()` 仍需要 `channelTodayCache` 注入，而该缓存不属于 lifecycle 独占依赖；已把 `channelTodayCache` import 补回 `index.js`。
5. `cascade-test.js` 新增 `pluginLifecycle` 模块加载、导出、语法检查和重复函数扫描。
6. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再内联 ready / sensitive timer / today cache restore，并确保 lifecycle 模块不 import `index.js`、不注册 middleware、不发送消息。
7. 按后台 explorer 建议补强 `random-state.js` 守卫：显式检查 `channelMutedUntil`、`lastRandomReplyTs`、`channelPendingRandom`、`channelMessageVersions`、`channelExplicitVersions` 和 `clearRandomPendingState()` 仍归属 `random-state.js`，避免以后换名重新内联回 `index.js`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/plugin-lifecycle.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2510`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 207 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 command middleware、AI reload、today emotion、sensitive detection、agent cron / reminder、random reply、file history、simulated e2e Agent chain 等 scenario 覆盖 apply 生命周期和中间件注册后的主链路。
2. 断言的失败现象：生命周期迁出后，插件仍能完成初始化、命令仍能拿到 today cache、敏感扫描和 dispose 清理仍有所有权，`index.js` 不再内联 ready/dispose 细节，随机状态不会被重新塞回入口。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖生产长时间运行下 daily cleanup / expression harvest / sensitive timer 的真实时间触发；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 43,582 bytes，降到约 40,015 bytes。
- `chat.js` 保持约 43,189 bytes。
- 新增 `plugin-lifecycle.js` 约 4,573 bytes，承接插件生命周期、启动恢复和 dispose 清理。

### 5. 本节结论

- `index.js` 已不再持有启动初始化和 dispose 清理实现，`exports.apply()` 的生命周期部分收成一行接线。
- 这一刀没有改变 ready 初始化顺序、today cache 恢复规则、敏感扫描间隔、startup scheduler 调度、Agent cron 恢复或 dispose 清理顺序。
- 后续继续收薄入口时，优先拆 `exports.apply()` 内仍可独立成段的文件快捷读取、Agent 自动路由和最终发送流水段；随机延迟执行体仍属于更高风险切口，需要继续谨慎处理。

## 三十八、2026-05-25 `file-quick-read.js` 文件快捷读取执行归档

> 本节记录 `index.js` 中“读文件/看文件/分析文件/打开文件/文件内容”显式快捷读取早退分支的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并复核了 `index.js`、`file-store.js`、`file-analyzer.js`、`file-safety.js` 与 `cascade-test.js` 的当前状态。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/file-quick-read.js`。
- 从 `index.js` 拆出：
  - `isFileQuickReadIntent()`：识别显式读文件快捷意图。
  - `resolveFileQuickReadReply()`：查找最近未跳过文件，优先分析未分析文件，其次复用已有分析，生成自然摘要或失败文案。
- `index.js` 保留发送所有权：新模块只返回可发送文本，入口仍通过 `safeSendReplyWithFreshness()` 发送。
- 新模块不发送消息、不注册 middleware、不调用 chat/Agent、不反向 import `index.js`。

### 2. 实际执行步骤

1. 新建 `file-quick-read.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将原内联 `getRecentFiles(channelKey, 10)`、未分析优先选择、`analyzeFileNow()`、`summarizeFileContentForChat()` 和三种返回文案迁入新模块。
3. `index.js` 改为导入 `isFileQuickReadIntent()` 与 `resolveFileQuickReadReply()`。
4. `index.js` 的快捷读取分支保持原早退位置不变，只负责拿 live session、调用 helper、统一安全发送。
5. `cascade-test.js` 新增 `fileQuickRead` 模块加载、导出、语法检查和重复函数扫描。
6. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再直接 import `file-analyzer` / 内联快捷读取细节，并确保新模块不 import `index.js`、不发送、不注册 middleware、不调用 chat/Agent。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/file-quick-read.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2516`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 208 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 `@bot 读文件`、文件追问、自然读文件、禁用 analyze_file、上传文件变体等 scenario 覆盖原快捷读取和文件工具链路。
2. 断言的失败现象：快捷读取迁出后，仍会读取最近文件真实内容，仍返回自然摘要，不泄漏 `[用户上传文件:]` wrapper / `---文件内容开始---` 标记，找不到文件和下载失败仍返回原文案。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session、临时 data dir，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖生产网络文件 URL 下载成功率；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 40,015 bytes，降到约 38,969 bytes。
- 新增 `file-quick-read.js` 约 1,590 bytes，承接显式文件快捷读取 helper。
- `chat.js` 保持约 43,189 bytes。

### 5. 本节结论

- `index.js` 已不再直接持有最近文件选择、即时分析和文件内容自然摘要细节，入口只保留意图命中后的发送编排。
- 这一刀没有改变文件快捷读取命令词、文件选择优先级、分析失败文案、自然摘要格式或发送 freshness 策略。
- 入口 `exports.apply()` 仍较长，下一候选是 Agent 自动路由和最终发送流水段；这两个切口触碰队列、模型调用和发送，因此需要继续按场景测试保护谨慎拆分。

## 三十九、2026-05-25 `agent-auto-route-flow.js` Agent 自动路由执行归档

> 本节记录 `index.js` 中 QQ Agent 自动路由桥接分支的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并按工作目录规则复核了 `git rev-parse --show-toplevel`、当前分支、`git status --short --branch`、`HEAD` 与 `origin/bot_ZZY`。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/agent-auto-route-flow.js`。
- 从 `index.js` 拆出：
  - `heuristicRoute(userText, 'qq', ...)` 自动路由判断。
  - jailbreak 输入降级回普通 chat 的保护。
  - `buildExplicitSearchRunOptions()` 显式搜索参数构造。
  - Agent queue 配置读取与 `enqueueAgentTask()` 调用。
  - Agent 结果经 `retellAgentResult()` 转成 chat 口吻文本。
  - Agent 队列满、队列拒绝和运行失败的原有兜底文案。
- `index.js` 保留中间件、队列入口、live session、最终发送和 freshness 所有权，只调用 `handleAgentAutoRoute()` 并根据 `{ handled, reply }` 早退。
- 新模块不发送消息、不注册 middleware、不拥有队列状态、不反向 import `index.js`。

### 2. 实际执行步骤

1. 新建 `agent-auto-route-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将原 `enqueueForChannel()` 内联 Agent 自动路由逻辑迁入 `handleAgentAutoRoute()`，通过参数注入 `chat`、`agentEngine`、`enqueueAgentTask`、`configureAgentQueue`、`retellAgentResult` 和 `resolveBot`。
3. `index.js` 改为导入 `handleAgentAutoRoute()`，保留 `recentUserMessages`、`searchContext` 的构建位置，方便普通 chat 与 heavy tool 复用同一上下文。
4. 首轮替换后 `index.js` 在自动路由块后多收了一层 `}`，`node -c` 报 `Missing catch or finally after try`；已按当前 `try/catch` 结构修正括号，使普通 chat 分支仍位于同一个错误兜底内。
5. `cascade-test.js` 新增 `agentAutoRouteFlow` 模块加载、导出、语法检查和重复函数扫描。
6. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再内联 `heuristicRoute(userText...)` / `getAgentConfig()`，确保新模块持有自动路由实现且不发送、不注册 middleware、不反向 import。
7. 同步调整既有 Agent 路径守卫：自动路由迁出后，`bot: resolveBot()` 的检查移动到 `agent-auto-route-flow.js`，heavy tool 路径继续检查 `chat-result-flow.js` 的 `bot: getBot()`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/agent-auto-route-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2521`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 209 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过 `agent auto route default off uses normal chat`、`agent auto route ignores casual greeting`、`agent auto route sends tool answer`、`mocked auto route time does not return empty agent reply`、显式搜索与 heavy web_search / web_fetch 场景覆盖原自动路由链路。
2. 断言的失败现象：自动路由迁出后，短闲聊不会误进 Agent，启用自动路由的时间/搜索类请求仍走 Agent 工具再经 chat 转述，队列与失败兜底不泄漏空 Agent 回复、内部 prompt、reasoning 或原始工具报告。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部搜索服务和生产 Agent 队列长时间压力；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 38,969 bytes，降到约 37,644 bytes / 795 行。
- 新增 `agent-auto-route-flow.js` 约 2,452 bytes / 73 行，承接 QQ Agent 自动路由桥接。
- `chat.js` 保持约 43,189 bytes / 795 行。

### 5. 本节结论

- `index.js` 已不再直接持有 Agent 自动路由执行细节，入口只保留上下文构建、依赖注入、发送早退和普通 chat fallback。
- 这一刀没有改变 Agent 自动路由开关、启发式路由、jailbreak 降级、显式搜索参数、队列配置、Agent 失败文案或 chat 转述策略。
- 当前 `index.js` 与 `chat.js` 均稳定低于 50 KB；继续拆分应优先复扫实际收益，避免把随机延迟、最终发送和 chat 主循环这类强时序编排硬拆成更脆的模块。

## 四十、2026-05-25 `chat-send-flow.js` 普通回复发送流水执行归档

> 本节记录 `index.js` 中普通 chat 回复生成后的诊断、随机语音、敏感通知、罕见语音与最终发送流水收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划；本节延续同一轮已确认的工作目录与 git 状态，未部署、未重启、未推送。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/chat-send-flow.js`。
- 从 `index.js` 的普通 chat 分支拆出：
  - 情绪路由旁路诊断。
  - sticker shadow 发送前旁路诊断。
  - 群聊随机语音升级尝试。
  - `别问了，这个我不聊` 敏感处理者通知。
  - `【语音风格：...】` 标签剥离。
  - 罕见固定语音发送尝试。
  - 最终 `safeSendReplyWithFreshness()` 调用。
- `index.js` 保留 chat / Agent 路由、队列、异常兜底、随机发送选项构造和 delayed random 发送体。
- 新模块不调用 chat/Agent、不注册 middleware、不拥有队列、不反向 import `index.js`；安全发送函数由入口注入。

### 2. 实际执行步骤

1. 新建 `chat-send-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 在新模块内实现 `sendChatReplyFlow()`，把普通回复尾段的诊断、随机语音、敏感通知、罕见语音与最终发送顺序原样搬入。
3. 保留 `trySendRandomVoice()` 内部懒加载 `tts` 的模式，避免入口直接展开 TTS 依赖，也不改变随机语音失败后回落文字发送的行为。
4. `index.js` 改为导入 `sendChatReplyFlow()`，普通 chat 分支在拿到 `reply` 后只注入当前上下文和 `safeSendReplyWithFreshness()`。
5. delayed random 分支暂不迁出：它仍绑定 pending timer、版本 freshness、延迟触发和入口队列时序，继续留在 `index.js` 以降低风险。
6. `cascade-test.js` 新增 `chatSendFlow` 模块加载、导出、语法检查和重复函数扫描。
7. `cascade-test.js` 新增 cross-file guard，确保 `index.js` 不再内联普通发送尾段，确保新模块不 import `index.js`、不注册 middleware、不调用 chat/Agent。
8. 同步更新 sticker shadow 发送顺序守卫：发送前诊断的位置从 `index.js` 改为 `chat-send-flow.js`，仍要求诊断发生在真实发送之前且不修改消息。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat-send-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2526`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 210 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过普通聊天、随机回复、sticker 发送顺序、敏感检测、send guard、TTS/随机语音、罕见固定语音和 simulated e2e Agent chain 场景覆盖原发送尾段。
2. 断言的失败现象：发送流水迁出后，普通回复仍只发送最终自然文本，sticker shadow 仍只旁路观察，随机语音 stale 时不会补发过期语音，罕见语音失败仍回落文字，敏感通知仍只在对应回复触发。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session，并 mock 外部 HTTP、模型、TTS、文件下载等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实语音合成服务和生产群聊随机语音分布；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 37,644 bytes / 795 行，降到约 35,180 bytes / 755 行。
- 新增 `chat-send-flow.js` 约 3,988 bytes / 133 行，承接普通 chat 回复发送流水。
- `chat.js` 保持约 43,189 bytes / 795 行。

### 5. 本节结论

- `index.js` 已不再直接持有普通 chat 回复发送尾段的大块实现，入口只保留“拿到 reply 后调用发送流水”的编排。
- 这一刀没有改变诊断顺序、随机语音触发、语音风格标签剥离、敏感通知、罕见语音或最终安全发送策略。
- 剩余 `index.js` 主要是入站消息编排、随机延迟 timer、上下文保存、显式/随机门控和异常兜底；其中延迟随机与门控时序仍不建议为追求行数硬拆。

## 四十一、2026-05-25 `incoming-message-flow.js` 入站材料处理执行归档

> 本节记录 `index.js` 中入站图片、文件、语音材料处理流水的收口结果。继续任务前已重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并按工作目录规则复核了 `git rev-parse --show-toplevel`、当前分支、`git status --short --branch`、`HEAD` 与 `origin/bot_ZZY`；本节未部署、未重启、未推送。

### 1. 拆分范围

- 新增 `packages/koishi-plugin-dongxuelian-ai/lib/incoming-message-flow.js`。
- 从 `index.js` 拆出：
  - 入站图片 segment/content 引用解析、`storeImageUrl()` 落库与 `enqueueAnalysis()` 排队。
  - sticker shadow 入站旁路诊断，保持在图片落库后、分析队列前。
  - 入站文件 segment 解析、安全检查、文件元数据落库、小文件后台缓存。
  - 直聊或点名语音的 ASR 转写与 10 秒超时兜底。
  - 图片、文件、语音锚点对 `plain` 文本的补全。
- `index.js` 保留 middleware 顺序、`analyzed/plain/content/channelKey/directAt` 的生成和后续聊天/随机/记录编排，只调用 `handleIncomingMessageArtifacts()` 接收补全后的 `plain`。
- 新模块不发送消息、不注册 middleware、不调用 chat/Agent、不反向 import `index.js`，也不拥有跨消息状态。

### 2. 实际执行步骤

1. 新建 `incoming-message-flow.js`，添加 MODULE 注释，明确职责、边界和无状态说明。
2. 将原内联图片处理迁入 `handleIncomingImage()`，保留 `getConversationKey(session)`、安全 userId、content 引用兜底、图片锚点追加和分析排队顺序。
3. 将原内联文件处理迁入 `handleIncomingFile()`，保留 `checkFile()`、`sanitizeFileName()`、`storeFile()`、小文件 `cacheSmallFileBackground()` 和跳过文案格式。
4. 将原内联语音处理迁入 `resolveIncomingAudioPlain()`，保留仅私聊/点名触发 ASR、`loadConfig()`、`transcribeVoice()`、10 秒超时和失败回落 `[语音消息]`。
5. `index.js` 改为导入 `handleIncomingMessageArtifacts()`，并删除对 `message-segment`、`image-store`、`image-analyzer`、`file-store`、`file-safety`、`incoming-file`、`voice`、`runtime-config`、`diagnostics` 中相关入站 helper 的直接依赖。
6. `cascade-test.js` 新增 `incomingMessageFlow` 模块加载、导出、语法检查和重复函数扫描。
7. `cascade-test.js` 新增/调整 cross-file guard：确保 `index.js` 只接线 `incoming-message-flow`，`message-segment` 与 `incoming-file` helper 的使用归属迁入新模块，旧 helper 仍保留在原模块；sticker shadow 入站顺序守卫也从 `index.js` 改为检查 `incoming-message-flow.js`。

### 3. 验证结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/incoming-message-flow.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2531`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 211 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本轮是结构等价拆分，不针对单一用户失败输入；重点通过图片追问、短图片追问、文件上传、`CQ content-only file`、Koishi elements file、mixed segment sources file、私聊纯文件、群聊空文件静默、语音 ASR 模块等 scenario 覆盖原入站材料链路。
2. 断言的失败现象：入站材料迁出后，图片仍会落库并排队分析，文件仍会安全落库并保留可追问锚点，群文件仍保持静默，语音仍只在私聊/点名时转写，sticker shadow 仍只旁路观察且不发送、不写生产 sticker 池。
3. mock 范围：scenario 使用 fake Koishi ctx / fake session、临时 data dir，并 mock 外部 HTTP、模型、TTS、文件下载、ASR 等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖生产网络图片/文件 URL 下载成功率、真实 ASR 服务和长时间后台小文件缓存表现；本轮未部署、未重启、未推送。

### 4. 体量变化

- `index.js` 从上一阶段约 35,180 bytes / 755 行，降到约 31,161 bytes / 668 行。
- 新增 `incoming-message-flow.js` 约 4,810 bytes / 109 行，承接入站图片、文件、语音材料处理。
- `chat.js` 保持约 43,189 bytes / 795 行。

### 5. 本节结论

- `index.js` 已不再直接持有图片/文件/语音材料落库、排队、缓存和 ASR 细节，入口只保留当前消息材料处理的一行接线。
- 这一刀没有改变图片分析排队、文件安全检查、小文件后台缓存、语音转写触发条件、失败兜底或 sticker shadow 入站诊断顺序。
- 当前 `index.js` 已低于 32 KB，`chat.js` 已低于 44 KB；后续继续执行前应优先复扫剩余 `exports.apply()` 内的随机延迟、显式交互标记和异常兜底，避免把强时序代码为追求体量继续硬拆。

## 四十二、2026-05-26 架构债优化计划最终完成态归档

> 本节记录 `index.js` / `chat.js` 主干拆分计划的最终收束口径。继续任务前已在 2026-05-26 重新读取 `AGENTS.md`、`AI协作规则.md` 和本计划，并按工作目录规则复核了 `git rev-parse --show-toplevel`、当前分支、`git status --short --branch`、`HEAD` 与 `origin/bot_ZZY`；本节只做完成态归档，未部署、未重启、未推送。

### 1. 最终完成结论

- `index.js` 已从计划初始约 86 KB / 1928 行，收束到约 31,161 bytes / 668 行。
- `chat.js` 已从计划初始约 75 KB / 1479 行，收束到约 43,189 bytes / 795 行。
- 两个主文件均已稳定低于本计划 50 KB 的主干体量目标，且入口职责已变成中间件编排、显式交互标记、随机门控、上下文保存和最终接线。
- 本计划内适合低风险纯搬运的职责已完成模块化：Session 兼容、bot 解析、频道队列、event dump、startup scheduler、plugin lifecycle、message segment、incoming file、incoming message artifacts、shared record text、file quick read、runtime settings、user blacklist、safe send、random state、chat skills、chat tool flow、chat final output、chat jailbreak、chat topic switch、chat agent retell、chat result flow、Agent auto route、chat send flow。

### 2. 最终复扫决定

- 不再继续拆 `index.js` 中的延迟随机执行体：它同时绑定 pending timer、频道消息版本、显式交互版本、live session、chat/Agent、rare voice、ambient water 和 freshness，继续外移会提高取消语义和时序回归风险。
- 不再继续拆 `safeSendReplyWithFreshness()`：它是入口把 `safe-send.js` 与 `random-state.js` 通过依赖注入连起来的薄桥，留在入口可以避免发送层反向感知 `index.js`。
- 不再继续拆 `chat()` 的剩余主循环：模型调用所有权、prompt 编排、conversation 保存和最终返回边界已经清楚；剩余 `hostileLevelCache` / `trimRuntimeCaches()` 属于 chat 内部短缓存，若未来再动应另立 `chat-runtime-state.js` 专项，不作为本计划阻塞项。
- 不再为了行数拆 `lastEmotionCache`、显式交互标记、命令早退和异常兜底：这些段落体量小，且贴近入口 middleware 顺序。

### 3. 架构守卫现状

- 新增生产模块均有 MODULE 注释、导出检查、语法检查、重复函数扫描和 cross-file ownership guard。
- 结构守卫确认：
  - `index.js` 不再直接拥有入站材料、文件快捷读取、Agent 自动路由、chat 发送尾段、safe-send 细节、random state Map、plugin lifecycle。
  - `chat.js` 不再直接拥有 skill loader、tool flow、final output flow、jailbreak flow、topic switch、Agent retell flow。
  - 新模块均不反向 import `index.js`，且不越权注册 middleware 或直接发送消息。
- 固定字符串复扫显示生产 `lib/` 中没有新增 `require('./index')` / `require("../lib/index")` 反向依赖；相关字符串只出现在 cascade 防线和注释中。

### 4. 最终验收命令

本节后需要重新跑完整验证链，作为部署前基线：

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`
- `npm run test:quick`
- `npm run test:scenario`
- `npm run check`
- `git diff --check`

实测结果：

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/index.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js`：通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/test/cascade-test.js`：通过。
- `npm run test:quick`：通过，`passed: 2531`、`failed: 0`、`skipped: 1`。
- `npm run test:scenario`：通过，`passed: 949`、`failed: 0`、`skipped: 2`。
- `npm run check`：通过，检查 211 个文件。
- `git diff --check`：通过。

测试四问：

1. 复现的真实失败输入：本计划是结构等价主干拆分，不针对单一线上失败输入；真实回归入口覆盖了 `莲` / `你好` 短闲聊不误进 Agent、显式搜索、heavy web_search / web_fetch、图片追问、文件追问、读文件、群文件静默、sticker 发送/入站 shadow、随机回复、send guard、敏感命令和 simulated e2e Agent chain。
2. 断言的失败现象：主干拆分后，入口不会泄漏内部 Agent/工具/思考内容，不会把旧随机延迟补发到新交互，不会把入站图片/文件/语音锚点丢失，不会让群文件主动回复，不会让 sticker shadow 改变生产发送行为。
3. mock 范围：本地 scenario 使用 fake Koishi ctx / fake session、临时 data dir，并 mock 外部 HTTP、模型、TTS、文件下载、ASR 等外部依赖；cascade 负责模块导出、语法、重复函数和源码边界守卫。
4. 仍未覆盖真实链路：未覆盖真实 QQ / NapCat / OneBot / Koishi adapter 在线收发，也未覆盖真实外部搜索、真实语音合成、真实 ASR、生产长时间随机 timer 和服务器部署后的进程健康；这些进入后续部署和六轮 Bug 扫描阶段补足。

### 5. 本计划状态

- `架构债优化计划.md` 对应的 `index.js` / `chat.js` 主干拆分目标已完成。
- 下一阶段按用户要求进入：部署到服务器、按 Bug 扫描方案扫描六轮、修复隐藏 bug、完成隐私扫描后提交并推送。
- 只有上述全部完成并确认仓库是最新版本后，才允许开始 `2026-05-25-模块治理与TypeScript渐进迁移待审核方案.md`；进入该计划后，每轮必读文档切换为模块治理与 TypeScript 迁移方案。
