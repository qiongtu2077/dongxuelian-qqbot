# 东雪莲 QQ 机器人开发成果总结

## v1.1.1 — npm 自动代理修复与莲莲图集闪卡样式

- 一键配置流程和普通 `npm install` 会在启动前诊断 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`、npm proxy 配置等来源；发现失效的 `127.0.0.1/localhost` 代理端口时，自动清理本次子进程环境、删除 npm proxy/https-proxy，并切换到 `https://registry.npmmirror.com` 后重试安装。
- 部署页改名为 `一键配置环境并启动`，并在 Node.js、npm、项目依赖状态卡中说明：Node 官方 Windows zip 自带 npm；npm 卡片检查命令是否存在，项目依赖卡片检查 `node_modules` 是否完整。
- npm 失败向导补充代理来源、自动清理状态、清理动作、实际 npm 路径和工作目录，减少把项目依赖失败误判成 Node/npm 未安装的问题。
- 莲莲图集新增按图片持久化的 A-G 闪卡样式；默认无样式，关闭预览或刷新后仍保留已选效果。
- 图集预览改为无边框外壳，保留右上角关闭叉；详情图改成拖拽触发有限旋转，单击不再产生突兀倾斜。
- 图集批量删除按钮改为图标按钮，并保留 Esc、右键和关闭叉关闭预览的交互。

## v1.1.0 — Windows 部署器便携工作目录与安装链路修复

- Release 改为上传 `LianLianBOT-Deployer-v版本号.zip`，zip 内含顶层目录、部署器 EXE 和 README，不再裸传 EXE。
- 打包版启动后优先在 EXE 所在目录旁创建 `LianLianBOT/` 工作目录，所有环境、依赖、配置、下载包、NapCat 和日志都集中写入该目录，避免落到 Electron portable 的临时 `resources/app` 路径。
- 启动器会把内置 `packages/`、`scripts/`、`package.json`、`package-lock.json`、`start.js` 和 `koishi.example.yml` 同步到 `LianLianBOT/`，同时保留用户 `data/`、`runtime/`、`node_modules/`、`koishi.yml` 和 `start-local.bat`。
- 下载器支持保留官方 zip 文件名、校验文件大小和 zip 魔数，避免 GitHub/Node 重定向后下载包变成无扩展名 hash 文件。
- npm 代理错误支持部署器内置“一键修复代理并重试”：使用实际 `runtime/node/npm.cmd` 路径执行配置，并为本次 npm install 清除 `HTTP_PROXY/HTTPS_PROXY` 环境变量。
- NapCat OneKey 安装和便携 Node/npm 安装增加更清晰的下载、解压、复制和路径冲突诊断，避免只显示裸 `ENOTDIR`。

## 一、服务器部署与基础架构修复

从零开始在 YOUR_SERVER_IP（Ubuntu 22.04）部署了完整的 QQ 机器人系统，架构为：

```
NapCat（QQ 登录, WS 服务端 :8080）
  → Koishi v4.18.11（adapter-onebot, WS 客户端）
  → dongxuelian-ai 插件
  → 多 API 后端（OpenCode Go / DeepSeek 官方 / 阿里云）
```

修复了三大基础设施问题：

**1. NapCat Worker 进程崩溃：** 添加 `--disable-gpu` 启动参数解决 GPU 兼容性问题。

**2. adapter-onebot 无法连接 NapCat：** 根因是 koishi.yml 格式错误，将 server 插件格式改为根级 port/selfUrl，协议从 ws-reverse 改为 ws 正向，因为 NapCat 是 WebSocket 服务端，Koishi 须以客户端身份连接。

**3. @satorijs/core 版本冲突：** Koishi 4.x 需要 core ^4.6.0，但 adapter-onebot 依赖 satori 3.x 锁死 core 3.7.0。通过 package.json 添加 overrides 锁定版本，同时创建 patch.js 通过 NODE_OPTIONS 预加载补齐 3.x 缺失的 Session API（resolve、stripped、send）。

---

## 二、识图功能（多模态 + OCR 双轨）

实现了完整的图片识别管线：

从 QQ 消息中提取图片 file 参数 → 调用 NapCat 的 get_image API 获取本地缓存路径 → 读取本地图片文件 → base64 编码 → 发送给多模态模型。

同时集成了 tesseract OCR（chi_sim+eng）作为非视觉模型的回退方案，当模型不支持多模态时自动走 OCR 管线。

中间件中添加了白名单和概率控制：只有白名单群中的图片才会触发识图，非白名单群和无 @ 的图片静默跳过。非多模态模型（如 deepseek-chat）自动跳过识图流程。

---

## 三、转发消息处理

实现了完整的合并转发消息识别管线：

**外层转发：** 检测 session.content 中的 `<forward id="xxx"/>` 或 CQ 码格式 → 调用 callGetForwardMsg 通过 NapCat API 拉取消息节点 → 转换为统一格式 → 通过 summarizeForwardNodes 格式化为带层级的可读文本 → 注入 AI 上下文。

**嵌套转发：** 检测消息段中的 forward 类型或 raw_message 中的 CQ 码 → 递归调用 callGetForwardMsg 尝试拉取 → NapCat 明确返回"内层消息无法获取"时降级显示 `[嵌套转发：内容暂不可见]`。

**格式优化：** 加入层级标记（`[对话]` / `└─ [内层转发]`），接入 MAX_FORWARD_NODES（50 条）和 MAX_FORWARD_DEPTH（4 层）做截断控制，超出部分提示"还有 N 条消息未显示"。

**昵称净化：** 过滤不可见字符（韩文填充符、零宽空格等 U+200B-U+11FF 范围），空昵称回退为"群友"。

---

## 四、引用消息处理

实现了引用消息的内容注入：直接从 session.quote 提取被引用的消息内容，解析文本/图片/表情/@/转发/视频/语音/文件等多种消息段类型，将引用内容以 `[引用内容]xxx[以上是对方说的话，不是在对你说]` 格式注入用户消息，同时去掉重复的系统消息注入，避免 AI 混淆。

---

## 五、模型切换系统（三分层父子菜单）

设计了三层切换体系：`切换模型` → 选择供应商 → 选择模型。支持三个供应商：

- **OpenCode Go**：14 个模型（千问 3.5、千问 3.6、GLM-5、GLM-5.1、Kimi K2.5、Kimi K2.6、DSv4pro、DSv4、MiMo-V2-Pro、MiMo-V2-Omni、MiMo-V2.5-Pro、MiMo-V2.5、MiniMax M2.7、MiniMax M2.5）
- **阿里云 DashScope**：qwen3.5-plus、qwen3.6-plus
- **DeepSeek 官方**：deepseek-chat、deepseek-v4-flash、deepseek-v4-pro

切换时自动切换 API 地址和对应密钥。为 OpenCode Go 的 DeepSeek 模型添加了 reasoning_content 回退处理（当 content 为空时使用 reasoning_content）。

---

## 六、联网搜索能力

在 getSearchCapability 函数中添加了 qwen 模型识别逻辑，使通过 OpenCode Go 调用的 qwen 模型也能使用 enable_search 参数进行联网搜索，突破了原有代码只识别 DashScope 和 OpenAI 官方 API 的限制。支持 dashscope-chat、openai-chat-search、openai-responses 三种搜索模式。

---

## 七、消息队列与稳定性

给 enqueueForChannel 添加了 60 秒超时机制，防止单个 API 请求卡死整个消息队列。修复了 config 作用域问题（config is not defined、vConfig is not defined），在视觉处理代码块中单独加载配置。修复了 helpAI 菜单重复发送的问题。

---

## 八、白名单与概率回复系统

实现了群聊白名单加概率触发的双层过滤机制。白名单群中的消息按设定概率回复，非白名单群全部跳过。每个群可单独设置概率（如 30%、5%），带有 warmup 机制（50 条消息内不递增，之后每条递增 2%）。

修改了 shouldSkipForRandomReply 逻辑，使图片也参与概率判断（修复了图片总是回复的 bug）。同时保留了 @ 提及和私聊的始终回复逻辑。随机回复时添加 `[群聊刷到]` 标记，让 AI 以群友插话口吻回应而非误以为被直接召唤。

---

## 九、多 API 密钥管理

实现了三套 API 密钥的自动切换机制：ai-openai-key.txt（OpenCode Go）、ai-deepseek-key.txt（DeepSeek 官方）、ai-dashscope-key.txt（阿里云），根据当前激活的供应商自动选择对应的密钥和 API 地址。

---

## 十、提示词工程与人设优化

**政治红线：** 写入两套人格，包含红线示例（党和国家领导人、领土主权、政治运动、历史事件等）、隐喻/谐音/反讽识别提醒、标准拒绝话术。

**脏话替代词：** 嘴臭人格中加入屎→史、妈→老冯、傻逼→沙鼻、废物→飞舞、智障→智战等谐音替代规则，同时要求减少屎尿屁输出，并提醒模仿例子时切换代替词。友善人格中删除替代规则（因为友善模式不骂人）。

**字数控制：** 取消 trimReply 的硬截断，改为提示词层面的软性引导"评价类回复控制在两三句话以内"。

**评价类回复：** 加入引导"先看完内容再回应，有值得评论的地方直接说"，避免 AI 盲目输出。识图提示词改为"直接评价这张图，不要描述过程"。

**随机回复插话：** 加入"当前消息是你在群聊里看到的，对方不是在叫你"的上下文标记，防止 AI 用回应对方的口吻回复。

**回复字数放宽：** max_tokens 从 500 提升到 1000，支持更长的回复。

**摆烂机制：** 当 completion_tokens 超过 600 时，AI 自动输出"太麻烦不想了，摆烂了"等随机摆烂回复。

---

## 十一、测试与维护工具

**测试人格：** 管理员通过"测试开/测试关"命令切换，开启后 AI 进入绝对服从模式，原有所有人格设定被覆盖。

**维护模式：** 通过 ai-paused.txt 文件开关，开启后所有消息统一回复"优化中，别急"，不触发任何 AI 调用。

**代码同步：** 本地开发环境与服务器代码定期同步，通过 git 管理并推送到 GitHub bot_ZZY 分支（不碰 main 分支）。

**Release 规范：** 每次发布 release 都必须打 Git 标签，确保 GitHub Releases、发布产物和代码提交能一一对应。

**冗余清理：** 清理了调试日志、未使用的常量等冗余代码，备份了被删除的代码行以便回退。

---

## 十二、QQ 机器人识图配置

QQ 图片有本地缓存路径，位于服务器 `/root/.config/QQ/nt_qq_*/nt_data/Pic/YYYY-MM/Ori/`。识图流程通过 NapCat 的 get_image API 获取本地文件路径，再读取并编码为 base64。非视觉模型自动回退到 tesseract OCR（chi_sim+eng）。

---

## 十三、服务器部署教训集

### 1. `pkill -f` 误杀 SSH
```bash
# 错误 ❌ — 会杀 SSH 守护进程导致服务器失联
pkill -f "start"

# 正确 ✅
pkill -f 'koishi/lib/worker'
```
`pkill -f` 用ERE匹配进程名，`"start"`会匹配到包含`start`的所有进程包括sshd。

### 2. SCP 编码损坏
PowerShell `Get-Content file | ssh ... Set-Content` 会输出 UTF-16 LE带BOM，损坏中文字符和API key。必须直接用 `scp` 二进制传输：
```bash
scp file user@host:/path/    # ✅ 正确
```

### 3. API Key 格式
| 供应商 | Key 前缀 | 文件 |
|---|---|---|
| DeepSeek | `sk-*` | `ai-deepseek-key.txt` |
| mimorium | `tp-*` | `ai-mimorium-key.txt` |
| opencode | `sk-*` | `ai-openai-key.txt` |
| dashscope | `sk-*` | `ai-dashscope-key.txt` |
| GLM | 无固定前缀 | `ai-glm-key.txt` |

服务端和本地的 key 相互不通用，服务端 key 不能复制到本地使用（反之亦然）。

### 4. 进程残留导致双实例
重启时若 `pkill` 没杀干净，旧进程孤儿化后继续监听端口，新进程启动失败。必须 `pkill -f 'koishi/lib/worker'` 后 `sleep 3` 确保释放。

### 5. 重复部署导致双实例
部署时如果第一次 `pkill + start` 已经成功（日志能看到 `loaded`），但没仔细确认就再次 `nohup start`，会跑起两个 bot 实例。两个实例同时处理同一条消息，群友会收到重复回复。

**避免方法**：部署后等 15 秒，确认日志出现 `dongxuelian-ai.*loaded` 且只有一条，不要盲目重试。清理时用 `pkill -f 'koishi/lib/worker'` 精确杀进程，不要用 `pkill -f 'koishi'`（过于宽泛）。

### 6. koishi.yml 格式错误
`server` 插件必须以根级 `port`/`selfUrl` 配置，`adapter-onebot` 必须用 `ws` 协议正向连接 NapCat（NapCat 是 WS 服务端）。

---

## 十四、v0.9.2 ~ v0.9.3 功能更新

### v0.9.2 — 代码拆分 + bug 修复
- **代码拆分**：index.js 拆为 6 个文件（constants.js、utils.js、persona.js、api.js、conversation.js、index.js），函数去重 124 个零重复
- **拆分原则**：可变状态随所属模块走（personaGroupsCache→persona.js、conversationCache→conversation.js）；避免循环 require（callOpenAI 留在 index.js，requestChatCompletions 在 api.js）
- **normalizeText 导入修复**：conversation.js 从 utils 导入 normalizeText，实际该函数定义在 message-reader.js，改正
- **splitSentences 修复**：回退到备份版正则，解决多消息碎片化回复
- **sanitizeUserName / getRandomDelayMs 恢复备份版**：确保与备份版行为完全一致
- **saveSharedChannelTurn / saveUserProfile 恢复备份版**：补回 today cache、sensitive cache、user profile writing 逻辑
- **channelTodayCache 重复声明删除**：index.js 中的重复声明已移除（已移至 conversation.js）
- **今日情绪修复**：5 天对比移到"原因"之前，解决不显示历史对比的问题
- **Help 菜单补全**：复读/敏感/思考/测试/今日情绪/黑名单/群级人格等命令全量加入
- **括号约束移入 core**：`回复尽量不使用括号`从友好模式移至 core，对所有人格生效
- **备份版回归基准**：`E:\莲莲Bot备份（无阴阳人格）\` 作为所有函数行为对比的 ground truth

### v0.9.3 — 敏感话题 + 评价系统 + 记忆优化 + 人格防御重构

**敏感话题系统改造：**
- `saveSensitiveCache` 从 `saveConversationTurn`（需 bot 回复才触发）移到中间件独立执行，所有用户消息都记录
- 加内存缓存 `getPoliticalDetectList()`，30 秒自动刷新白名单，避免每次消息都读文件
- 出站过滤（`SENSITIVE_KEYWORDS_RE` 匹配 AI 回复则吞掉不发）——已删除，理由：正常聊天误伤太多，已有入站检测和 core 规则足够
- 正则命中敏感词 → @处理者告警 + 清除群的共享上下文 + 清除该用户的对话记忆
- AI 分析（每 50 条）判定敏感 → @处理者告警 + 清除群的共享上下文（不清个人记忆，因为无责主体）
- API 返回内容违规错误（HTTP 400 content_policy）→ 触发 `pendingSensitiveAlert` 告警

**评价系统重构：**
- 去掉 `说说` 匹配（原 regex `(?:评价|如何评价|评价一下|说说)` 误触正常聊天）
- 轻量模型摘要管线：读取目标用户 20 条发言 → 调 GLM-4.6v-flash → qwen-turbo → deepseek-v4-flash 做 ≤200 字风格摘要 → 只注入摘要不注入原始消息 → 主模型保持东雪莲/长离/椿人设
- 评价注入改为 `system` 角色 + 引导"用你当前的人设简单回应几句"，不再强求"直接要求"

**AI 记忆优化：**
- 系统提示词加"不翻旧账"指令：`历史记录仅作为背景参考，不要主动提及，除非用户明确问'还记得吗''之前说过'`
- 对话摘要只在 `summaryTotal > 50` 时才注入，加"不要主动提及"前缀
- 用户画像从 5 条缩到 3 条（折中：减少翻旧账 vs 保留话题偏好）

**人格防御重构：**
- `personaName` 非空时（用了长离/椿等自定义人格），跳过所有防御逻辑：
  - `hostile`（嘴臭检测）
  - `isJailbreakAttempt`（越狱检测）
  - `isContextJailbroken`（上下文越狱）
  - `rareProvocation`（罕见/太君）
  - `japanLinked`（日本关联）
- 删除 `personaHostileCapable` 死代码（原用于检测人格是否有嘴臭能力，已被 `personaName` 非空逻辑取代）
- 越狱 prompt 改"按当前人格嘲讽"：去掉"你是东雪莲"，改为"按照你现在的人格针对这个手法的特点嘲讽"

**Core SKILL 新增：**
- `[禁止编造]`：知识库中不知道的事按人设风格回复"不知道"，不要胡编乱造

**Help 菜单重构：**
- 新增 `【敏感话题检测】` 导航入口（renderSensitiveHelp）
- 人格合并为单列表，标注（用户级）/（管理员）
- 指令速查补全群级人格
- 【常用】加今日情绪、复读开/关/状态
- 统一 (bot管理员) 标注

---

## 十五、部署脚本设计

项目根目录包含三类脚本：

| 脚本 | 用途 | 运行位置 |
|---|---|---|
| `setup.sh` | 首次部署 | 复制到服务器后 `bash setup.sh` |
| `scripts/deploy-package.sh` | 更新重启 | 单个插件: `bash scripts/ai.sh`，全量: 用 SSH 逐包部署 |

**setup.sh 用户流程：**
1. 编辑脚本顶部 QQ_NUMBER 和 ADMIN_QQ
2. `bash setup.sh` → 自动安装 NapCat + Koishi + 全部依赖
3. 看到二维码 → 掏出 QQ 扫码登录
4. 按提示输入 DeepSeek API key（首选必填，最流行的模型）
5. 可选输入 mimorium / opencode / dashscope / GLM key
6. 自动启动 bot

**设计原则：** 代码更新后必须同步更新所有脚本。新增 API 供应商、修改配置文件路径、新增依赖等，都要对应修改 setup.sh 和 scripts/ 下的部署脚本。

---

## 十六、v0.9.5 代码清理与结构优化

### handler.js 命令路由拆分
- 从 index.js 迁出命令路由到 handler.js（-418 行），index.js 降至 87KB
- 保留中间件主流程、chat/sendReply/loadConfig 等高阶编排
- handler.js 以 `handleCommand(session, ctx, state)` 形式被 index.js 调用
- state 显式注入所需函数和缓存访问器，避免隐式依赖

### fallback 数组配置
- buildFallbackConfig 从 switch 分支改为 FALLBACK_STEPS 数组驱动
- 顺序保持：GLM → OpenCode deepseek-v4-flash → DashScope qwen3.5-plus → qwen3.6-plus
- 新增 getFallbackSteps() 返回不可变副本供测试校验

### 昵称替换禁用
- sendReply 中的昵称全局替换（`.replace(esc, 'g')` → `'你'`）注释掉
- 原因：暴力替换导致 AI 对用户的称呼被强制改为"你"，效果诡异
- 代码保留注释，后续有好的思路可恢复

### 死代码清理
- 删嘴臭开关完整链路（命令 handler + 群人格显示 + help 菜单共 5 处）
- 删 `buildFriendlyPersona()`、`BANNED_OUTPUT_RE`、`EMOTION_HISTORY_PREFIX`
- 清理 10 处冗余 import（utils/conversation/api）
- 清理 3 处死 export（message-reader）
- 清理临时文件：tmp_fix_bom.js、tmp_rm_dups.py、tmp_help.b64

### 版本与仓库健康
- PLUGIN_VERSION 统一为 0.9.5（常量 + package.json 双写一致）
- 根 package.json 包路径修正 4 处（dongxuelian-ai → koishi-plugin-dongxuelian-ai 等）
- .gitignore 改 `/data/` 限根目录 + 细粒度规则保护 package data 下的 key/用户画像/会话/缓存
- npm scripts 增加 `test`、`check`、`start`

### 括号规则加强
- 核心规则：`尽量不使用` → `禁止使用括号，动作通过语言自然表达`
- 长离示例对话：6 处 `（动作神态）` 全部删除，保留对话内容，消除括号范例对 AI 的引导

### 回归测试 392 项
- 从 45 项扩展到 392 项，15 个测试区块
- 覆盖：仓库结构、模块加载、常量、fallback、消息解析、复读候选/冷却/窗口、命令路由、persona 资源、conversation 纯函数、help 菜单完整性、gitignore 敏感数据、跨文件引用、部署脚本
- 用 fake session/ctx/state 测试命令路由，不写真实 key 或线上数据
- AI 状态增加"不泄露 API key"的断言
- 复读测试覆盖 QQ 原生 face、CQ face、HTML face、多 face、mface/image/file/forward unsupported、冷却刷新、120s 窗口、开关清状态
- 消息解析覆盖 type=face、[CQ:face]、&lt;face&gt;、mface、图片、文件、json、转发
- help 菜单扫描所有 renderXxx() 函数是否都有对应函数定义
- sticker/文本发送路径用静态 guard 锁死：文本走 session.send(text)，sticker fallback 只在 pendingStickers 分支

---

## 十七、世界观注入与人格匹配原则

**人格与世界观严格对应。** 每个世界观 lore 必须按照所属人格注入，禁止混入不相关人格：

| 世界观 | 适用人格 | 例外 |
|---|---|---|
| 鸣潮（wuwa-lore） | 默认人格、长离、椿 | 特蕾西娅不注入，避免 OOC |
| 泰拉（terra-lore） | 仅特蕾西娅 | 鸣潮角色不注入 |

新增人格或 lore 时必须同时考虑匹配规则，禁止出现跨世界观常识混淆（如特蕾西娅知道索拉里斯、长离知道矿石病）。

---

## 十八、代码拆分教训

### repeat.js 拆分失败教训

**目标**：将 index.js 里的复读检测 8 个函数 + 3 个状态 Map 搬到新建 repeat.js。

**失败原因**：一次性改了 4 个文件（index.js、utils.js、chat.js、cascade-test.js），删旧代码和加新代码同时进行，测试报错时分不清是哪个操作引入的。

**具体失误**：

| 操作 | 后果 |
|---|---|
| 删 index.js 函数定义 + 加 import 同时做 | 漏了一个未闭合的大括号，语法检查通过前已污染了文件 |
| cascade-test.js 改了 section 16.5 的源码匹配 | 改到一半的测试引用了不存在的 `index.buildRepeatCandidate` |
| `getSegmentData` 加到了 utils.js 但没及时删 index.js 里的旧定义 | 函数去重检查报错 |

**正确步骤（后续拆文件必须遵守）**：

```
第 1 步：只创建目标文件，完整复制函数 + 状态，跑 node -c 确认语法
第 2 步：源文件加 import，跑 node -c 确认语法
第 3 步：注释掉源文件里的旧定义（不删），跑测试验证 import 生效
第 4 步：测试通过后删掉注释掉的旧代码，再跑一次测试
第 5 步：更新 cascade-test.js 的引用路径（如果需要），再跑测试
```

每步之间 `node -c` + `npm run test:quick` 验证，不过不进入下一步。后三步中任何一步失败，退回上一步重来。

---

## 十九、chat.js 回复防线拆分

### 本轮目标

只拆 `reply-guard.js`，不拆 `prompt-builder.js`。原因是回复安全、防泄漏、重复回复 retry 的边界清楚，收益高、风险低；prompt 组合牵涉人格、lore、history、memory、quoted、forward、vision 注入顺序，暂时不碰。

### reply-guard.js 边界

- 只做判断和 retry/fallback prompt 生成。
- 不调用模型，不修改 `messages`。
- 不保存 conversation，不 import `chat.js` 或 `index.js`。
- `chat.js` 继续拥有 `callOpenAI()`、retry 循环、日志和最终保存顺序。

### 测试结果

- `node -c packages/koishi-plugin-dongxuelian-ai/lib/reply-guard.js` 通过。
- `node -c packages/koishi-plugin-dongxuelian-ai/lib/chat.js` 通过。
- `npm run check` 通过。
- `npm run test:quick` 通过。
- `npm run test:scenario` 通过。
- `npm test` 通过。
- `git diff --check` 通过。

## 十七、记忆清除功能

### 新增命令

| 命令 | 权限 | 效果 |
|---|---|---|
| `东雪莲忘记我` | 任何人 | 清空 bot 对该用户的记忆 + 对话历史 |
| `东雪莲清空群记忆` | 群管理员 | 清空整个群的记忆 + 对话历史 |

### 实现

- `conversation.js` — 新增 `clearUserMemory(userId, channelKey)` 和 `clearGroupMemory(channelKey)`
  - 用户记忆存储在 `USER_PROFILE_DIR/{channelKey}/{userId}.json` 的 `memory` 数组
  - `clearUserMemory` 清空单用户文件中的 memory
  - `clearGroupMemory` 遍历目录清空所有用户 memory
- `handler.js` — 新增两条命令路由
- 帮助菜单 `【常用】` — 添加两项

### 后续规则

- 改 thinking leak、禁词、重复回复、兜底选择时，优先维护 `reply-guard.js`。
- 改 prompt、人格、lore、history 注入时，再评估是否拆 `prompt-builder.js`。
- 不为了减少行数硬拆；只在边界自然且有测试护栏时拆。

---

## 二十、群聊日报插件（koishi-plugin-daily-report）

### 功能概述

独立插件，为 QQ 群生成每日聊天分析报告图片。两条命令：
- `群聊日报` — 基础模式（话题总结 + 今日圣经，~1000 tokens）
- `群聊详细日报` — 完整模式（+ 群友画像 + 群聊锐评，~2500 tokens）

### 架构

```
index.js          中间件入口（命令匹配、白名单、冷却）
config.js         配置（DATA_DIR fallback、超时、模板调试）
data-collector.js 读取 today-cache-{key}.json，统计消息数据
ai-analyzer.js    复用主插件 runtime-config.js + api.js，compress→analyze 两阶段
html-renderer.js  模板加载、时段选择、占位符替换、Puppeteer 截图
models.js         数据模型工厂函数
analyzers/        独立分析器（topic/user-title/golden-quote/chat-quality）
templates/        HTML 模板（light.html 亮色 / dark.html 暗色）
test/             plugin-test.js（29 项单元测试）
```

### 关键设计决策

1. **复用主插件 API**：禁止自己写 AIClient，直接 require 主插件的 `runtime-config.js` + `api.js`，获得 fallback 链、超时、重试
2. **白名单共用**：读取主插件的 `summary-whitelist.json`，群里启用消息缓存即自动启用日报
3. **中间件拦截**：`ctx.middleware()` 匹配 `群聊日报`/`群聊详细日报` 后 bare `return` 阻止后续插件
4. **compress→analyze 两阶段**：先批量压缩消息（100条/批），再分析压缩结果，节省 60% tokens
5. **Puppeteer 信号量**：最多 2 个并发实例，30 秒超时，防止内存溢出
6. **模板时段选择**：6:00-18:00 用 light.html（亮色初音绿），18:00-6:00 用 dark.html（暗色深蓝黑）
7. **占位符系统**：HTML 模板用 `{{variable}}` 占位符（非 `${}` 避免 JS 冲突），运行时 split+join 替换
8. **QQ 头像**：今日圣经和群友画像均使用 `https://q1.qlogo.cn/g?b=qq&nk={userId}&s=100` 抓取真实头像
9. **DATA_DIR fallback**：本地未设 `DONGXUELIAN_AI_DATA_DIR` 时自动定位到主插件 data 目录

### 模板文件

| 模板 | 时段 | 风格 |
|---|---|---|
| `light.html` | 6:00-18:00 | 白底初音绿，SVG 柱状图 |
| `dark.html` | 18:00-6:00 | 深蓝黑底，渐变标题，LIAN 水印 |

### 开发中踩过的坑

1. **中间件 return 语义**：`return session.send()` 有返回值会终止但语义不清，必须 `await session.send()` + bare `return`
2. **`${}` 模板冲突**：HTML 模板用 `${}` 会和 JS 模板字符串冲突，改用 `{{}}`
3. **scp -r 不删文件**：本地删除的文件不会从远程删除，服务器残留 paper.html/miku.html 导致崩溃
4. **DATA_DIR 环境变量**：index.js 直接读 `process.env` 而非 config.js 的 DATA_DIR，导致本地白名单读不到
5. **dark.html CSS 损坏**：旧版 CSS 每条规则末尾多 `}}`，类名和渲染器不匹配，需完全重写
6. **Puppeteer 并发**：多个群同时请求会启动多个 Chromium（各 ~200MB），无信号量会 OOM

### 测试

`npm run test:plugins` 覆盖 29 项：模块加载、导出检查、models 单元测试、data-collector 校验、中间件注册、命令匹配。

---

## 二十一、谁艾特我 + 定位消息功能

### 功能概述

`谁艾特我` / `谁@我` — 查看今天群里谁 @了你，按时间倒序列出最近 10 条。`定位消息 <编号>` — 查看指定消息的前后文（共 5 条）。

### 架构

- `handler.js` — 新增命令路由，复用 `summary-whitelist.json` 白名单
- `conversation.js` — today-cache 每条消息加 `mentionUserIds` 字段（谁被 @了）
- `constants.js` — `RESERVED_PREFIXES` 加 `'谁'`
- help 插件 — 常用菜单加菜单项

### 关键设计

1. **复用解除上限群白名单**：和白名单共享，不需要额外开关
2. **双路检测**：
   - 主路：`mentionUserIds` 字段（新缓存）
   - 兜底：`extractAtIds()` 直接从消息内容解析 @ 语法（兼容旧缓存或字段丢失）
3. **索引定位**：`定位消息 N` 中的 N 对应输出列表的编号，用 `Array.indexOf` 在缓存数组中定位，不依赖 messageId
4. **纯 @ 无文字也记录**：`saveSharedChannelTurn` 的早期返回改为 `!value && !hasMentions`

### 踩过的坑

1. **频繁重启导致消息丢失**：today-cache 每 20 条或 5 分钟写盘一次，重启间隔内的消息从内存丢失。`mentionUserIds` 字段的写入同时受此影响
2. **双路检测的必要性**：最初只依赖 `mentionUserIds`，但该字段因缓存丢失可能不存在。补充 `extractAtIds` 从 content 解析作为兜底

---

## 二十二、嘴臭开关 + 反击值系统

### 功能概述

将 hostile 模式从自动正则触发改为智能三态评分系统：

```
友善（0-60） → 阴阳（60-90） → 嘴臭（90-100，管理员开关）
```

### 架构

| 文件 | 职责 |
|------|------|
| `retaliation.js`（新建） | `calculateRetaliationScore()` — AI 评分 0-100，正则命中后才调用 |
| `chat.js` | `hostile` boolean → `retaliationLevel` 三态，15 处引用改造 |
| `handler.js` | `东雪莲嘴臭开/关`（管理员专用） |
| `constants.js` | `HOSTILE_MODE_FILE` + 阈值常量 |
| `SKILL.persona-yinyang.md`（新建） | 阴阳人格 mode skill（被动攻击、阴阳怪气、不带脏话） |

### 三态行为

| 等级 | 反击值 | 默认人格 | 自定义人格 |
|------|--------|---------|-----------|
| 0 = 友善 | ≤ 60 | 正常回复，忽略攻击 | 完全绕过，使用人格自有 skill |
| 1 = 阴阳 | 61-90 | 阴阳怪气、讽刺、不带脏话 | 完全绕过 |
| 2 = 嘴臭 | ≥ 90 | 脏话反击（管理员开启后） | 完全绕过 |

### 逻辑流程

```
消息 → 正则检测（零成本）
  ↓ 未命中 → 友善
  ↓ 命中 → AI 评分（max_tokens=10，看最近 10 条历史）
    ↓ ≤ 60 → 友善
    ↓ 61-90 → 阴阳
    ↓ ≥ 90 → 嘴臭（管理员开启后）
```

自定义人格时完全不进入反击值计算。

### 踩过的坑

1. **AI 评分 token 成本极低**：正则命中率约 10%，每次 ~200 tokens，一天几千条消息约 4000 tokens ≈ ¥0.004
2. **人格绕过必须在评分之前判断**：`!personaName` 在最外层，确保自定义人格时完全不消耗 AI 评分 token
3. **阴阳模式默认开启**：不需要开关文件，`YINYANG_MODE_FILE` 不存在即为开启状态

---

## 二十三、Bot Dashboard 管理面板（koishi-plugin-dashboard）

### 功能概述

独立 HTTP 服务（端口 5150），Vue 3 + Vite 前端，提供可视化 bot 管理界面。

### 架构

```
Vue 3 SPA（端口 5150）↕ HTTP JSON API ↕ 独立 HTTP 服务器（内置在插件中）
  ↕ 读写文件
ai-provider.txt / ai-model.txt / 人格文件 / key 文件
```

### 标签页

| 标签页 | 功能 |
|--------|------|
| **模型配置** | 供应商/模型下拉联动，API 地址编辑，保存后热加载 |
| **API Keys** | 查看所有密钥存在状态（掩码显示），可编辑并热加载 |
| **人格管理** | 自定义人格 + 系统模式列表 |
| **功能介绍** | 卡片式功能说明，关联功能可点击跳转 |
| **指令速查** | 分类指令列表，带搜索过滤 |
| **白名单** | 解除上限群白名单列表 |
| **状态** | 当前供应商和模型 |
| **控制** | Bot 启动/停止、维护模式开关 |

### 关键技术决策

1. **独立 HTTP 服务器**：由于 `@koishijs/plugin-server@3.x` 的 `ctx.router` 兼容问题，Dashboard 使用独立的 `http.createServer` 监听独立端口（默认 5150），不依赖 koishi 的路由系统
2. **密码登录**：SHA256 静态 token，24 小时有效。密码可通过 `DASHBOARD_PASSWORD` 环境变量设置
3. **相对路径 API**：前端请求使用 `/dashboard/api` 相对路径，同时兼容 SSH 隧道（`localhost:5150`）和公网访问（`服务器IP:5150`）
4. **硬编码环境变量化**：端口、路径、Shell 命令全部使用环境变量覆盖

### 踩过的坑

1. **`ctx.router` 不可用**：`@koishijs/plugin-server@3.2.9` 不提供 `ctx.router` 给插件。改用独立 HTTP 服务器
2. **Token 生成用 Date.now() 导致登录后立刻失效**：每次 `createToken()` 调用生成不同 hash，修复为静态密码 hash
3. **维护模式写入 "on" 导致 bot 回复 "on"**：`writeFileSync('on')` → bot 读文件内容当回复。修复为写入"优化中，别急"，关闭时 `fs.unlinkSync` 删除文件
4. **维护模式回复所有群**：维护检查在中间件最前面，跳过白名单和 @ 检测。修复为只回复 @bot 和私聊

---

## 二十四、引用消息归属修复

### 问题

群友引用消息时，AI 把引用内容归到回复发送者名下，导致混淆（"这他妈这句话不是我说的"）。

### 根因

`chat.js` 将引用内容直接塞进当前用户的 `<user>` 块：

```
<user>
昵称：水落烟雨          ← 回复发送者
发言：这他妈不是我说的
[引用内容]ru的原话       ← 被引用消息
[以上是对方说的话，不是在对你说]
</user>
```

### 修复

改 `chat.js` 的引用消息处理，从 `session.quote` 中提取原作者昵称：

```
[引用 ru 的原话]
ru的原话
[以上是引用内容，不是 水落烟雨 说的]
```

`session.quote.userId`、`session.quote.nickname` 等字段原本已存在但从未被读取。

---

## 二十五、patch.js 删除与补丁合并

### 背景

`@satorijs/core@3.7.0` 缺少 `Session.prototype` 的 `stripped`、`resolve`、`send`、`text` 四个方法。原方案使用独立的 `patch.js` 文件通过 `node -r ./patch.js` 预加载补丁，但存在两个问题：

1. `start.js` 的 `require('./patch')` 在父进程执行，prototype 修改不继承给子进程（协程 bug）
2. 插件 `index.js` 已有一份重复的补丁代码

### 修复

- 删除 `patch.js`
- 在 `dongxuelian-ai/index.js` 的补丁区域新增 `text()` 方法
- 更新 `start.js` 为干净启动入口
- 更新 `教训总结.md` 删除 patch.js 相关说明
- 服务器启动命令去掉 `-r ./patch.js`

### 影响

| 项目 | 状态 |
|------|------|
| 补丁位置 | `dongxuelian-ai/index.js` 插件加载时自动补全 |
| 启动命令 | `node node_modules/.bin/koishi start`，不再需要 `-r` |
| 兼容性 | 旧启动方式（带 `-r`）仍可工作（补丁会在 index.js 重复执行，但 `if (!...)` 守卫会跳过） |

---

## 二十六、群聊日报优化

### 基础模式零 token

基础模式（`群聊日报`）跳过 AI 分析，只渲染统计数据。改动在 `index.js`：

```javascript
// 旧：const analysis = await analyzeWithAI(data, isFull)
// 新：const analysis = isFull ? await analyzeWithAI(data, true) : {}
```

### AI 分析失败兜底

GPT 添加了 `completeFullAnalysis` + 统计 fallback 逻辑。当 AI 分析失败时，用统计数据生成画像和锐评，保证日报不会空白。同时添加了 `safeParseJSON` 返回 null 时的兜底。

### 总字数统计修正

`totalChars` 之前只剥离 `[CQ:xxx]` 格式码，但缓存中的内容已被主插件替换为中文标签（`【QQ表情：眨眼】`等），导致字数虚高。新增 URL 和 `【】` 标签的剥离。

---

## 二十七、控制面板（Bot 开关与维护模式）

### 功能

| 按钮 | 操作 | 确认 |
|------|------|------|
| 启动 Bot | 执行 `restart.sh` | 无 |
| 停止 Bot | `pkill -9 -f koishi` | 二次确认弹窗 |
| 维护模式开关 | 写入/删除 `ai-paused.txt` | 即时生效 |

### 实现

- Dashboard 后端通过 `child_process.exec` 调用系统命令
- 状态检测：`ps aux | grep 'koishi/lib/worker'` 判断进程存活
- 安全：停止按钮需要二次确认，防止误触
- 维护模式只回复 @bot 和私聊消息，群聊静默

---

## 二十八、开发教训总结

### Dashboard 硬编码问题

端口、路径、Shell 命令不能硬编码：

| 硬编码 | 修复 |
|--------|------|
| `const PORT = 5150` | `process.env.DASHBOARD_PORT \|\| 5150` |
| `bash /root/koishi-app/restart.sh` | `process.env.KOISHI_DIR \|\| __dirname` |
| `=== 'on'` 维护检测 | `!!readFileSync(...)` 非空检测 |

### NapCat Bearer Token

NapCat 有两个独立的认证体系：
1. **OneBot HTTP API** — token 来自配置文件
2. **WebUI（QQ 管理）** — 动态 token，启动时从 webui.json 读取，运行时可能被刷新

当前 WebUI 的 Bearer token 验证未通过。替代方案：Dashboard 通过 `/dashboard/napcat/*` 反向代理到 NapCat WebUI，在服务器本地访问。

### 多 worker 进程隔离

koishi 多 worker 模式下，每个 worker 有独立的内存空间和模块缓存。`channelTodayCache`、`channelSharedCache` 等 Map 实例在各 worker 之间不共享。跨 worker 数据共享必须通过磁盘文件。

### 重启流程必须用脚本

手动拼命令重启容易漏步骤。`restart.sh` 做 5 件事：杀进程 → 等端口 → 设环境变量 → 启动 → 验证日志。验证时通过时间戳标记区分新旧日志，防止旧日志中的成功记录导致误报。

---

## 二十九、Dashboard 视觉升级

### 毛玻璃效果

卡片使用半透明 `rgba` 背景 + `backdrop-filter: blur(12px)` 毛玻璃效果。CSS 变量化后支持 dark/light 双主题切换，切换按钮在右上角。

### 鼠标拖尾

使用 Canvas 渲染的渐变色线条拖尾（青→粉→紫渐变），加上星星点点的溢出粒子（随机颜色、闪烁、缓慢消散）。鼠标停止后线条通过 lerp 方式平滑缩短（旧点以 10% 速度移向下一个点，靠近后删除），不会突兀截断。

### 毛玻璃卡顿的教训

首次实现时所有卡片使用 `backdrop-filter: blur(16px)`，加上 `<Transition>` 动画切换组件，导致连续切换标签页时严重卡顿。解决方案：
- `blur` 从 16px 降到 12px（性能与美观平衡）
- `<Transition>` 改为 `<KeepAlive>`，组件缓存不卸载，切换瞬间恢复
- KeepAlive 缓存的组件需要通过 `onActivated` 钩子重置按钮状态，避免锁死

---

## 三十、管理员密码系统

### 二次验证密码体系

| 密码 | 默认值 | 用途 |
|------|--------|------|
| 访问密码 | `DASHBOARD_PASSWORD` | 登录 Dashboard |
| 管理员密码 | `DASHBOARD_ADMIN_PASSWORD` | 敏感操作二次验证；变量名保留兼容旧版本 |

### 受保护的操作

所有涉及 Bot 配置变更的操作都需要管理员密码二次验证：启动/停止 Koishi、修改 API 地址/Key、新增人格、修改白名单、更换 QQ 号、开/关维护模式、修改密码。

### 实现

- 前端统一使用 `window.showAdminDialog(msg, callback)` 全局弹窗，由 App.vue 的 `AdminDialog` 组件渲染（`z-index: 10000`，高于鼠标拖尾的 9999）
- 管理员 token 存储在 `localStorage`，有效期 1 小时，过期后自动清除
- 后端敏感 API 路由检查 `X-Admin-Token` 请求头
- 密码存储在 `data/dashboard-admin-pwd.txt` 和 `data/dashboard-access-pwd.txt`，环境变量 `DASHBOARD_PASSWORD` / `DASHBOARD_ADMIN_PASSWORD` 可覆盖

### 失败教训

首次实现时 `AdminDialog` 的 `visible` 默认设为 `true`，导致弹窗默认挡住整个界面。且弹窗 `z-index` 低于鼠标拖尾 Canvas（9999），按钮点不了。正确做法：`visible` 默认 `false`，在 API 返回 403 时才弹窗。

---

## 三十一、Dashboard 稳定性

### 独立进程

Dashboard 从 koishi 插件分离为独立进程（`standalone.js`），监听独立端口（默认 5150），不受 koishi 启停影响。`restart.sh` 先启动 Dashboard 再启动 koishi。

### 崩溃保护

- `process.on('uncaughtException')` 和 `process.on('unhandledRejection')` 全局捕获，单请求错误不拖垮整个进程
- `watchdog.sh` 守护脚本，每 10 秒检查端口 5150，崩溃后自动重启
- 所有按钮操作使用 `try/catch/finally` 兜底，确保 `acting` 状态永远解锁
- 所有 API 路由独立错误处理，单个 500 不影响其他请求

---

## 三十二、session.stripped 兼容修复与代码清理（2026-05-08 ~ 05-09）

### 背景

`@satorijs/core@3.7.0` 的 `Session.prototype` 缺少 `stripped`、`resolve`、`send` 四个方法，导致 Commander 中间件访问 `session.stripped` 时返回 `undefined`，整个中间件链断裂，所有插件收不到消息。

### 排查链路

1. 日志出现 `[W] session ... TypeError: Cannot destructure property 'hasAt' of 'session.stripped' as it is undefined.`
2. 直接原因：`session.stripped` 的 getter 在运行时 session 对象上不存在
3. 补丁打在 `require('@satorijs/core').Session.prototype` 但被 `require('koishi').Session`（undefined）误导
4. 辅助原因是重启脚本使用了全局 `/usr/bin/koishi`（含独立 `@satorijs/core@4.6.0`），而非本地 binary

### 修复内容

| 修复 | 文件 | 说明 |
|------|------|------|
| Session 补丁 | `index.js` 顶部 | 从 `@satorijs/core` 直接引用 Session/Bot，安装 `stripped`/`parsed` 访问器 |
| Bot.prototype.session 包装 | `index.js` | 确保每个新创建的 session 实例都有 stripped 兜底 |
| resolve 补丁 | `index.js` | 补 `Session.prototype.resolve` |
| send 补丁 | `index.js` | 补 `Session.prototype.send`，加 `h.normalize()` 确保 CQ 码被正确解析 |
| safeSendReply | `index.js`（YUN 已有） | 发送失败封控：挫折计数、管理员通知、1 小时熔断 |
| chat().catch 兜底 | `index.js` | 从裸 `session.send(msg)` 改为 `safeSendReply` |
| 删除 patch.js | 根目录 | 补丁已合并到 index.js，不再需要独立文件 + NODE_OPTIONS |
| 恢复 start.js | 根目录 | 去掉 NODE_OPTIONS 拼接，恢复 `require('koishi/bin')` |
| 修复 restart-bot.sh | `scripts/` | 用本地 `node_modules/koishi/bin.js` 替换全局 `/usr/bin/koishi`，去掉 NODE_OPTIONS |
| 修复 restart.sh BOM | 服务器 | 移除 UTF-8 BOM（`ef bb bf`），防止 `#!/bin/bash` 无法识别 |
| 修复 setup.sh 编码 | `setup.sh` | 从二进制损坏的 20966 字节恢复到正常 UTF-8 文本 10835 字节 |
| 删除 standalone.js 密码 debug 日志 | `standalone.js` | `log('login failed: input_len=...')` 改为 `log('login failed')` |

### 关键发现

1. **`require('koishi').Session` 是 `undefined`**，必须用 `require('@satorijs/core').Session`
2. **全局 `/usr/bin/koishi` 有自己的 `@satorijs/core@4.6.0`**，与 app 的 `@satorijs/core@3.7.0` 不兼容，adapter-onebot 的 `http.ws()` 静默失败
3. **PowerShell 管道写入文件会损坏二进制**：`git show ... > file` 或 `| Set-Content` 会输出 UTF-16 导致文件编码损坏。必须用 `cmd /c "git show ... > file"` 或 SCP 传输

### 当前分支策略

- **`bot_ZZY`**（开发分支）：包含所有修复
- **`YUN`**（备选基线）：模块拆分更彻底，setup.sh 编码已修。已恢复原状 `eb391fe`
- **`main`**（生产分支）：稳定的旧版，未更新

## 🚀 2026-05-10 ~ 05-11 重大更新

### API Fallback 链重构

将原有的单条 `FALLBACK_STEPS` 拆为 `chat / vision / lightweight` 三条独立链，各自最后都配有主模型兜底。

- 自定义供应商的 key 和模型自动注入 fallback 链，前端可编辑 fallback 步骤
- 所有轻量调用（反击打分、摘要、敏感检测、话题切换、越狱回复、情绪分析、评价总结）标注 `_fallbackSet: 'lightweight'`
- `chatJailbreak` 从裸 fetch 改为走 `requestChatCompletions`，获得完整 fallback 链
- `isVisionModel` 从硬编码正则改为查模型 `vision` 标记，自定义供应商的视觉模型也能正确识别

### 发送控制与风控防护

- `safeSendReply` catch 块仅对 `retcode: 1200` 走冻结逻辑，非风控错误跳过 streak
- `maxStreak: 2`，连续 2 次 1200 冻结 1 小时
- 冻结期管理员指令正常执行，非管理员 @bot 回「我被盯上了，有内鬼终止交易」
- 通知：首次失败立即通知 + 冻结时 setTimeout 30 分钟二次通知
- `sendReply` 内部 `session.send()` 加 try/catch re-throw，让上层正确感知失败
- 新增全局发送节流（前端终端控制配置，默认 20 条/分钟/群）
- 随机回复冷却 30s → 15s

### 人格系统

- Dashboard 人格列表拆为三张独立卡片：核心规则 / 默认人格 / 自定义人格
- 编辑按钮点击后显示「加载中...」并自动滚动到编辑区
- Will 值滑动条（0.1~2.0），`calculateWillFactor` 改从 frontmatter 读取 will
- NSFW 策略下拉（不参与 / 可以接话），仅对自定义人格显示
- `sanitizeReply` 句尾名字改为删除而非替换成「你」

### 罕见/稀有度检测重置

- 宽泛正则预过滤 + LLM 确认（YES/NO，max_tokens=5，lightweight fallback）
- 自定义人格完全跳过此链路
- 仇恨缓存 30s：攻击中叠加等级，正常消息衰减，decay 到 0 删除 entry

### 情绪分析 → 转发消息

- 今日情绪改为发送 QQ 合并转发，点开才看到分析内容
- 新增 `api.sendForwardMsg()`，失败降级为纯文本

### Config 修复

- Dashboard DATA_DIR 回退链改为 `KOISHI_DIR/data` 优先
- 全部 8 处 frontmatter 解析点加 BOM 剥离
- `isLocalAuthBypass(req)` 补传参数
- 节流配置写入改为原子 `.tmp` + `rename`，读取加 BOM 防护
- 前后端 NaN 输入校验

### YUN 分支合并

fast-forward 合并 `qiongtu2077/YUN`（6 commit），零冲突：
- 统一管理员密码验证逻辑
- NapCat Token 按需验证（控制终端不再自动请求）
- AdminModal 弹窗重构（替代旧 prompt 弹窗）
- 登录侧边栏布局
- 白名单 3 秒轮询实时同步

### 测试

- `npm run test:quick`：751/751 ✅
- `npm run test:scenario`：196/196 ✅
- `getFallbackSteps` 适配三链返回格式
- 新增 `isWideRareProvocation`、`CUSTOM_PROVIDERS_FILE`、`FALLBACK_CHAINS_FILE`、`THROTTLE_CONFIG_FILE` 注册
