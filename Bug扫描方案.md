# Bug 扫描方案

本方案用于长期扫描 `待完成与待审核任务/优化建议.md`、`claude的优化建议.md` 和归档计划中仍要求持续覆盖的 bug。核心目标不是把扫描做成安全策略清单，而是先发现真实用户会直接感知到的错回、漏回、乱回和工具失效，再检查权限、安全、架构和部署边界。

## 1. 收敛规则

- 本地和服务器交替扫描：本地源码/测试/模拟一轮，服务器日志/运行包/MCP 对照一轮。
- 连续 10 轮没有发现新的真实 bug，才允许判定长期扫描任务暂时收敛。
- 修复完成后至少再做 5 轮本地/服务器复扫，确认没有引入新的上下文、权限、路由、工具调用或用户体验 bug。
- 每轮即使没有新增 bug，也要记录排除的疑点、排除依据和下一轮优先方向。
- 扫描记录写入待审核区的“优化建议”类文档；已修复并验收后再归档到 `PLANNNNNNNNNNN/`。

## 2. 扫描优先级

### P0：用户可见体验

优先从真实聊天日志和用户现场开始看：

- 多次 @ bot 不回复，或只回复后一个问题。
- 回复错人、错接上一位用户、把 A 的私人任务接到 B 身上。
- 把转发聊天记录、内层转发壳、工具包装或内部 JSON 当成用户可见内容。
- 图片后短句、文件后短句、转发后追问时不调用应有工具却乱编。
- 用户纠错后仍沿着错误线程继续说。
- 随机主动回复插入当前明确交互，或延迟补发旧话题。
- 人格语气突然退化成系统报错、英文硬编码、固定模板或 OOC 说明。

### P1：上下文和工具路由

- active scene 是否优先当前热窗口、引用链、媒体/文件/语音锚点和当前用户。
- 群聊跨用户承接是否能区分公共任务、主动加入、误接私人任务和需要澄清。
- 私聊裸 URL 自动读取是否合理；群聊 URL 是否按 active scene 和上下文自由路由。
- 旧图旧文件进入 prompt 是否有权重、归属、锚点和时效，不把“出现旧材料”本身判成 bug。
- 搜索/fetch、读图、读文件、查旧记录是否给模型自由判断机会，而不是靠关键词主路径。
- 工具调用失败、超时或权限拒绝时，最终回复是否自然、可信、不假装已经完成。

### P2：权限、安全和副作用

- 会写文件、删状态、创建提醒/计划、发文件、发消息、跑浏览器、访问外部资源的工具是否有确认和权限门。
- QQ 默认工具列表是否暴露不该默认开放的行动类工具。
- 读文件关闭时，是否仍能通过发送文件、计划、定时任务等旁路产生外发或写状态。
- 浏览器、fetch、文件下载是否有 SSRF、重定向、子资源、内网和超时防护。
- 所有发送路径是否捕获平台拒发，不让 middleware reject 或造成链路半成功。

### P3：架构、测试和部署一致性

- 本地源码、服务器运行包、远端分支是否能对照出同一版本。
- 修复是否为了跑通而硬编码，或绕过模型自由路由。
- 新修复是否引入新的权限、上下文、路由、测试覆盖或用户体验 bug。
- 测试是否覆盖真实失败输入，而不是只做源码字符串扫描。
- 服务器日志、运行包和 MCP 诊断是否和本地判断一致。

## 3. 每轮固定流程

1. 先读约束：确认 `AGENTS.md`、当前分支、工作区状态、相关优化建议和未归档计划。
2. 先看人话：抽取真实聊天日志或构造最贴近现场的对话，先判断正常用户会不会觉得 bot 在乱说。
3. 再看工具：用日志、MCP、测试 mock 或源码确认工具是否启用、是否被权限门挡住、是否给模型调用机会、实际有没有调用。
4. 再看代码：定位最小代码路径，区分 prompt 问题、scene 构造问题、工具注册问题、执行层权限问题和发送层问题。
5. 写入结论：只记录真实存在或能最小复现的问题；疑点必须写“排除/暂不新增”。
6. 形成修复建议：说明要改哪里、为什么这样改、如何避免硬编码、如何防止新 bug。
7. 验证闭环：补 scenario 或最小测试，回答真实失败输入、断言现象、mock 范围、未覆盖真实链路。

## 4. MCP 使用要求

MCP 是常规排查手段，不只是连通性检查。涉及工具、权限和线上状态时必须优先考虑使用 MCP 或等价诊断：

- 确认 bot 当前可用工具列表、工具 schema、危险标记、默认渠道和权限门。
- 对照 chat 路由是否允许模型自由触发工具，而不是被关键词或配置挡死。
- 检查线上运行源码、运行配置、日志片段和本地提交是否一致。
- 对图片、文件、搜索、fetch、计划、提醒、发送文件等能力做只读或隔离诊断。
- 服务器排查默认只读；部署、重启、写生产数据、清理日志、推送必须另有明确授权。

## 5. 不要误判的场景

以下不是天然 bug，必须先判断产品语义：

- B 承接 A 的“帮我找找吧”：可能是公共任务、主动加入、误接私人任务或需要澄清，不能直接判错。
- 私聊裸 URL 自动读取：通常合理；群聊裸 URL 要看 active scene、@、引用、上下文和群内互动。
- 旧图旧文件进入第二轮 prompt：可能必要；问题在权重、归属、锚点、时效和执行预算。
- 工具被模型调用：不等于 bug；要看调用对象、权限、时机、上下文和最终回复。
- 模型没有调用工具：也不一定是 bug；只有在当前对话无法自然回答且已有明确媒体/文件/搜索需求时才判问题。

## 6. 每轮记录模板

```md
## 本地/服务器第 N 轮（主题）

范围：
- 覆盖哪些日志、代码路径、工具、测试或服务器对照。

结论：
- 新增真实 bug 数量。
- 连续无新增计数是否重置。

### LN/SN-X 标题

真实性：
- 真实日志 / 本地最小复现 / 服务器只读确认 / MCP 对照确认。

证据：
- 真实失败输入。
- bot 实际回复或实际工具行为。
- 关键代码路径或日志依据。

影响：
- 用户会如何感知。
- 是否会造成错回、漏回、权限副作用、数据泄露或排查困难。

建议修复：
- 改动位置。
- 修复逻辑。
- 为什么不是硬编码。
- 如何避免引入新的上下文、权限、路由或体验 bug。

验证方式：
- 修复前复现。
- 修复后断言。
- mock 了哪些依赖。
- mock 未覆盖哪些真实链路。

## 本轮排除/暂不新增

- 疑点：
- 排除依据：
- 下轮是否继续观察：
```

## 7. 修复前后复核

- 修复计划生成时先问：这个方案会不会制造新的权限洞、路由短路、上下文错接、测试假绿或用户体验退化。
- 如果方案只是把某个输入写死成固定回复、固定工具、固定关键词路由，默认不通过，除非它是权限边界、安全兜底或确定性异常降级。
- 修复落地后必须跑对应测试，并复核相关反例：不该回复时不回复、该工具时给工具机会、该澄清时自然澄清、该沉默时沉默。
- 用户否决过的优化方向，后续扫描不得反复当成 bug；必要时在代码附近或计划中留下简短批注说明已评估不采用。

## 8. 最小场景池

每轮至少抽取其中几类做体验扫描，避免只钻安全策略：

- 群聊 A 问、B 短追问、C 插话。
- 图片后一条短句：`666`、`这啥`、`什么鬼`、`看这个`。
- 文件后一条短句：`这里面说啥`、`会卡吗`、`这个呢`。
- 转发聊天记录后问：`这里面说的是什么`。
- 空转发、内层转发、转发壳。
- 多次 @ bot，其中前一条需要搜索/读图/读记录。
- bot 回错后用户纠错：`你读的是什么`、`说梦话呢`。
- 私聊旧任务后隔很久说：`帮我找找吧`。
- 群聊裸 URL、私聊裸 URL、URL 后追问。
- 自定义人格绑定后呼名、点名、沉默边界。
- 视频/图片/文件过大或失败时的人格化兜底。

一句话原则：先当群友看它像不像正常聊天对象，再当工程师看它的安全边界和架构边界。
