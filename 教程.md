# 无 AI 版本部署简明教程

这个分支不部署 `dongxuelian-ai`，机器人不会参与自由聊天。

## 部署顺序

1. 先部署 NapCat，确认 QQ 能正常登录。
2. 再部署 Koishi，确认 OneBot 适配器能连上 NapCat。
3. 如果服务器以前部署过 AI 版本，先执行 `scripts/disable-ai.sh`。
4. 依次执行需要的插件脚本：
   - `scripts/name.sh`
   - `scripts/help.sh`
   - `scripts/leave.sh`
   - `scripts/poke.sh`
   - `scripts/vedio.sh`
5. 每个脚本执行完都会自动重启 Koishi。

## 这个分支保留什么

- 昵称绑定
- 集合管理
- 帮助菜单
- 退群提醒
- 戳一戳
- B 站视频解析

## 这个分支移除什么

- AI 自由聊天
- `AI状态`
- `AI重载`
- `@东雪莲` 触发聊天
- 任何模型 API Key / Base URL / Skill 配置需求

## 为什么每个脚本都清理 AI

无 AI 分支的目标是“不让 bot 参与聊天”。  
所以每个部署脚本都会：

- 删除旧的 `koishi-plugin-dongxuelian-ai` 目录
- 从 `koishi.yml` 清理旧的 `dongxuelian-ai:*` 配置
- 清理当前插件的重复配置
- 写入当前插件的新配置
- 重启 Koishi

这样即使服务器以前部署过 AI 版本，也不会留下多个模块共存抢资源。

## 验证

查看配置里是否还有 AI：

```bash
grep -n "dongxuelian-ai" /root/koishi-app/koishi.yml
```

如果没有输出，说明 AI 已禁用。

查看最近日志：

```bash
journalctl -u koishi -n 120 --no-pager
```

重点确认没有：

- `duplicate plugin detected`
- `dongxuelian-ai loaded`
