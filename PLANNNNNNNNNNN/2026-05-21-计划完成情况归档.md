# 计划完成情况归档

> 归档时间：2026-05-21 10:18 CST
> 归档范围：`PLANNNNNNNNNNN/` 与 `待完成与待审核任务/`
> 本轮补充完成：`index.js` / `chat.js` 架构拆分收口

## 已归档并删除原计划

### `PLANNNNNNNNNNN/借鉴开发计划.md`

状态：已完成主体目标。

完成证据：

- Agent 引擎、工具注册、安全策略、Dashboard / Agent API、会话和自动路由已经落地。
- 相关模块已进入 `package.json` check 清单和 cascade 模块加载清单。
- 本轮 `npm test` 通过。

### `PLANNNNNNNNNNN/第二期借鉴开发计划.md`

状态：已完成。

完成证据：

- append、grep、execute_javascript、send_file_to_user、token usage、timezone、browser action、stats、context、skill-hub CLI 等能力已落地。
- cascade 已覆盖工具导出、危险工具策略、上下文压缩、Skill CLI 等防回退项。
- 本轮 `npm test` 通过。

### `PLANNNNNNNNNNN/第三期借鉴开发计划.md`

状态：已完成主体目标。

完成证据：

- `packages/agent-console/` 已存在，Dashboard 也保留 Agent 入口。
- Agent 队列、计划模式、主动推送、cron、memory、上下文压缩、浏览器补齐、PPTX skill 均有对应文件。
- `agent-phase3.test.js` 覆盖队列、计划、push、cron、浏览器动作等关键场景。
- 本轮 `npm test` 通过。

### `PLANNNNNNNNNNN/第五期开发计划.md`

状态：已完成主体目标，远程 Skill 生态仍按保守默认运行。

完成证据：

- Auto-Memory、Dream、Skill Pool、Workspace、Scanner、Hub 调度、GitHub adapter 已落地。
- `auto-memory.test.js`、`skill-market.test.js` 覆盖自动记忆、梦境整理、池/工作区安装、安全扫描和 Hub 基础行为。
- 远程下载类能力仍受 adapter、扫描和安装流程约束，不作为默认开放的无审查外部执行能力。
- 本轮 `npm test` 通过。

### `PLANNNNNNNNNNN/语音识别与合成计划.md`

状态：已完成。

完成证据：

- ASR、TTS、人格音色映射、语音克隆资产、临时语音风格、当前人格发声、随机语音概率配置均已落地。
- `voice-tts.test.js` 覆盖 TTS 风格、克隆模型、发送不引用源消息、当前人格语音、随机语音默认 10% 等场景。
- 本轮 `npm test` 通过。

### `待完成与待审核任务/TTS人格语音风格优化总方案.md`

状态：已完成。

完成证据：

- 后端不再按人格名硬编码风格，`voice_style` 来自人格配置和前端保存。
- `东雪莲说句话` 使用用户/群当前配置人格。
- Dashboard 试听、保存、清空风格链路已有测试覆盖。
- 本轮 `npm test` 通过。

### `待完成与待审核任务/架构优化升级方案.md`

状态：已完成。

完成证据：

- 语音克隆资产系统已从单文件上传升级为可管理、可复用、可追踪的 `voice_asset_id` 资产。
- `voice-assets.js`、Dashboard 人格编辑、TTS 解析和相关 scenario 已覆盖多版本、复用、删除引用者等行为。
- 本轮 `npm test` 通过。

## 已移出独立计划但不标记为完成

### `PLANNNNNNNNNNN/待确认可能的伪自由编码.md`

状态：已从独立待办移除，未作为“完成计划”归档。

原因：

- 该文件此前已处于删除状态。
- 其中部分条目仍能在现有代码里看到痕迹，例如搜索结果过滤、失败模式策略、工具上限和危险工具列表。
- 后续如继续处理，应转入 `待完成与待审核任务/《优化建议》.md` 或单独新建更小的可执行任务。

## 本轮完成的架构拆分

来源：备份审计报告中的架构治理方向，以及 `待完成与待审核任务/《优化建议》.md` 中“大文件仍有继续膨胀风险”的建议。

完成内容：

- `chat.js`：新增 `chat-memory.js`，把群记忆定时清理、直接记住/记下、记忆确认、记忆纠正、记忆提示时间戳迁出聊天主流程。
- `index.js`：把白名单、黑名单、敏感检测、事件抓取、随机回复概率、群聊语音概率等内联管理指令收口到 `admin-commands.js`。
- `package.json` 与 `cascade-test.js`：补入 `chat-memory.js` 的语法检查、模块加载、重复扫描和静态防线。

边界：

- `chat.js` 仍保留模型调用和聊天主流程所有权。
- `index.js` 仍保留 Koishi 入口和消息路由所有权。
- 没有把子模块反向 import `index.js`。

## 保留未归档的计划

### `PLANNNNNNNNNNN/服务器搜索与chat重构计划.md`

保留原因：

- Chat 轻量工具、图片历史追溯等主体能力已落地。
- MCP 远程浏览器仍没有完整服务端链路和线上可用验证，不能整体标记完成。

### `PLANNNNNNNNNNN/未完待续.md`

保留原因：长期路线清单，不是已完成计划。

### `PLANNNNNNNNNNN/风险.md`

保留原因：风险记录，不是已完成计划。

### `待完成与待审核任务/fetch方案.md`

保留原因：

- Phase 1 独立 `web_fetch` 已落地。
- URL 意图路由、Chat heavy handoff、统一 reader 等后续阶段仍需继续跟踪。

### `待完成与待审核任务/后续优化.md`

保留原因：

- Agent QQ 转述、图片工具、OOC 修复等多项已落地。
- MCP 浏览器接入 web-search 搜索链仍未完整完成。

### `待完成与待审核任务/《人格架构升级计划》.md`

保留原因：人格 schema、PersonaRuntimePlan、回复时机决策、证据化 profile、表达学习、Dashboard 人格实验室等仍是后续大计划。

### `待完成与待审核任务/《优化建议》.md`

保留原因：当前仍是长期优化索引，包含 regression、URL guard、日志脱敏、权限边界等未完成项。

## 本轮验证

- `npm run test:quick` 通过。
- `npm run test:scenario -- --grep random` 通过。
- `npm test` 通过：scenario 573 passed / 0 failed / 2 skipped，插件测试全部通过。

## 备注

- 本归档不包含任何真实服务器地址、SSH 信息、部署路径或私有 token。
- 后续删除新计划前，应先确认该计划没有未完成阶段；部分完成的计划只归档完成部分，原计划继续保留。
